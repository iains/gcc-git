//  { dg-additional-options "-fpreprocessed -w" }
# 0 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/async-coroutine-test.c++"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/async-coroutine-test.c++"
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/async-coroutine-test.c++"
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h"
       

# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-prelude.h" 1
# 25 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-prelude.h"
       

# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h"
       

# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h"
       

# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 1
# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h"
       
# 51 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h"

# 51 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h"

# 51 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3

# 89 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
# 1 "/usr/include/c++/v1/stddef.h" 1 3 4
# 39 "/usr/include/c++/v1/stddef.h" 3 4
# 1 "/usr/include/c++/v1/__config" 1 3 4
# 60 "/usr/include/c++/v1/__config" 3 4
       
# 61 "/usr/include/c++/v1/__config" 3
# 266 "/usr/include/c++/v1/__config" 3
# 1 "/usr/include/features.h" 1 3 4
# 473 "/usr/include/features.h" 3 4
# 1 "/usr/include/sys/cdefs.h" 1 3 4
# 462 "/usr/include/sys/cdefs.h" 3 4
# 1 "/usr/include/bits/wordsize.h" 1 3 4
# 463 "/usr/include/sys/cdefs.h" 2 3 4
# 1 "/usr/include/bits/long-double.h" 1 3 4
# 464 "/usr/include/sys/cdefs.h" 2 3 4
# 474 "/usr/include/features.h" 2 3 4
# 497 "/usr/include/features.h" 3 4
# 1 "/usr/include/gnu/stubs.h" 1 3 4
# 10 "/usr/include/gnu/stubs.h" 3 4
# 1 "/usr/include/gnu/stubs-64.h" 1 3 4
# 11 "/usr/include/gnu/stubs.h" 2 3 4
# 498 "/usr/include/features.h" 2 3 4
# 267 "/usr/include/c++/v1/__config" 2 3
# 820 "/usr/include/c++/v1/__config" 3

# 820 "/usr/include/c++/v1/__config" 3
namespace std { inline namespace __1 { } }
# 40 "/usr/include/c++/v1/stddef.h" 2 3 4


       
# 43 "/usr/include/c++/v1/stddef.h" 3


# 1 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stddef.h" 1 3 4
# 143 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stddef.h" 3 4
typedef long int ptrdiff_t;
# 209 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stddef.h" 3 4
typedef long unsigned int size_t;
# 415 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stddef.h" 3 4
typedef struct {
  long long __max_align_ll __attribute__((__aligned__(__alignof__(long long))));
  long double __max_align_ld __attribute__((__aligned__(__alignof__(long double))));
# 426 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stddef.h" 3 4
} max_align_t;






  typedef decltype(nullptr) nullptr_t;
# 46 "/usr/include/c++/v1/stddef.h" 2 3



extern "C++" {
# 1 "/usr/include/c++/v1/__nullptr" 1 3 4
# 13 "/usr/include/c++/v1/__nullptr" 3 4
# 1 "/usr/include/c++/v1/__config" 1 3 4
# 14 "/usr/include/c++/v1/__nullptr" 2 3 4


       
# 17 "/usr/include/c++/v1/__nullptr" 3
# 54 "/usr/include/c++/v1/__nullptr" 3
namespace std
{
    typedef decltype(nullptr) nullptr_t;
}
# 51 "/usr/include/c++/v1/stddef.h" 2 3
using std::nullptr_t;
}
# 90 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 2 3
# 1 "/usr/include/c++/v1/initializer_list" 1 3 4
# 45 "/usr/include/c++/v1/initializer_list" 3 4
# 1 "/usr/include/c++/v1/__config" 1 3 4
# 46 "/usr/include/c++/v1/initializer_list" 2 3 4
# 1 "/usr/include/c++/v1/cstddef" 1 3 4
# 36 "/usr/include/c++/v1/cstddef" 3 4
# 1 "/usr/include/c++/v1/__config" 1 3 4
# 37 "/usr/include/c++/v1/cstddef" 2 3 4
# 1 "/usr/include/c++/v1/version" 1 3 4
# 166 "/usr/include/c++/v1/version" 3 4
# 1 "/usr/include/c++/v1/__config" 1 3 4
# 167 "/usr/include/c++/v1/version" 2 3 4


       
# 170 "/usr/include/c++/v1/version" 3
# 38 "/usr/include/c++/v1/cstddef" 2 3 4


       
# 41 "/usr/include/c++/v1/cstddef" 3



# 1 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stddef.h" 1 3 4
# 45 "/usr/include/c++/v1/cstddef" 2 3


namespace std { inline namespace __1 {

using ::ptrdiff_t;
using ::size_t;


using ::max_align_t;


template <class _Tp> struct __libcpp_is_integral { enum { value = 0 }; };
template <> struct __libcpp_is_integral<bool> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<char> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<signed char> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<unsigned char> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<wchar_t> { enum { value = 1 }; };

template <> struct __libcpp_is_integral<char8_t> { enum { value = 1 }; };


template <> struct __libcpp_is_integral<char16_t> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<char32_t> { enum { value = 1 }; };

template <> struct __libcpp_is_integral<short> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<unsigned short> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<int> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<unsigned int> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<long> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<unsigned long> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<long long> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<unsigned long long> { enum { value = 1 }; };

template <> struct __libcpp_is_integral<__int128_t> { enum { value = 1 }; };
template <> struct __libcpp_is_integral<__uint128_t> { enum { value = 1 }; };


} }


namespace std
{
enum class byte : unsigned char {};


template <bool> struct __enable_if_integral_imp {};
template <> struct __enable_if_integral_imp<true> { using type = byte; };
template <class _Tp> using _EnableByteOverload = typename __enable_if_integral_imp<__libcpp_is_integral<_Tp>::value>::type;

constexpr byte operator| (byte __lhs, byte __rhs) noexcept
{
    return static_cast<byte>(
      static_cast<unsigned char>(
         static_cast<unsigned int>(__lhs) | static_cast<unsigned int>(__rhs)
    ));
}

constexpr byte& operator|=(byte& __lhs, byte __rhs) noexcept
{ return __lhs = __lhs | __rhs; }

constexpr byte operator& (byte __lhs, byte __rhs) noexcept
{
    return static_cast<byte>(
      static_cast<unsigned char>(
         static_cast<unsigned int>(__lhs) & static_cast<unsigned int>(__rhs)
    ));
}

constexpr byte& operator&=(byte& __lhs, byte __rhs) noexcept
{ return __lhs = __lhs & __rhs; }

constexpr byte operator^ (byte __lhs, byte __rhs) noexcept
{
    return static_cast<byte>(
      static_cast<unsigned char>(
         static_cast<unsigned int>(__lhs) ^ static_cast<unsigned int>(__rhs)
    ));
}

constexpr byte& operator^=(byte& __lhs, byte __rhs) noexcept
{ return __lhs = __lhs ^ __rhs; }

constexpr byte operator~ (byte __b) noexcept
{
    return static_cast<byte>(
      static_cast<unsigned char>(
        ~static_cast<unsigned int>(__b)
    ));
}
template <class _Integer>
  constexpr _EnableByteOverload<_Integer> &
  operator<<=(byte& __lhs, _Integer __shift) noexcept
  { return __lhs = __lhs << __shift; }

template <class _Integer>
  constexpr _EnableByteOverload<_Integer>
  operator<< (byte __lhs, _Integer __shift) noexcept
  { return static_cast<byte>(static_cast<unsigned char>(static_cast<unsigned int>(__lhs) << __shift)); }

template <class _Integer>
  constexpr _EnableByteOverload<_Integer> &
  operator>>=(byte& __lhs, _Integer __shift) noexcept
  { return __lhs = __lhs >> __shift; }

template <class _Integer>
  constexpr _EnableByteOverload<_Integer>
  operator>> (byte __lhs, _Integer __shift) noexcept
  { return static_cast<byte>(static_cast<unsigned char>(static_cast<unsigned int>(__lhs) >> __shift)); }

template <class _Integer, class = _EnableByteOverload<_Integer> >
  constexpr _Integer
  to_integer(byte __b) noexcept { return static_cast<_Integer>(__b); }
}
# 47 "/usr/include/c++/v1/initializer_list" 2 3 4


       
# 50 "/usr/include/c++/v1/initializer_list" 3


namespace std
{



template<class _Ep>
class __attribute__ ((__visibility__("default"))) initializer_list
{
    const _Ep* __begin_;
    size_t __size_;

    __attribute__ ((__visibility__("hidden"))) __attribute__ ((__always_inline__))
    constexpr
    initializer_list(const _Ep* __b, size_t __s) noexcept
        : __begin_(__b),
          __size_(__s)
        {}
public:
    typedef _Ep value_type;
    typedef const _Ep& reference;
    typedef const _Ep& const_reference;
    typedef size_t size_type;

    typedef const _Ep* iterator;
    typedef const _Ep* const_iterator;

    __attribute__ ((__visibility__("hidden"))) __attribute__ ((__always_inline__))
    constexpr
    initializer_list() noexcept : __begin_(nullptr), __size_(0) {}

    __attribute__ ((__visibility__("hidden"))) __attribute__ ((__always_inline__))
    constexpr
    size_t size() const noexcept {return __size_;}

    __attribute__ ((__visibility__("hidden"))) __attribute__ ((__always_inline__))
    constexpr
    const _Ep* begin() const noexcept {return __begin_;}

    __attribute__ ((__visibility__("hidden"))) __attribute__ ((__always_inline__))
    constexpr
    const _Ep* end() const noexcept {return __begin_ + __size_;}
};

template<class _Ep>
inline __attribute__ ((__visibility__("hidden"))) __attribute__ ((__always_inline__))
constexpr
const _Ep*
begin(initializer_list<_Ep> __il) noexcept
{
    return __il.begin();
}

template<class _Ep>
inline __attribute__ ((__visibility__("hidden"))) __attribute__ ((__always_inline__))
constexpr
const _Ep*
end(initializer_list<_Ep> __il) noexcept
{
    return __il.end();
}



}
# 91 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 2 3
# 109 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
namespace kj {

typedef unsigned int uint;
typedef unsigned char byte;
# 237 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
namespace _ {

void inlineRequireFailure( const char* file, int line, const char* expectation, const char* macroArgs, const char* message = nullptr) __attribute__((noreturn))

                                   ;

void unreachable() __attribute__((noreturn));

}
# 356 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
template <typename T> struct NoInfer_ { typedef T Type; };
template <typename T> using NoInfer = typename NoInfer_<T>::Type;



template <typename T> struct RemoveConst_ { typedef T Type; };
template <typename T> struct RemoveConst_<const T> { typedef T Type; };
template <typename T> using RemoveConst = typename RemoveConst_<T>::Type;

template <typename> struct IsLvalueReference_ { static constexpr bool value = false; };
template <typename T> struct IsLvalueReference_<T&> { static constexpr bool value = true; };
template <typename T>
inline constexpr bool isLvalueReference() { return IsLvalueReference_<T>::value; }

template <typename T> struct Decay_ { typedef T Type; };
template <typename T> struct Decay_<T&> { typedef typename Decay_<T>::Type Type; };
template <typename T> struct Decay_<T&&> { typedef typename Decay_<T>::Type Type; };
template <typename T> struct Decay_<T[]> { typedef typename Decay_<T*>::Type Type; };
template <typename T> struct Decay_<const T[]> { typedef typename Decay_<const T*>::Type Type; };
template <typename T, size_t s> struct Decay_<T[s]> { typedef typename Decay_<T*>::Type Type; };
template <typename T, size_t s> struct Decay_<const T[s]> { typedef typename Decay_<const T*>::Type Type; };
template <typename T> struct Decay_<const T> { typedef typename Decay_<T>::Type Type; };
template <typename T> struct Decay_<volatile T> { typedef typename Decay_<T>::Type Type; };
template <typename T> using Decay = typename Decay_<T>::Type;

template <bool b> struct EnableIf_;
template <> struct EnableIf_<true> { typedef void Type; };
template <bool b> using EnableIf = typename EnableIf_<b>::Type;





template <typename...> struct VoidSfinae_ { using Type = void; };
template <typename... Ts> using VoidSfinae = typename VoidSfinae_<Ts...>::Type;


template <typename T>
T instance() noexcept;



struct DisallowConstCopy {
# 431 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
};
# 451 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
template <typename T>
struct DisallowConstCopyIfNotConst: public DisallowConstCopy {



};

template <typename T>
struct DisallowConstCopyIfNotConst<const T> {};

template <typename T> struct IsConst_ { static constexpr bool value = false; };
template <typename T> struct IsConst_<const T> { static constexpr bool value = true; };
template <typename T> constexpr bool isConst() { return IsConst_<T>::value; }

template <typename T> struct EnableIfNotConst_ { typedef T Type; };
template <typename T> struct EnableIfNotConst_<const T>;
template <typename T> using EnableIfNotConst = typename EnableIfNotConst_<T>::Type;

template <typename T> struct EnableIfConst_;
template <typename T> struct EnableIfConst_<const T> { typedef T Type; };
template <typename T> using EnableIfConst = typename EnableIfConst_<T>::Type;

template <typename T> struct RemoveConstOrDisable_ { struct Type; };
template <typename T> struct RemoveConstOrDisable_<const T> { typedef T Type; };
template <typename T> using RemoveConstOrDisable = typename RemoveConstOrDisable_<T>::Type;

template <typename T> struct IsReference_ { static constexpr bool value = false; };
template <typename T> struct IsReference_<T&> { static constexpr bool value = true; };
template <typename T> constexpr bool isReference() { return IsReference_<T>::value; }

template <typename From, typename To>
struct PropagateConst_ { typedef To Type; };
template <typename From, typename To>
struct PropagateConst_<const From, To> { typedef const To Type; };
template <typename From, typename To>
using PropagateConst = typename PropagateConst_<From, To>::Type;

namespace _ {

template <typename T>
T refIfLvalue(T&&);

}
# 504 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
template <typename T, typename U> struct IsSameType_ { static constexpr bool value = false; };
template <typename T> struct IsSameType_<T, T> { static constexpr bool value = true; };
template <typename T, typename U> constexpr bool isSameType() { return IsSameType_<T, U>::value; }

template <typename T>
struct CanConvert_ {
  static int sfinae(T);
  static bool sfinae(...);
};

template <typename T, typename U>
constexpr bool canConvert() {
  return sizeof(CanConvert_<U>::sfinae(instance<T>())) == sizeof(int);
}
# 534 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
template <typename T>
constexpr bool canMemcpy() {



  return __is_trivially_constructible(T, const T&) && __is_trivially_assignable(T, const T&);
}




template <typename T>
class Badge {
# 569 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
private:
  Badge() {}
  friend T;
};
# 582 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
template<typename T> constexpr T&& mv(T& t) noexcept { return static_cast<T&&>(t); }
template<typename T> constexpr T&& fwd(NoInfer<T>& t) noexcept { return static_cast<T&&>(t); }

template<typename T> constexpr T cp(T& t) noexcept { return t; }
template<typename T> constexpr T cp(const T& t) noexcept { return t; }


template <typename T, typename U, bool takeT, bool uOK = true> struct ChooseType_;
template <typename T, typename U> struct ChooseType_<T, U, true, true> { typedef T Type; };
template <typename T, typename U> struct ChooseType_<T, U, true, false> { typedef T Type; };
template <typename T, typename U> struct ChooseType_<T, U, false, true> { typedef U Type; };

template <typename T, typename U>
using WiderType = typename ChooseType_<T, U, sizeof(T) >= sizeof(U)>::Type;

template <typename T, typename U>
inline constexpr auto min(T&& a, U&& b) -> WiderType<Decay<T>, Decay<U>> {
  return a < b ? WiderType<Decay<T>, Decay<U>>(a) : WiderType<Decay<T>, Decay<U>>(b);
}

template <typename T, typename U>
inline constexpr auto max(T&& a, U&& b) -> WiderType<Decay<T>, Decay<U>> {
  return a > b ? WiderType<Decay<T>, Decay<U>>(a) : WiderType<Decay<T>, Decay<U>>(b);
}

template <typename T, size_t s>
inline constexpr size_t size(T (&arr)[s]) { return s; }
template <typename T>
inline constexpr size_t size(T&& arr) { return arr.size(); }



class MaxValue_ {
private:
  template <typename T>
  inline constexpr T maxSigned() const {
    return (1ull << (sizeof(T) * 8 - 1)) - 1;
  }
  template <typename T>
  inline constexpr T maxUnsigned() const {
    return ~static_cast<T>(0u);
  }

public:



  inline constexpr operator signed char() const { return MaxValue_::maxSigned < signed char>(); } inline constexpr operator unsigned char() const { return MaxValue_::maxUnsigned<unsigned char>(); }
  inline constexpr operator signed short() const { return MaxValue_::maxSigned < signed short>(); } inline constexpr operator unsigned short() const { return MaxValue_::maxUnsigned<unsigned short>(); }
  inline constexpr operator signed int() const { return MaxValue_::maxSigned < signed int>(); } inline constexpr operator unsigned int() const { return MaxValue_::maxUnsigned<unsigned int>(); }
  inline constexpr operator signed long() const { return MaxValue_::maxSigned < signed long>(); } inline constexpr operator unsigned long() const { return MaxValue_::maxUnsigned<unsigned long>(); }
  inline constexpr operator signed long long() const { return MaxValue_::maxSigned < signed long long>(); } inline constexpr operator unsigned long long() const { return MaxValue_::maxUnsigned<unsigned long long>(); }


  inline constexpr operator char() const {


    return char(-1) < 0 ? MaxValue_::maxSigned<char>()
                        : MaxValue_::maxUnsigned<char>();
  }
};

class MinValue_ {
private:
  template <typename T>
  inline constexpr T minSigned() const {
    return 1ull << (sizeof(T) * 8 - 1);
  }
  template <typename T>
  inline constexpr T minUnsigned() const {
    return 0u;
  }

public:



  inline constexpr operator signed char() const { return MinValue_::minSigned < signed char>(); } inline constexpr operator unsigned char() const { return MinValue_::minUnsigned<unsigned char>(); }
  inline constexpr operator signed short() const { return MinValue_::minSigned < signed short>(); } inline constexpr operator unsigned short() const { return MinValue_::minUnsigned<unsigned short>(); }
  inline constexpr operator signed int() const { return MinValue_::minSigned < signed int>(); } inline constexpr operator unsigned int() const { return MinValue_::minUnsigned<unsigned int>(); }
  inline constexpr operator signed long() const { return MinValue_::minSigned < signed long>(); } inline constexpr operator unsigned long() const { return MinValue_::minUnsigned<unsigned long>(); }
  inline constexpr operator signed long long() const { return MinValue_::minSigned < signed long long>(); } inline constexpr operator unsigned long long() const { return MinValue_::minUnsigned<unsigned long long>(); }


  inline constexpr operator char() const {


    return char(-1) < 0 ? MinValue_::minSigned<char>()
                        : MinValue_::minUnsigned<char>();
  }
};

static constexpr MaxValue_ maxValue = MaxValue_();






static constexpr MinValue_ minValue = MinValue_();






template <typename T>
inline bool operator==(T t, MaxValue_) { return t == Decay<T>(maxValue); }
template <typename T>
inline bool operator==(T t, MinValue_) { return t == Decay<T>(minValue); }

template <uint bits>
inline constexpr unsigned long long maxValueForBits() {



  return (bits == 64 ? 0 : (1ull << bits)) - 1;
}

struct ThrowOverflow {



  void operator()() const;
};


inline constexpr float inf() { return __builtin_huge_valf(); }
inline constexpr float nan() { return __builtin_nanf(""); }





inline constexpr bool isNaN(float f) { return f != f; }
inline constexpr bool isNaN(double f) { return f != f; }

inline int popCount(unsigned int x) {




  return __builtin_popcount(x);

}




template <typename T>
class Range {
public:
  inline constexpr Range(const T& begin, const T& end): begin_(begin), end_(end) {}
  inline explicit constexpr Range(const T& end): begin_(0), end_(end) {}

  class Iterator {
  public:
    Iterator() = default;
    inline Iterator(const T& value): value(value) {}

    inline const T& operator* () const { return value; }
    inline const T& operator[](size_t index) const { return value + index; }
    inline Iterator& operator++() { ++value; return *this; }
    inline Iterator operator++(int) { return Iterator(value++); }
    inline Iterator& operator--() { --value; return *this; }
    inline Iterator operator--(int) { return Iterator(value--); }
    inline Iterator& operator+=(ptrdiff_t amount) { value += amount; return *this; }
    inline Iterator& operator-=(ptrdiff_t amount) { value -= amount; return *this; }
    inline Iterator operator+ (ptrdiff_t amount) const { return Iterator(value + amount); }
    inline Iterator operator- (ptrdiff_t amount) const { return Iterator(value - amount); }
    inline ptrdiff_t operator- (const Iterator& other) const { return value - other.value; }

    inline bool operator==(const Iterator& other) const { return value == other.value; }
    inline bool operator!=(const Iterator& other) const { return value != other.value; }
    inline bool operator<=(const Iterator& other) const { return value <= other.value; }
    inline bool operator>=(const Iterator& other) const { return value >= other.value; }
    inline bool operator< (const Iterator& other) const { return value < other.value; }
    inline bool operator> (const Iterator& other) const { return value > other.value; }

  private:
    T value;
  };

  inline Iterator begin() const { return Iterator(begin_); }
  inline Iterator end() const { return Iterator(end_); }

  inline auto size() const -> decltype(instance<T>() - instance<T>()) { return end_ - begin_; }

private:
  T begin_;
  T end_;
};

template <typename T, typename U>
inline constexpr Range<WiderType<Decay<T>, Decay<U>>> range(T begin, U end) {
  return Range<WiderType<Decay<T>, Decay<U>>>(begin, end);
}

template <typename T>
inline constexpr Range<Decay<T>> range(T begin, T end) { return Range<Decay<T>>(begin, end); }






template <typename T>
inline constexpr Range<Decay<T>> zeroTo(T end) { return Range<Decay<T>>(end); }






template <typename T>
inline constexpr Range<size_t> indices(T&& container) {




  return range<size_t>(0, kj::size(container));
}

template <typename T>
class Repeat {
public:
  inline constexpr Repeat(const T& value, size_t count): value(value), count(count) {}

  class Iterator {
  public:
    Iterator() = default;
    inline Iterator(const T& value, size_t index): value(value), index(index) {}

    inline const T& operator* () const { return value; }
    inline const T& operator[](ptrdiff_t index) const { return value; }
    inline Iterator& operator++() { ++index; return *this; }
    inline Iterator operator++(int) { return Iterator(value, index++); }
    inline Iterator& operator--() { --index; return *this; }
    inline Iterator operator--(int) { return Iterator(value, index--); }
    inline Iterator& operator+=(ptrdiff_t amount) { index += amount; return *this; }
    inline Iterator& operator-=(ptrdiff_t amount) { index -= amount; return *this; }
    inline Iterator operator+ (ptrdiff_t amount) const { return Iterator(value, index + amount); }
    inline Iterator operator- (ptrdiff_t amount) const { return Iterator(value, index - amount); }
    inline ptrdiff_t operator- (const Iterator& other) const { return index - other.index; }

    inline bool operator==(const Iterator& other) const { return index == other.index; }
    inline bool operator!=(const Iterator& other) const { return index != other.index; }
    inline bool operator<=(const Iterator& other) const { return index <= other.index; }
    inline bool operator>=(const Iterator& other) const { return index >= other.index; }
    inline bool operator< (const Iterator& other) const { return index < other.index; }
    inline bool operator> (const Iterator& other) const { return index > other.index; }

  private:
    T value;
    size_t index;
  };

  inline Iterator begin() const { return Iterator(value, 0); }
  inline Iterator end() const { return Iterator(value, count); }

  inline size_t size() const { return count; }
  inline const T& operator[](ptrdiff_t) const { return value; }

private:
  T value;
  size_t count;
};

template <typename T>
inline constexpr Repeat<Decay<T>> repeat(T&& value, size_t count) {



  return Repeat<Decay<T>>(value, count);
}

template <typename Inner, class Mapping>
class MappedIterator: private Mapping {



public:
  template <typename... Params>
  MappedIterator(Inner inner, Params&&... params)
      : Mapping(kj::fwd<Params>(params)...), inner(inner) {}

  inline auto operator->() const { return &Mapping::map(*inner); }
  inline decltype(auto) operator* () const { return Mapping::map(*inner); }
  inline decltype(auto) operator[](size_t index) const { return Mapping::map(inner[index]); }
  inline MappedIterator& operator++() { ++inner; return *this; }
  inline MappedIterator operator++(int) { return MappedIterator(inner++, *this); }
  inline MappedIterator& operator--() { --inner; return *this; }
  inline MappedIterator operator--(int) { return MappedIterator(inner--, *this); }
  inline MappedIterator& operator+=(ptrdiff_t amount) { inner += amount; return *this; }
  inline MappedIterator& operator-=(ptrdiff_t amount) { inner -= amount; return *this; }
  inline MappedIterator operator+ (ptrdiff_t amount) const {
    return MappedIterator(inner + amount, *this);
  }
  inline MappedIterator operator- (ptrdiff_t amount) const {
    return MappedIterator(inner - amount, *this);
  }
  inline ptrdiff_t operator- (const MappedIterator& other) const { return inner - other.inner; }

  inline bool operator==(const MappedIterator& other) const { return inner == other.inner; }
  inline bool operator!=(const MappedIterator& other) const { return inner != other.inner; }
  inline bool operator<=(const MappedIterator& other) const { return inner <= other.inner; }
  inline bool operator>=(const MappedIterator& other) const { return inner >= other.inner; }
  inline bool operator< (const MappedIterator& other) const { return inner < other.inner; }
  inline bool operator> (const MappedIterator& other) const { return inner > other.inner; }

private:
  Inner inner;
};

template <typename Inner, typename Mapping>
class MappedIterable: private Mapping {



public:
  template <typename... Params>
  MappedIterable(Inner inner, Params&&... params)
      : Mapping(kj::fwd<Params>(params)...), inner(inner) {}

  typedef Decay<decltype(instance<Inner>().begin())> InnerIterator;
  typedef MappedIterator<InnerIterator, Mapping> Iterator;
  typedef Decay<decltype(instance<const Inner>().begin())> InnerConstIterator;
  typedef MappedIterator<InnerConstIterator, Mapping> ConstIterator;

  inline Iterator begin() { return { inner.begin(), (Mapping&)*this }; }
  inline Iterator end() { return { inner.end(), (Mapping&)*this }; }
  inline ConstIterator begin() const { return { inner.begin(), (const Mapping&)*this }; }
  inline ConstIterator end() const { return { inner.end(), (const Mapping&)*this }; }

private:
  Inner inner;
};
# 929 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
namespace _ {
struct PlacementNew {};
}
}

inline void* operator new(size_t, kj::_::PlacementNew, void* __p) noexcept {
  return __p;
}

inline void operator delete(void*, kj::_::PlacementNew, void* __p) noexcept {}

namespace kj {

template <typename T, typename... Params>
inline void ctor(T& location, Params&&... params) {
  new (_::PlacementNew(), &location) T(kj::fwd<Params>(params)...);
}

template <typename T>
inline void dtor(T& location) {
  location.~T();
}
# 976 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
template <typename T>
class Maybe;

namespace _ {

template <typename T>
class NullableValue {



public:
  inline NullableValue(NullableValue&& other)
      : isSet(other.isSet) {
    if (isSet) {
      ctor(value, kj::mv(other.value));
    }
  }
  inline NullableValue(const NullableValue& other)
      : isSet(other.isSet) {
    if (isSet) {
      ctor(value, other.value);
    }
  }
  inline NullableValue(NullableValue& other)
      : isSet(other.isSet) {
    if (isSet) {
      ctor(value, other.value);
    }
  }
  inline ~NullableValue()





      noexcept(noexcept(instance<T&>().~T()))

  {
    if (isSet) {
      dtor(value);
    }
  }

  inline T& operator*() & { return value; }
  inline const T& operator*() const & { return value; }
  inline T&& operator*() && { return kj::mv(value); }
  inline const T&& operator*() const && { return kj::mv(value); }
  inline T* operator->() { return &value; }
  inline const T* operator->() const { return &value; }
  inline operator T*() { return isSet ? &value : nullptr; }
  inline operator const T*() const { return isSet ? &value : nullptr; }

  template <typename... Params>
  inline T& emplace(Params&&... params) {
    if (isSet) {
      isSet = false;
      dtor(value);
    }
    ctor(value, kj::fwd<Params>(params)...);
    isSet = true;
    return value;
  }

  inline NullableValue(): isSet(false) {}
  inline NullableValue(T&& t)
      : isSet(true) {
    ctor(value, kj::mv(t));
  }
  inline NullableValue(T& t)
      : isSet(true) {
    ctor(value, t);
  }
  inline NullableValue(const T& t)
      : isSet(true) {
    ctor(value, t);
  }
  template <typename U>
  inline NullableValue(NullableValue<U>&& other)
      : isSet(other.isSet) {
    if (isSet) {
      ctor(value, kj::mv(other.value));
    }
  }
  template <typename U>
  inline NullableValue(const NullableValue<U>& other)
      : isSet(other.isSet) {
    if (isSet) {
      ctor(value, other.value);
    }
  }
  template <typename U>
  inline NullableValue(const NullableValue<U&>& other)
      : isSet(other.isSet) {
    if (isSet) {
      ctor(value, *other.ptr);
    }
  }
  inline NullableValue(decltype(nullptr)): isSet(false) {}

  inline NullableValue& operator=(NullableValue&& other) {
    if (&other != this) {

      if (isSet) {
        isSet = false;
        dtor(value);
      }
      if (other.isSet) {
        ctor(value, kj::mv(other.value));
        isSet = true;
      }
    }
    return *this;
  }

  inline NullableValue& operator=(NullableValue& other) {
    if (&other != this) {

      if (isSet) {
        isSet = false;
        dtor(value);
      }
      if (other.isSet) {
        ctor(value, other.value);
        isSet = true;
      }
    }
    return *this;
  }

  inline NullableValue& operator=(const NullableValue& other) {
    if (&other != this) {

      if (isSet) {
        isSet = false;
        dtor(value);
      }
      if (other.isSet) {
        ctor(value, other.value);
        isSet = true;
      }
    }
    return *this;
  }

  inline NullableValue& operator=(T&& other) { emplace(kj::mv(other)); return *this; }
  inline NullableValue& operator=(T& other) { emplace(other); return *this; }
  inline NullableValue& operator=(const T& other) { emplace(other); return *this; }
  template <typename U>
  inline NullableValue& operator=(NullableValue<U>&& other) {
    if (other.isSet) {
      emplace(kj::mv(other.value));
    } else {
      *this = nullptr;
    }
    return *this;
  }
  template <typename U>
  inline NullableValue& operator=(const NullableValue<U>& other) {
    if (other.isSet) {
      emplace(other.value);
    } else {
      *this = nullptr;
    }
    return *this;
  }
  template <typename U>
  inline NullableValue& operator=(const NullableValue<U&>& other) {
    if (other.isSet) {
      emplace(other.value);
    } else {
      *this = nullptr;
    }
    return *this;
  }
  inline NullableValue& operator=(decltype(nullptr)) {
    if (isSet) {
      isSet = false;
      dtor(value);
    }
    return *this;
  }

  inline bool operator==(decltype(nullptr)) const { return !isSet; }
  inline bool operator!=(decltype(nullptr)) const { return isSet; }

  NullableValue(const T* t) = delete;
  NullableValue& operator=(const T* other) = delete;



private:
  bool isSet;
# 1176 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
  union {
    T value;
  };





  friend class kj::Maybe<T>;
  template <typename U>
  friend NullableValue<U>&& readMaybe(Maybe<U>&& maybe);
};

template <typename T>
inline NullableValue<T>&& readMaybe(Maybe<T>&& maybe) { return kj::mv(maybe.ptr); }
template <typename T>
inline T* readMaybe(Maybe<T>& maybe) { return maybe.ptr; }
template <typename T>
inline const T* readMaybe(const Maybe<T>& maybe) { return maybe.ptr; }
template <typename T>
inline T* readMaybe(Maybe<T&>&& maybe) { return maybe.ptr; }
template <typename T>
inline T* readMaybe(const Maybe<T&>& maybe) { return maybe.ptr; }

template <typename T>
inline T* readMaybe(T* ptr) { return ptr; }


}



template <typename T>
class Maybe {




public:
  Maybe(): ptr(nullptr) {}
  Maybe(T&& t): ptr(kj::mv(t)) {}
  Maybe(T& t): ptr(t) {}
  Maybe(const T& t): ptr(t) {}
  Maybe(Maybe&& other): ptr(kj::mv(other.ptr)) { other = nullptr; }
  Maybe(const Maybe& other): ptr(other.ptr) {}
  Maybe(Maybe& other): ptr(other.ptr) {}

  template <typename U>
  Maybe(Maybe<U>&& other) {
    if (auto val = ::kj::_::readMaybe(kj::mv(other))) {
      ptr.emplace(kj::mv(*val));
      other = nullptr;
    }
  }
  template <typename U>
  Maybe(Maybe<U&>&& other) {
    if (auto val = ::kj::_::readMaybe(other)) {
      ptr.emplace(*val);
      other = nullptr;
    }
  }
  template <typename U>
  Maybe(const Maybe<U>& other) {
    if (auto val = ::kj::_::readMaybe(other)) {
      ptr.emplace(*val);
    }
  }

  Maybe(decltype(nullptr)): ptr(nullptr) {}

  template <typename... Params>
  inline T& emplace(Params&&... params) {




    return ptr.emplace(kj::fwd<Params>(params)...);
  }

  inline Maybe& operator=(T&& other) { ptr = kj::mv(other); return *this; }
  inline Maybe& operator=(T& other) { ptr = other; return *this; }
  inline Maybe& operator=(const T& other) { ptr = other; return *this; }

  inline Maybe& operator=(Maybe&& other) { ptr = kj::mv(other.ptr); other = nullptr; return *this; }
  inline Maybe& operator=(Maybe& other) { ptr = other.ptr; return *this; }
  inline Maybe& operator=(const Maybe& other) { ptr = other.ptr; return *this; }

  template <typename U>
  Maybe& operator=(Maybe<U>&& other) {
    if (auto val = ::kj::_::readMaybe(kj::mv(other))) {
      ptr.emplace(kj::mv(*val));
      other = nullptr;
    } else {
      ptr = nullptr;
    }
    return *this;
  }
  template <typename U>
  Maybe& operator=(const Maybe<U>& other) {
    if (auto val = ::kj::_::readMaybe(other)) {
      ptr.emplace(*val);
    } else {
      ptr = nullptr;
    }
    return *this;
  }

  inline Maybe& operator=(decltype(nullptr)) { ptr = nullptr; return *this; }

  inline bool operator==(decltype(nullptr)) const { return ptr == nullptr; }
  inline bool operator!=(decltype(nullptr)) const { return ptr != nullptr; }

  Maybe(const T* t) = delete;
  Maybe& operator=(const T* other) = delete;



  T& orDefault(T& defaultValue) & {
    if (ptr == nullptr) {
      return defaultValue;
    } else {
      return *ptr;
    }
  }
  const T& orDefault(const T& defaultValue) const & {
    if (ptr == nullptr) {
      return defaultValue;
    } else {
      return *ptr;
    }
  }
  T&& orDefault(T&& defaultValue) && {
    if (ptr == nullptr) {
      return kj::mv(defaultValue);
    } else {
      return kj::mv(*ptr);
    }
  }
  const T&& orDefault(const T&& defaultValue) const && {
    if (ptr == nullptr) {
      return kj::mv(defaultValue);
    } else {
      return kj::mv(*ptr);
    }
  }

  template <typename Func>
  auto map(Func&& f) & -> Maybe<decltype(f(instance<T&>()))> {
    if (ptr == nullptr) {
      return nullptr;
    } else {
      return f(*ptr);
    }
  }

  template <typename Func>
  auto map(Func&& f) const & -> Maybe<decltype(f(instance<const T&>()))> {
    if (ptr == nullptr) {
      return nullptr;
    } else {
      return f(*ptr);
    }
  }

  template <typename Func>
  auto map(Func&& f) && -> Maybe<decltype(f(instance<T&&>()))> {
    if (ptr == nullptr) {
      return nullptr;
    } else {
      return f(kj::mv(*ptr));
    }
  }

  template <typename Func>
  auto map(Func&& f) const && -> Maybe<decltype(f(instance<const T&&>()))> {
    if (ptr == nullptr) {
      return nullptr;
    } else {
      return f(kj::mv(*ptr));
    }
  }

private:
  _::NullableValue<T> ptr;

  template <typename U>
  friend class Maybe;
  template <typename U>
  friend _::NullableValue<U>&& _::readMaybe(Maybe<U>&& maybe);
  template <typename U>
  friend U* _::readMaybe(Maybe<U>& maybe);
  template <typename U>
  friend const U* _::readMaybe(const Maybe<U>& maybe);
};

template <typename T>
class Maybe<T&> {
public:
  constexpr Maybe(): ptr(nullptr) {}
  constexpr Maybe(T& t): ptr(&t) {}
  constexpr Maybe(T* t): ptr(t) {}

  inline constexpr Maybe(PropagateConst<T, Maybe>& other): ptr(other.ptr) {}
# 1389 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
  inline constexpr Maybe(Maybe&& other): ptr(other.ptr) { other.ptr = nullptr; }

  template <typename U>
  inline constexpr Maybe(Maybe<U&>& other): ptr(other.ptr) {}
  template <typename U>
  inline constexpr Maybe(const Maybe<U&>& other): ptr(const_cast<const U*>(other.ptr)) {}
  template <typename U>
  inline constexpr Maybe(Maybe<U&>&& other): ptr(other.ptr) { other.ptr = nullptr; }
  template <typename U>
  inline constexpr Maybe(const Maybe<U&>&& other) = delete;
  template <typename U>
  constexpr Maybe(Maybe<U>& other): ptr(other.ptr.operator U*()) {}
  template <typename U>
  constexpr Maybe(const Maybe<U>& other): ptr(other.ptr.operator const U*()) {}
  inline constexpr Maybe(decltype(nullptr)): ptr(nullptr) {}

  inline Maybe& operator=(T& other) { ptr = &other; return *this; }
  inline Maybe& operator=(T* other) { ptr = other; return *this; }
  inline Maybe& operator=(PropagateConst<T, Maybe>& other) { ptr = other.ptr; return *this; }
  inline Maybe& operator=(Maybe&& other) { ptr = other.ptr; other.ptr = nullptr; return *this; }
  template <typename U>
  inline Maybe& operator=(Maybe<U&>& other) { ptr = other.ptr; return *this; }
  template <typename U>
  inline Maybe& operator=(const Maybe<const U&>& other) { ptr = other.ptr; return *this; }
  template <typename U>
  inline Maybe& operator=(Maybe<U&>&& other) { ptr = other.ptr; other.ptr = nullptr; return *this; }
  template <typename U>
  inline Maybe& operator=(const Maybe<U&>&& other) = delete;

  inline bool operator==(decltype(nullptr)) const { return ptr == nullptr; }
  inline bool operator!=(decltype(nullptr)) const { return ptr != nullptr; }

  T& orDefault(T& defaultValue) {
    if (ptr == nullptr) {
      return defaultValue;
    } else {
      return *ptr;
    }
  }
  const T& orDefault(const T& defaultValue) const {
    if (ptr == nullptr) {
      return defaultValue;
    } else {
      return *ptr;
    }
  }

  template <typename Func>
  auto map(Func&& f) -> Maybe<decltype(f(instance<T&>()))> {
    if (ptr == nullptr) {
      return nullptr;
    } else {
      return f(*ptr);
    }
  }

  template <typename Func>
  auto map(Func&& f) const -> Maybe<decltype(f(instance<const T&>()))> {
    if (ptr == nullptr) {
      return nullptr;
    } else {
      const T& ref = *ptr;
      return f(ref);
    }
  }

private:
  T* ptr;

  template <typename U>
  friend class Maybe;
  template <typename U>
  friend U* _::readMaybe(Maybe<U&>&& maybe);
  template <typename U>
  friend U* _::readMaybe(const Maybe<U&>& maybe);
};






template <typename T>
class Array;

template <typename T>
class ArrayPtr: public DisallowConstCopyIfNotConst<T> {



public:
  inline constexpr ArrayPtr(): ptr(nullptr), size_(0) {}
  inline constexpr ArrayPtr(decltype(nullptr)): ptr(nullptr), size_(0) {}
  inline constexpr ArrayPtr(T* ptr, size_t size): ptr(ptr), size_(size) {}
  inline constexpr ArrayPtr(T* begin, T* end): ptr(begin), size_(end - begin) {}
# 1504 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Winit-list-lifetime"

  inline constexpr ArrayPtr(::std::initializer_list<RemoveConstOrDisable<T>> init)
      : ptr(init.begin()), size_(init.size()) {}

#pragma GCC diagnostic pop


  template <size_t size>
  inline constexpr ArrayPtr(T (&native)[size]): ptr(native), size_(size) {
# 1531 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
    static_assert(!isSameType<T, const char>(),
        "Can't implicitly convert literal char array to ArrayPtr because we don't know if "
        "you meant to include the NUL terminator. We may change this in the future to "
        "automatically drop the NUL terminator. For now, try explicitly converting to StringPtr, "
        "which can in turn implicitly convert to ArrayPtr<const char>.");
    static_assert(!isSameType<T, const char16_t>(), "see above");
    static_assert(!isSameType<T, const char32_t>(), "see above");
  }

  inline operator ArrayPtr<const T>() const {
    return ArrayPtr<const T>(ptr, size_);
  }
  inline ArrayPtr<const T> asConst() const {
    return ArrayPtr<const T>(ptr, size_);
  }

  inline constexpr size_t size() const { return size_; }
  inline const T& operator[](size_t index) const {
    if (__builtin_expect(index < size_, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h", 1549, "index < size_", "\"Out-of-bounds ArrayPtr access.\"", "Out-of-bounds ArrayPtr access.");
    return ptr[index];
  }
  inline T& operator[](size_t index) {
    if (__builtin_expect(index < size_, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h", 1553, "index < size_", "\"Out-of-bounds ArrayPtr access.\"", "Out-of-bounds ArrayPtr access.");
    return ptr[index];
  }

  inline T* begin() { return ptr; }
  inline T* end() { return ptr + size_; }
  inline T& front() { return *ptr; }
  inline T& back() { return *(ptr + size_ - 1); }
  inline constexpr const T* begin() const { return ptr; }
  inline constexpr const T* end() const { return ptr + size_; }
  inline const T& front() const { return *ptr; }
  inline const T& back() const { return *(ptr + size_ - 1); }

  inline ArrayPtr<const T> slice(size_t start, size_t end) const {
    if (__builtin_expect(start <= end && end <= size_, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h", 1567, "start <= end && end <= size_", "\"Out-of-bounds ArrayPtr::slice().\"", "Out-of-bounds ArrayPtr::slice().");
    return ArrayPtr<const T>(ptr + start, end - start);
  }
  inline ArrayPtr slice(size_t start, size_t end) {
    if (__builtin_expect(start <= end && end <= size_, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h", 1571, "start <= end && end <= size_", "\"Out-of-bounds ArrayPtr::slice().\"", "Out-of-bounds ArrayPtr::slice().");
    return ArrayPtr(ptr + start, end - start);
  }

  inline ArrayPtr<PropagateConst<T, byte>> asBytes() const {


    return { reinterpret_cast<PropagateConst<T, byte>*>(ptr), size_ * sizeof(T) };
  }
  inline ArrayPtr<PropagateConst<T, char>> asChars() const {


    return { reinterpret_cast<PropagateConst<T, char>*>(ptr), size_ * sizeof(T) };
  }

  inline bool operator==(decltype(nullptr)) const { return size_ == 0; }
  inline bool operator!=(decltype(nullptr)) const { return size_ != 0; }

  inline bool operator==(const ArrayPtr& other) const {
    if (size_ != other.size_) return false;
    for (size_t i = 0; i < size_; i++) {
      if (ptr[i] != other[i]) return false;
    }
    return true;
  }
  inline bool operator!=(const ArrayPtr& other) const { return !(*this == other); }

  template <typename U>
  inline bool operator==(const ArrayPtr<U>& other) const {
    if (size_ != other.size()) return false;
    for (size_t i = 0; i < size_; i++) {
      if (ptr[i] != other[i]) return false;
    }
    return true;
  }
  template <typename U>
  inline bool operator!=(const ArrayPtr<U>& other) const { return !(*this == other); }

  template <typename... Attachments>
  Array<T> attach(Attachments&&... attachments) const __attribute__((warn_unused_result));





private:
  T* ptr;
  size_t size_;
};

template <typename T>
inline constexpr ArrayPtr<T> arrayPtr(T* ptr, size_t size) {

  return ArrayPtr<T>(ptr, size);
}

template <typename T>
inline constexpr ArrayPtr<T> arrayPtr(T* begin, T* end) {

  return ArrayPtr<T>(begin, end);
}




template <typename To, typename From>
To implicitCast(From&& from) {


  return kj::fwd<From>(from);
}

template <typename To, typename From>
Maybe<To&> dynamicDowncastIfAvailable(From& from) {
# 1653 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/common.h" 3
  if (false) {
    kj::implicitCast<From*>(kj::implicitCast<To*>(nullptr));
  }


  return nullptr;



}

template <typename To, typename From>
To& downcast(From& from) {





  if (false) {
    kj::implicitCast<From*>(kj::implicitCast<To*>(nullptr));
  }





  return static_cast<To&>(from);
}




namespace _ {

template <typename Func>
class Deferred {
public:
  inline Deferred(Func&& func): func(kj::fwd<Func>(func)), canceled(false) {}
  inline ~Deferred() noexcept(false) { if (!canceled) func(); }
  Deferred(const Deferred&) = delete; Deferred& operator=(const Deferred&) = delete;


  inline Deferred(Deferred&& other): func(kj::fwd<Func>(other.func)), canceled(false) {
    other.canceled = true;
  }
private:
  Func func;
  bool canceled;
};

}

template <typename Func>
_::Deferred<Func> defer(Func&& func) {







  return _::Deferred<Func>(kj::fwd<Func>(func));
}




}


# 25 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h" 2


# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h"

# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h" 3


namespace kj {

template <typename T>
inline constexpr bool _kj_internal_isPolymorphic(T*) {
# 42 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h" 3
  return __is_polymorphic(T);
}
# 60 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h" 3
namespace _ {

template <typename T> struct RefOrVoid_ { typedef T& Type; };
template <> struct RefOrVoid_<void> { typedef void Type; };
template <> struct RefOrVoid_<const void> { typedef void Type; };

template <typename T>
using RefOrVoid = typename RefOrVoid_<T>::Type;




template <typename T, bool isPolymorphic = _kj_internal_isPolymorphic((T*)nullptr)>
struct CastToVoid_;

template <typename T>
struct CastToVoid_<T, false> {
  static void* apply(T* ptr) {
    return static_cast<void*>(ptr);
  }
  static const void* applyConst(T* ptr) {
    const T* cptr = ptr;
    return static_cast<const void*>(cptr);
  }
};

template <typename T>
struct CastToVoid_<T, true> {
  static void* apply(T* ptr) {
    return dynamic_cast<void*>(ptr);
  }
  static const void* applyConst(T* ptr) {
    const T* cptr = ptr;
    return dynamic_cast<const void*>(cptr);
  }
};

template <typename T>
void* castToVoid(T* ptr) {
  return CastToVoid_<T>::apply(ptr);
}

template <typename T>
const void* castToConstVoid(T* ptr) {
  return CastToVoid_<T>::applyConst(ptr);
}

}




class Disposer {







protected:



  virtual void disposeImpl(void* pointer) const = 0;





public:

  template <typename T>
  void dispose(T* object) const;
# 142 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h" 3
private:
  template <typename T, bool polymorphic = _kj_internal_isPolymorphic((T*)nullptr)>
  struct Dispose_;
};

template <typename T>
class DestructorOnlyDisposer: public Disposer {


public:
  static const DestructorOnlyDisposer instance;

  void disposeImpl(void* pointer) const override {
    reinterpret_cast<T*>(pointer)->~T();
  }
};

template <typename T>
const DestructorOnlyDisposer<T> DestructorOnlyDisposer<T>::instance = DestructorOnlyDisposer<T>();

class NullDisposer: public Disposer {


public:
  static const NullDisposer instance;

  void disposeImpl(void* pointer) const override {}
};




template <typename T>
class Own {
# 190 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h" 3
public:
  Own(const Own&) = delete; Own& operator=(const Own&) = delete;
  inline Own(): disposer(nullptr), ptr(nullptr) {}
  inline Own(Own&& other) noexcept
      : disposer(other.disposer), ptr(other.ptr) { other.ptr = nullptr; }
  inline Own(Own<RemoveConstOrDisable<T>>&& other) noexcept
      : disposer(other.disposer), ptr(other.ptr) { other.ptr = nullptr; }
  template <typename U, typename = EnableIf<canConvert<U*, T*>()>>
  inline Own(Own<U>&& other) noexcept
      : disposer(other.disposer), ptr(cast(other.ptr)) {
    other.ptr = nullptr;
  }
  inline Own(T* ptr, const Disposer& disposer) noexcept: disposer(&disposer), ptr(ptr) {}

  ~Own() noexcept(false) { dispose(); }

  inline Own& operator=(Own&& other) {




    const Disposer* disposerCopy = disposer;
    T* ptrCopy = ptr;
    disposer = other.disposer;
    ptr = other.ptr;
    other.ptr = nullptr;
    if (ptrCopy != nullptr) {
      disposerCopy->dispose(const_cast<RemoveConst<T>*>(ptrCopy));
    }
    return *this;
  }

  inline Own& operator=(decltype(nullptr)) {
    dispose();
    return *this;
  }

  template <typename... Attachments>
  Own<T> attach(Attachments&&... attachments) __attribute__((warn_unused_result));







  template <typename U>
  Own<U> downcast() {




    Own<U> result;
    if (ptr != nullptr) {
      result.ptr = &kj::downcast<U>(*ptr);
      result.disposer = disposer;
      ptr = nullptr;
    }
    return result;
  }


  inline T* operator->() { if (__builtin_expect(ptr != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h", 252, "ptr != nullptr", "\"null Own<> dereference\"", "null Own<> dereference"); return ptr; }
  inline const T* operator->() const { if (__builtin_expect(ptr != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h", 253, "ptr != nullptr", "\"null Own<> dereference\"", "null Own<> dereference"); return ptr; }
  inline _::RefOrVoid<T> operator*() { if (__builtin_expect(ptr != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h", 254, "ptr != nullptr", "\"null Own<> dereference\"", "null Own<> dereference"); return *ptr; }
  inline _::RefOrVoid<const T> operator*() const { if (__builtin_expect(ptr != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h", 255, "ptr != nullptr", "\"null Own<> dereference\"", "null Own<> dereference"); return *ptr; }

  inline T* get() { return ptr; }
  inline const T* get() const { return ptr; }
  inline operator T*() { return ptr; }
  inline operator const T*() const { return ptr; }

private:
  const Disposer* disposer;
  T* ptr;

  inline explicit Own(decltype(nullptr)): disposer(nullptr), ptr(nullptr) {}

  inline bool operator==(decltype(nullptr)) { return ptr == nullptr; }
  inline bool operator!=(decltype(nullptr)) { return ptr != nullptr; }


  inline void dispose() {


    T* ptrCopy = ptr;
    if (ptrCopy != nullptr) {
      ptr = nullptr;
      disposer->dispose(const_cast<RemoveConst<T>*>(ptrCopy));
    }
  }

  template <typename U>
  static inline T* cast(U* ptr) {
    static_assert(_kj_internal_isPolymorphic((T*)nullptr),
        "Casting owned pointers requires that the target type is polymorphic.");
    return ptr;
  }

  template <typename U>
  friend class Own;
  friend class Maybe<Own<T>>;
};

template <>
template <typename U>
inline void* Own<void>::cast(U* ptr) {
  return _::castToVoid(ptr);
}

template <>
template <typename U>
inline const void* Own<const void>::cast(U* ptr) {
  return _::castToConstVoid(ptr);
}

namespace _ {

template <typename T>
class OwnOwn {
public:
  inline OwnOwn(Own<T>&& value) noexcept: value(kj::mv(value)) {}

  inline Own<T>& operator*() & { return value; }
  inline const Own<T>& operator*() const & { return value; }
  inline Own<T>&& operator*() && { return kj::mv(value); }
  inline const Own<T>&& operator*() const && { return kj::mv(value); }
  inline Own<T>* operator->() { return &value; }
  inline const Own<T>* operator->() const { return &value; }
  inline operator Own<T>*() { return value ? &value : nullptr; }
  inline operator const Own<T>*() const { return value ? &value : nullptr; }

private:
  Own<T> value;
};

template <typename T>
OwnOwn<T> readMaybe(Maybe<Own<T>>&& maybe) { return OwnOwn<T>(kj::mv(maybe.ptr)); }
template <typename T>
Own<T>* readMaybe(Maybe<Own<T>>& maybe) { return maybe.ptr ? &maybe.ptr : nullptr; }
template <typename T>
const Own<T>* readMaybe(const Maybe<Own<T>>& maybe) { return maybe.ptr ? &maybe.ptr : nullptr; }

}

template <typename T>
class Maybe<Own<T>> {
public:
  inline Maybe(): ptr(nullptr) {}
  inline Maybe(Own<T>&& t) noexcept: ptr(kj::mv(t)) {}
  inline Maybe(Maybe&& other) noexcept: ptr(kj::mv(other.ptr)) {}

  template <typename U>
  inline Maybe(Maybe<Own<U>>&& other): ptr(mv(other.ptr)) {}
  template <typename U>
  inline Maybe(Own<U>&& other): ptr(mv(other)) {}

  inline Maybe(decltype(nullptr)) noexcept: ptr(nullptr) {}

  inline Own<T>& emplace(Own<T> value) {


    ptr = kj::mv(value);
    return ptr;
  }

  inline operator Maybe<T&>() { return ptr.get(); }
  inline operator Maybe<const T&>() const { return ptr.get(); }

  inline Maybe& operator=(Maybe&& other) { ptr = kj::mv(other.ptr); return *this; }

  inline bool operator==(decltype(nullptr)) const { return ptr == nullptr; }
  inline bool operator!=(decltype(nullptr)) const { return ptr != nullptr; }

  Own<T>& orDefault(Own<T>& defaultValue) {
    if (ptr == nullptr) {
      return defaultValue;
    } else {
      return ptr;
    }
  }
  const Own<T>& orDefault(const Own<T>& defaultValue) const {
    if (ptr == nullptr) {
      return defaultValue;
    } else {
      return ptr;
    }
  }

  template <typename Func>
  auto map(Func&& f) & -> Maybe<decltype(f(instance<Own<T>&>()))> {
    if (ptr == nullptr) {
      return nullptr;
    } else {
      return f(ptr);
    }
  }

  template <typename Func>
  auto map(Func&& f) const & -> Maybe<decltype(f(instance<const Own<T>&>()))> {
    if (ptr == nullptr) {
      return nullptr;
    } else {
      return f(ptr);
    }
  }

  template <typename Func>
  auto map(Func&& f) && -> Maybe<decltype(f(instance<Own<T>&&>()))> {
    if (ptr == nullptr) {
      return nullptr;
    } else {
      return f(kj::mv(ptr));
    }
  }

  template <typename Func>
  auto map(Func&& f) const && -> Maybe<decltype(f(instance<const Own<T>&&>()))> {
    if (ptr == nullptr) {
      return nullptr;
    } else {
      return f(kj::mv(ptr));
    }
  }

private:
  Own<T> ptr;

  template <typename U>
  friend class Maybe;
  template <typename U>
  friend _::OwnOwn<U> _::readMaybe(Maybe<Own<U>>&& maybe);
  template <typename U>
  friend Own<U>* _::readMaybe(Maybe<Own<U>>& maybe);
  template <typename U>
  friend const Own<U>* _::readMaybe(const Maybe<Own<U>>& maybe);
};

namespace _ {

template <typename T>
class HeapDisposer final: public Disposer {
public:
  virtual void disposeImpl(void* pointer) const override { delete reinterpret_cast<T*>(pointer); }

  static const HeapDisposer instance;
};
# 446 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h" 3
template <typename T>
const HeapDisposer<T> HeapDisposer<T>::instance = HeapDisposer<T>();


}

template <typename T, typename... Params>
Own<T> heap(Params&&... params) {





  return Own<T>(new T(kj::fwd<Params>(params)...), _::HeapDisposer<T>::instance);
}

template <typename T>
Own<Decay<T>> heap(T&& orig) {





  typedef Decay<T> T2;
  return Own<T2>(new T2(kj::fwd<T>(orig)), _::HeapDisposer<T2>::instance);
}

template <typename T, typename... Attachments>
Own<Decay<T>> attachVal(T&& value, Attachments&&... attachments);





template <typename T, typename... Attachments>
Own<T> attachRef(T& value, Attachments&&... attachments);
# 491 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h" 3
template <typename T>
class SpaceFor {




public:
  inline SpaceFor() {}
  inline ~SpaceFor() {}

  template <typename... Params>
  Own<T> construct(Params&&... params) {
    ctor(value, kj::fwd<Params>(params)...);
    return Own<T>(&value, DestructorOnlyDisposer<T>::instance);
  }

private:
  union {
    T value;
  };
};




template <typename T>
struct Disposer::Dispose_<T, true> {
  static void dispose(T* object, const Disposer& disposer) {



    disposer.disposeImpl(dynamic_cast<void*>(object));
  }
};
template <typename T>
struct Disposer::Dispose_<T, false> {
  static void dispose(T* object, const Disposer& disposer) {
    disposer.disposeImpl(static_cast<void*>(object));
  }
};

template <typename T>
void Disposer::dispose(T* object) const {
  Dispose_<T>::dispose(object, *this);
}

namespace _ {

template <typename... T>
struct OwnedBundle;

template <>
struct OwnedBundle<> {};

template <typename First, typename... Rest>
struct OwnedBundle<First, Rest...>: public OwnedBundle<Rest...> {
  OwnedBundle(First&& first, Rest&&... rest)
      : OwnedBundle<Rest...>(kj::fwd<Rest>(rest)...), first(kj::fwd<First>(first)) {}




  Decay<First> first;
};

template <typename... T>
struct DisposableOwnedBundle final: public Disposer, public OwnedBundle<T...> {
  DisposableOwnedBundle(T&&... values): OwnedBundle<T...>(kj::fwd<T>(values)...) {}
  void disposeImpl(void* pointer) const override { delete this; }
};

}

template <typename T>
template <typename... Attachments>
Own<T> Own<T>::attach(Attachments&&... attachments) {
  T* ptrCopy = ptr;

  if (__builtin_expect(ptrCopy != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/memory.h", 569, "ptrCopy != nullptr", "\"cannot attach to null pointer\"", "cannot attach to null pointer");




  if (ptrCopy == nullptr) ptrCopy = reinterpret_cast<T*>(1);

  auto bundle = new _::DisposableOwnedBundle<Own<T>, Attachments...>(
      kj::mv(*this), kj::fwd<Attachments>(attachments)...);
  return Own<T>(ptrCopy, *bundle);
}

template <typename T, typename... Attachments>
Own<T> attachRef(T& value, Attachments&&... attachments) {
  auto bundle = new _::DisposableOwnedBundle<Attachments...>(kj::fwd<Attachments>(attachments)...);
  return Own<T>(&value, *bundle);
}

template <typename T, typename... Attachments>
Own<Decay<T>> attachVal(T&& value, Attachments&&... attachments) {
  auto bundle = new _::DisposableOwnedBundle<T, Attachments...>(
      kj::fwd<T>(value), kj::fwd<Attachments>(attachments)...);
  return Own<Decay<T>>(&bundle->first, *bundle);
}

}


# 25 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 2
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h"
       


# 1 "/usr/include/c++/v1/string.h" 1 3 4
# 54 "/usr/include/c++/v1/string.h" 3 4
# 1 "/usr/include/c++/v1/__config" 1 3 4
# 55 "/usr/include/c++/v1/string.h" 2 3 4


       
# 58 "/usr/include/c++/v1/string.h" 3


# 1 "/usr/include/string.h" 1 3 4
# 26 "/usr/include/string.h" 3 4
# 1 "/usr/include/bits/libc-header-start.h" 1 3 4
# 27 "/usr/include/string.h" 2 3 4

extern "C" {




# 1 "/usr/include/c++/v1/stddef.h" 1 3 4
# 14 "/usr/include/c++/v1/stddef.h" 3 4
       
# 15 "/usr/include/c++/v1/stddef.h" 3


# 1 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stddef.h" 1 3 4
# 18 "/usr/include/c++/v1/stddef.h" 2 3
# 34 "/usr/include/string.h" 2 3 4
# 43 "/usr/include/string.h" 3 4
extern void *memcpy (void *__restrict __dest, const void *__restrict __src,
       size_t __n) noexcept (true) __attribute__ ((__nonnull__ (1, 2)));


extern void *memmove (void *__dest, const void *__src, size_t __n)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));





extern void *memccpy (void *__restrict __dest, const void *__restrict __src,
        int __c, size_t __n)
    noexcept (true) __attribute__ ((__nonnull__ (1, 2))) __attribute__ ((__access__ (__write_only__, 1, 4)));




extern void *memset (void *__s, int __c, size_t __n) noexcept (true) __attribute__ ((__nonnull__ (1)));


extern int memcmp (const void *__s1, const void *__s2, size_t __n)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));



extern "C++"
{
extern void *memchr (void *__s, int __c, size_t __n)
      noexcept (true) __asm ("memchr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
extern const void *memchr (const void *__s, int __c, size_t __n)
      noexcept (true) __asm ("memchr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
# 89 "/usr/include/string.h" 3 4
}
# 99 "/usr/include/string.h" 3 4
extern "C++" void *rawmemchr (void *__s, int __c)
     noexcept (true) __asm ("rawmemchr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
extern "C++" const void *rawmemchr (const void *__s, int __c)
     noexcept (true) __asm ("rawmemchr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));







extern "C++" void *memrchr (void *__s, int __c, size_t __n)
      noexcept (true) __asm ("memrchr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)))
      __attribute__ ((__access__ (__read_only__, 1, 3)));
extern "C++" const void *memrchr (const void *__s, int __c, size_t __n)
      noexcept (true) __asm ("memrchr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)))
      __attribute__ ((__access__ (__read_only__, 1, 3)));
# 125 "/usr/include/string.h" 3 4
extern char *strcpy (char *__restrict __dest, const char *__restrict __src)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));

extern char *strncpy (char *__restrict __dest,
        const char *__restrict __src, size_t __n)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));


extern char *strcat (char *__restrict __dest, const char *__restrict __src)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));

extern char *strncat (char *__restrict __dest, const char *__restrict __src,
        size_t __n) noexcept (true) __attribute__ ((__nonnull__ (1, 2)));


extern int strcmp (const char *__s1, const char *__s2)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));

extern int strncmp (const char *__s1, const char *__s2, size_t __n)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));


extern int strcoll (const char *__s1, const char *__s2)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));

extern size_t strxfrm (char *__restrict __dest,
         const char *__restrict __src, size_t __n)
    noexcept (true) __attribute__ ((__nonnull__ (2))) __attribute__ ((__access__ (__write_only__, 1, 3)));



# 1 "/usr/include/bits/types/locale_t.h" 1 3 4
# 22 "/usr/include/bits/types/locale_t.h" 3 4
# 1 "/usr/include/bits/types/__locale_t.h" 1 3 4
# 28 "/usr/include/bits/types/__locale_t.h" 3 4
struct __locale_struct
{

  struct __locale_data *__locales[13];


  const unsigned short int *__ctype_b;
  const int *__ctype_tolower;
  const int *__ctype_toupper;


  const char *__names[13];
};

typedef struct __locale_struct *__locale_t;
# 23 "/usr/include/bits/types/locale_t.h" 2 3 4

typedef __locale_t locale_t;
# 157 "/usr/include/string.h" 2 3 4


extern int strcoll_l (const char *__s1, const char *__s2, locale_t __l)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2, 3)));


extern size_t strxfrm_l (char *__dest, const char *__src, size_t __n,
    locale_t __l) noexcept (true) __attribute__ ((__nonnull__ (2, 4)))
     __attribute__ ((__access__ (__write_only__, 1, 3)));





extern char *strdup (const char *__s)
     noexcept (true) __attribute__ ((__malloc__)) __attribute__ ((__nonnull__ (1)));






extern char *strndup (const char *__string, size_t __n)
     noexcept (true) __attribute__ ((__malloc__)) __attribute__ ((__nonnull__ (1)));
# 208 "/usr/include/string.h" 3 4
extern "C++"
{
extern char *strchr (char *__s, int __c)
     noexcept (true) __asm ("strchr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
extern const char *strchr (const char *__s, int __c)
     noexcept (true) __asm ("strchr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
# 228 "/usr/include/string.h" 3 4
}






extern "C++"
{
extern char *strrchr (char *__s, int __c)
     noexcept (true) __asm ("strrchr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
extern const char *strrchr (const char *__s, int __c)
     noexcept (true) __asm ("strrchr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
# 255 "/usr/include/string.h" 3 4
}
# 265 "/usr/include/string.h" 3 4
extern "C++" char *strchrnul (char *__s, int __c)
     noexcept (true) __asm ("strchrnul") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
extern "C++" const char *strchrnul (const char *__s, int __c)
     noexcept (true) __asm ("strchrnul") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
# 277 "/usr/include/string.h" 3 4
extern size_t strcspn (const char *__s, const char *__reject)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));


extern size_t strspn (const char *__s, const char *__accept)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));


extern "C++"
{
extern char *strpbrk (char *__s, const char *__accept)
     noexcept (true) __asm ("strpbrk") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));
extern const char *strpbrk (const char *__s, const char *__accept)
     noexcept (true) __asm ("strpbrk") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));
# 305 "/usr/include/string.h" 3 4
}






extern "C++"
{
extern char *strstr (char *__haystack, const char *__needle)
     noexcept (true) __asm ("strstr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));
extern const char *strstr (const char *__haystack, const char *__needle)
     noexcept (true) __asm ("strstr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));
# 332 "/usr/include/string.h" 3 4
}







extern char *strtok (char *__restrict __s, const char *__restrict __delim)
     noexcept (true) __attribute__ ((__nonnull__ (2)));



extern char *__strtok_r (char *__restrict __s,
    const char *__restrict __delim,
    char **__restrict __save_ptr)
     noexcept (true) __attribute__ ((__nonnull__ (2, 3)));

extern char *strtok_r (char *__restrict __s, const char *__restrict __delim,
         char **__restrict __save_ptr)
     noexcept (true) __attribute__ ((__nonnull__ (2, 3)));





extern "C++" char *strcasestr (char *__haystack, const char *__needle)
     noexcept (true) __asm ("strcasestr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));
extern "C++" const char *strcasestr (const char *__haystack,
         const char *__needle)
     noexcept (true) __asm ("strcasestr") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));
# 373 "/usr/include/string.h" 3 4
extern void *memmem (const void *__haystack, size_t __haystacklen,
       const void *__needle, size_t __needlelen)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 3)))
    __attribute__ ((__access__ (__read_only__, 1, 2)))
    __attribute__ ((__access__ (__read_only__, 3, 4)));



extern void *__mempcpy (void *__restrict __dest,
   const void *__restrict __src, size_t __n)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));
extern void *mempcpy (void *__restrict __dest,
        const void *__restrict __src, size_t __n)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));




extern size_t strlen (const char *__s)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));




extern size_t strnlen (const char *__string, size_t __maxlen)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));




extern char *strerror (int __errnum) noexcept (true);
# 428 "/usr/include/string.h" 3 4
extern char *strerror_r (int __errnum, char *__buf, size_t __buflen)
     noexcept (true) __attribute__ ((__nonnull__ (2))) __attribute__ ((__access__ (__write_only__, 2, 3)));




extern const char *strerrordesc_np (int __err) noexcept (true);

extern const char *strerrorname_np (int __err) noexcept (true);





extern char *strerror_l (int __errnum, locale_t __l) noexcept (true);



# 1 "/usr/include/strings.h" 1 3 4
# 23 "/usr/include/strings.h" 3 4
# 1 "/usr/include/c++/v1/stddef.h" 1 3 4
# 14 "/usr/include/c++/v1/stddef.h" 3 4
       
# 15 "/usr/include/c++/v1/stddef.h" 3


# 1 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stddef.h" 1 3 4
# 18 "/usr/include/c++/v1/stddef.h" 2 3
# 24 "/usr/include/strings.h" 2 3 4






extern "C" {



extern int bcmp (const void *__s1, const void *__s2, size_t __n)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));


extern void bcopy (const void *__src, void *__dest, size_t __n)
  noexcept (true) __attribute__ ((__nonnull__ (1, 2)));


extern void bzero (void *__s, size_t __n) noexcept (true) __attribute__ ((__nonnull__ (1)));



extern "C++"
{
extern char *index (char *__s, int __c)
     noexcept (true) __asm ("index") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
extern const char *index (const char *__s, int __c)
     noexcept (true) __asm ("index") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
# 66 "/usr/include/strings.h" 3 4
}







extern "C++"
{
extern char *rindex (char *__s, int __c)
     noexcept (true) __asm ("rindex") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
extern const char *rindex (const char *__s, int __c)
     noexcept (true) __asm ("rindex") __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1)));
# 94 "/usr/include/strings.h" 3 4
}
# 104 "/usr/include/strings.h" 3 4
extern int ffs (int __i) noexcept (true) __attribute__ ((__const__));





extern int ffsl (long int __l) noexcept (true) __attribute__ ((__const__));
__extension__ extern int ffsll (long long int __ll)
     noexcept (true) __attribute__ ((__const__));



extern int strcasecmp (const char *__s1, const char *__s2)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));


extern int strncasecmp (const char *__s1, const char *__s2, size_t __n)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));






extern int strcasecmp_l (const char *__s1, const char *__s2, locale_t __loc)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2, 3)));



extern int strncasecmp_l (const char *__s1, const char *__s2,
     size_t __n, locale_t __loc)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2, 4)));


}
# 447 "/usr/include/string.h" 2 3 4



extern void explicit_bzero (void *__s, size_t __n) noexcept (true) __attribute__ ((__nonnull__ (1)))
    __attribute__ ((__access__ (__write_only__, 1, 2)));



extern char *strsep (char **__restrict __stringp,
       const char *__restrict __delim)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));




extern char *strsignal (int __sig) noexcept (true);



extern const char *sigabbrev_np (int __sig) noexcept (true);


extern const char *sigdescr_np (int __sig) noexcept (true);



extern char *__stpcpy (char *__restrict __dest, const char *__restrict __src)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));
extern char *stpcpy (char *__restrict __dest, const char *__restrict __src)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));



extern char *__stpncpy (char *__restrict __dest,
   const char *__restrict __src, size_t __n)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));
extern char *stpncpy (char *__restrict __dest,
        const char *__restrict __src, size_t __n)
     noexcept (true) __attribute__ ((__nonnull__ (1, 2)));




extern int strverscmp (const char *__s1, const char *__s2)
     noexcept (true) __attribute__ ((__pure__)) __attribute__ ((__nonnull__ (1, 2)));


extern char *strfry (char *__string) noexcept (true) __attribute__ ((__nonnull__ (1)));


extern void *memfrob (void *__s, size_t __n) noexcept (true) __attribute__ ((__nonnull__ (1)))
    __attribute__ ((__access__ (__write_only__, 1, 2)));







extern "C++" char *basename (char *__filename)
     noexcept (true) __asm ("basename") __attribute__ ((__nonnull__ (1)));
extern "C++" const char *basename (const char *__filename)
     noexcept (true) __asm ("basename") __attribute__ ((__nonnull__ (1)));
# 523 "/usr/include/string.h" 3 4
}
# 61 "/usr/include/c++/v1/string.h" 2 3
# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h" 2



# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h"

# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h" 3


namespace kj {




class ArrayDisposer {


protected:



  virtual void disposeImpl(void* firstElement, size_t elementSize, size_t elementCount,
                           size_t capacity, void (*destroyElement)(void*)) const = 0;





public:

  template <typename T>
  void dispose(T* firstElement, size_t elementCount, size_t capacity) const;





private:
  template <typename T, bool hasTrivialDestructor = __has_trivial_destructor(T)>
  struct Dispose_;
};

class ExceptionSafeArrayUtil {







public:
  inline ExceptionSafeArrayUtil(void* ptr, size_t elementSize, size_t constructedElementCount,
                                void (*destroyElement)(void*))
      : pos(reinterpret_cast<byte*>(ptr) + elementSize * constructedElementCount),
        elementSize(elementSize), constructedElementCount(constructedElementCount),
        destroyElement(destroyElement) {}
  ExceptionSafeArrayUtil(const ExceptionSafeArrayUtil&) = delete; ExceptionSafeArrayUtil& operator=(const ExceptionSafeArrayUtil&) = delete;

  inline ~ExceptionSafeArrayUtil() noexcept(false) {
    if (constructedElementCount > 0) destroyAll();
  }

  void construct(size_t count, void (*constructElement)(void*));


  void destroyAll();




  void release() { constructedElementCount = 0; }



private:
  byte* pos;
  size_t elementSize;
  size_t constructedElementCount;
  void (*destroyElement)(void*);
};

class DestructorOnlyArrayDisposer: public ArrayDisposer {
public:
  static const DestructorOnlyArrayDisposer instance;

  void disposeImpl(void* firstElement, size_t elementSize, size_t elementCount,
                   size_t capacity, void (*destroyElement)(void*)) const override;
};

class NullArrayDisposer: public ArrayDisposer {



public:
  static const NullArrayDisposer instance;

  void disposeImpl(void* firstElement, size_t elementSize, size_t elementCount,
                   size_t capacity, void (*destroyElement)(void*)) const override;
};




template <typename T>
class Array {




public:
  inline Array(): ptr(nullptr), size_(0), disposer(nullptr) {}
  inline Array(decltype(nullptr)): ptr(nullptr), size_(0), disposer(nullptr) {}
  inline Array(Array&& other) noexcept
      : ptr(other.ptr), size_(other.size_), disposer(other.disposer) {
    other.ptr = nullptr;
    other.size_ = 0;
  }
  inline Array(Array<RemoveConstOrDisable<T>>&& other) noexcept
      : ptr(other.ptr), size_(other.size_), disposer(other.disposer) {
    other.ptr = nullptr;
    other.size_ = 0;
  }
  inline Array(T* firstElement, size_t size, const ArrayDisposer& disposer)
      : ptr(firstElement), size_(size), disposer(&disposer) {}

  Array(const Array&) = delete; Array& operator=(const Array&) = delete;
  inline ~Array() noexcept { dispose(); }

  inline operator ArrayPtr<T>() {
    return ArrayPtr<T>(ptr, size_);
  }
  inline operator ArrayPtr<const T>() const {
    return ArrayPtr<T>(ptr, size_);
  }
  inline ArrayPtr<T> asPtr() {
    return ArrayPtr<T>(ptr, size_);
  }
  inline ArrayPtr<const T> asPtr() const {
    return ArrayPtr<T>(ptr, size_);
  }

  inline size_t size() const { return size_; }
  inline T& operator[](size_t index) {
    if (__builtin_expect(index < size_, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 164, "index < size_", "\"Out-of-bounds Array access.\"", "Out-of-bounds Array access.");
    return ptr[index];
  }
  inline const T& operator[](size_t index) const {
    if (__builtin_expect(index < size_, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 168, "index < size_", "\"Out-of-bounds Array access.\"", "Out-of-bounds Array access.");
    return ptr[index];
  }

  inline const T* begin() const { return ptr; }
  inline const T* end() const { return ptr + size_; }
  inline const T& front() const { return *ptr; }
  inline const T& back() const { return *(ptr + size_ - 1); }
  inline T* begin() { return ptr; }
  inline T* end() { return ptr + size_; }
  inline T& front() { return *ptr; }
  inline T& back() { return *(ptr + size_ - 1); }

  template <typename U>
  inline bool operator==(const U& other) const { return asPtr() == other; }
  template <typename U>
  inline bool operator!=(const U& other) const { return asPtr() != other; }

  inline ArrayPtr<T> slice(size_t start, size_t end) {
    if (__builtin_expect(start <= end && end <= size_, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 187, "start <= end && end <= size_", "\"Out-of-bounds Array::slice().\"", "Out-of-bounds Array::slice().");
    return ArrayPtr<T>(ptr + start, end - start);
  }
  inline ArrayPtr<const T> slice(size_t start, size_t end) const {
    if (__builtin_expect(start <= end && end <= size_, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 191, "start <= end && end <= size_", "\"Out-of-bounds Array::slice().\"", "Out-of-bounds Array::slice().");
    return ArrayPtr<const T>(ptr + start, end - start);
  }

  inline ArrayPtr<const byte> asBytes() const { return asPtr().asBytes(); }
  inline ArrayPtr<PropagateConst<T, byte>> asBytes() { return asPtr().asBytes(); }
  inline ArrayPtr<const char> asChars() const { return asPtr().asChars(); }
  inline ArrayPtr<PropagateConst<T, char>> asChars() { return asPtr().asChars(); }

  inline Array<PropagateConst<T, byte>> releaseAsBytes() {

    static_assert(sizeof(T) == sizeof(byte),
        "releaseAsBytes() only possible on arrays with byte-size elements (e.g. chars).");
    Array<PropagateConst<T, byte>> result(
        reinterpret_cast<PropagateConst<T, byte>*>(ptr), size_, *disposer);
    ptr = nullptr;
    size_ = 0;
    return result;
  }
  inline Array<PropagateConst<T, char>> releaseAsChars() {

    static_assert(sizeof(T) == sizeof(PropagateConst<T, char>),
        "releaseAsChars() only possible on arrays with char-size elements (e.g. bytes).");
    Array<PropagateConst<T, char>> result(
        reinterpret_cast<PropagateConst<T, char>*>(ptr), size_, *disposer);
    ptr = nullptr;
    size_ = 0;
    return result;
  }

  inline bool operator==(decltype(nullptr)) const { return size_ == 0; }
  inline bool operator!=(decltype(nullptr)) const { return size_ != 0; }

  inline Array& operator=(decltype(nullptr)) {
    dispose();
    return *this;
  }

  inline Array& operator=(Array&& other) {
    dispose();
    ptr = other.ptr;
    size_ = other.size_;
    disposer = other.disposer;
    other.ptr = nullptr;
    other.size_ = 0;
    return *this;
  }

  template <typename... Attachments>
  Array<T> attach(Attachments&&... attachments) __attribute__((warn_unused_result));


private:
  T* ptr;
  size_t size_;
  const ArrayDisposer* disposer;

  inline void dispose() {


    T* ptrCopy = ptr;
    size_t sizeCopy = size_;
    if (ptrCopy != nullptr) {
      ptr = nullptr;
      size_ = 0;
      disposer->dispose(ptrCopy, sizeCopy, sizeCopy);
    }
  }

  template <typename U>
  friend class Array;
  template <typename U>
  friend class ArrayBuilder;
};

static_assert(!canMemcpy<Array<char>>(), "canMemcpy<>() is broken");

namespace _ {

class HeapArrayDisposer final: public ArrayDisposer {
public:
  template <typename T>
  static T* allocate(size_t count);
  template <typename T>
  static T* allocateUninitialized(size_t count);

  static const HeapArrayDisposer instance;

private:
  static void* allocateImpl(size_t elementSize, size_t elementCount, size_t capacity,
                            void (*constructElement)(void*), void (*destroyElement)(void*));



  virtual void disposeImpl(void* firstElement, size_t elementSize, size_t elementCount,
                           size_t capacity, void (*destroyElement)(void*)) const override;

  template <typename T, bool hasTrivialConstructor = __has_trivial_constructor(T),
                        bool hasNothrowConstructor = __has_nothrow_constructor(T)>
  struct Allocate_;
};

}

template <typename T>
inline Array<T> heapArray(size_t size) {


  return Array<T>(_::HeapArrayDisposer::allocate<T>(size), size,
                  _::HeapArrayDisposer::instance);
}

template <typename T> Array<T> heapArray(const T* content, size_t size);
template <typename T> Array<T> heapArray(ArrayPtr<T> content);
template <typename T> Array<T> heapArray(ArrayPtr<const T> content);
template <typename T, typename Iterator> Array<T> heapArray(Iterator begin, Iterator end);
template <typename T> Array<T> heapArray(std::initializer_list<T> init);


template <typename T, typename Container>
Array<T> heapArrayFromIterable(Container&& a) { return heapArray<T>(a.begin(), a.end()); }
template <typename T>
Array<T> heapArrayFromIterable(Array<T>&& a) { return mv(a); }




template <typename T>
class ArrayBuilder {



public:
  ArrayBuilder(): ptr(nullptr), pos(nullptr), endPtr(nullptr) {}
  ArrayBuilder(decltype(nullptr)): ptr(nullptr), pos(nullptr), endPtr(nullptr) {}
  explicit ArrayBuilder(RemoveConst<T>* firstElement, size_t capacity,
                        const ArrayDisposer& disposer)
      : ptr(firstElement), pos(firstElement), endPtr(firstElement + capacity),
        disposer(&disposer) {}
  ArrayBuilder(ArrayBuilder&& other)
      : ptr(other.ptr), pos(other.pos), endPtr(other.endPtr), disposer(other.disposer) {
    other.ptr = nullptr;
    other.pos = nullptr;
    other.endPtr = nullptr;
  }
  ArrayBuilder(Array<T>&& other)
      : ptr(other.ptr), pos(other.ptr + other.size_), endPtr(pos), disposer(other.disposer) {


    other.ptr = nullptr;
    other.size_ = 0;
  }
  ArrayBuilder(const ArrayBuilder&) = delete; ArrayBuilder& operator=(const ArrayBuilder&) = delete;
  inline ~ArrayBuilder() noexcept(false) { dispose(); }

  inline operator ArrayPtr<T>() {
    return arrayPtr(ptr, pos);
  }
  inline operator ArrayPtr<const T>() const {
    return arrayPtr(ptr, pos);
  }
  inline ArrayPtr<T> asPtr() {
    return arrayPtr(ptr, pos);
  }
  inline ArrayPtr<const T> asPtr() const {
    return arrayPtr(ptr, pos);
  }

  inline size_t size() const { return pos - ptr; }
  inline size_t capacity() const { return endPtr - ptr; }
  inline T& operator[](size_t index) {
    if (__builtin_expect(index < implicitCast<size_t>(pos - ptr), true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 362, "index < implicitCast<size_t>(pos - ptr)", "\"Out-of-bounds Array access.\"", "Out-of-bounds Array access.");
    return ptr[index];
  }
  inline const T& operator[](size_t index) const {
    if (__builtin_expect(index < implicitCast<size_t>(pos - ptr), true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 366, "index < implicitCast<size_t>(pos - ptr)", "\"Out-of-bounds Array access.\"", "Out-of-bounds Array access.");
    return ptr[index];
  }

  inline const T* begin() const { return ptr; }
  inline const T* end() const { return pos; }
  inline const T& front() const { return *ptr; }
  inline const T& back() const { return *(pos - 1); }
  inline T* begin() { return ptr; }
  inline T* end() { return pos; }
  inline T& front() { return *ptr; }
  inline T& back() { return *(pos - 1); }

  ArrayBuilder& operator=(ArrayBuilder&& other) {
    dispose();
    ptr = other.ptr;
    pos = other.pos;
    endPtr = other.endPtr;
    disposer = other.disposer;
    other.ptr = nullptr;
    other.pos = nullptr;
    other.endPtr = nullptr;
    return *this;
  }
  ArrayBuilder& operator=(decltype(nullptr)) {
    dispose();
    return *this;
  }

  template <typename... Params>
  T& add(Params&&... params) {
    if (__builtin_expect(pos < endPtr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 397, "pos < endPtr", "\"Added too many elements to ArrayBuilder.\"", "Added too many elements to ArrayBuilder.");
    ctor(*pos, kj::fwd<Params>(params)...);
    return *pos++;
  }

  template <typename Container>
  void addAll(Container&& container) {
    addAll<decltype(container.begin()), !isReference<Container>()>(
        container.begin(), container.end());
  }

  template <typename Iterator, bool move = false>
  void addAll(Iterator start, Iterator end);

  void removeLast() {
    if (__builtin_expect(pos > ptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 412, "pos > ptr", "\"No elements present to remove.\"", "No elements present to remove.");
    kj::dtor(*--pos);
  }

  void truncate(size_t size) {
    if (__builtin_expect(size <= this->size(), true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 417, "size <= this->size()", "\"can't use truncate() to expand\"", "can't use truncate() to expand");

    T* target = ptr + size;
    if (__has_trivial_destructor(T)) {
      pos = target;
    } else {
      while (pos > target) {
        kj::dtor(*--pos);
      }
    }
  }

  void clear() {
    if (__has_trivial_destructor(T)) {
      pos = ptr;
    } else {
      while (pos > ptr) {
        kj::dtor(*--pos);
      }
    }
  }

  void resize(size_t size) {
    if (__builtin_expect(size <= capacity(), true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 440, "size <= capacity()", "\"can't resize past capacity\"", "can't resize past capacity");

    T* target = ptr + size;
    if (target > pos) {

      if (__has_trivial_constructor(T)) {
        pos = target;
      } else {
        while (pos < target) {
          kj::ctor(*pos++);
        }
      }
    } else {

      if (__has_trivial_destructor(T)) {
        pos = target;
      } else {
        while (pos > target) {
          kj::dtor(*--pos);
        }
      }
    }
  }

  Array<T> finish() {






    if (__builtin_expect(pos == endPtr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 471, "pos == endPtr", "\"ArrayBuilder::finish() called prematurely.\"", "ArrayBuilder::finish() called prematurely.");
    Array<T> result(reinterpret_cast<T*>(ptr), pos - ptr, *disposer);
    ptr = nullptr;
    pos = nullptr;
    endPtr = nullptr;
    return result;
  }

  inline bool isFull() const {
    return pos == endPtr;
  }

private:
  T* ptr;
  RemoveConst<T>* pos;
  T* endPtr;
  const ArrayDisposer* disposer = &NullArrayDisposer::instance;

  inline void dispose() {


    T* ptrCopy = ptr;
    T* posCopy = pos;
    T* endCopy = endPtr;
    if (ptrCopy != nullptr) {
      ptr = nullptr;
      pos = nullptr;
      endPtr = nullptr;
      disposer->dispose(ptrCopy, posCopy - ptrCopy, endCopy - ptrCopy);
    }
  }
};

template <typename T>
inline ArrayBuilder<T> heapArrayBuilder(size_t size) {



  return ArrayBuilder<T>(_::HeapArrayDisposer::allocateUninitialized<RemoveConst<T>>(size),
                         size, _::HeapArrayDisposer::instance);
}




template <typename T, size_t fixedSize>
class FixedArray {


public:
  inline constexpr size_t size() const { return fixedSize; }
  inline constexpr T* begin() { return content; }
  inline constexpr T* end() { return content + fixedSize; }
  inline constexpr const T* begin() const { return content; }
  inline constexpr const T* end() const { return content + fixedSize; }

  inline constexpr operator ArrayPtr<T>() {
    return arrayPtr(content, fixedSize);
  }
  inline constexpr operator ArrayPtr<const T>() const {
    return arrayPtr(content, fixedSize);
  }

  inline constexpr T& operator[](size_t index) { return content[index]; }
  inline constexpr const T& operator[](size_t index) const { return content[index]; }

private:
  T content[fixedSize];
};

template <typename T, size_t fixedSize>
class CappedArray {





public:
  inline constexpr CappedArray(): currentSize(fixedSize) {}
  inline explicit constexpr CappedArray(size_t s): currentSize(s) {}

  inline size_t size() const { return currentSize; }
  inline void setSize(size_t s) { if (__builtin_expect(s <= fixedSize, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 553, "s <= fixedSize", ""); currentSize = s; }
  inline T* begin() { return content; }
  inline T* end() { return content + currentSize; }
  inline const T* begin() const { return content; }
  inline const T* end() const { return content + currentSize; }

  inline operator ArrayPtr<T>() {
    return arrayPtr(content, currentSize);
  }
  inline operator ArrayPtr<const T>() const {
    return arrayPtr(content, currentSize);
  }

  inline T& operator[](size_t index) { return content[index]; }
  inline const T& operator[](size_t index) const { return content[index]; }

private:
  size_t currentSize;
  T content[fixedSize];
};
# 587 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h" 3
namespace _ {

template <typename T>
struct Mapper {
  T array;
  Mapper(T&& array): array(kj::fwd<T>(array)) {}
  template <typename Func>
  auto operator*(Func&& func) -> Array<decltype(func(*array.begin()))> {
    auto builder = heapArrayBuilder<decltype(func(*array.begin()))>(array.size());
    for (auto iter = array.begin(); iter != array.end(); ++iter) {
      builder.add(func(*iter));
    }
    return builder.finish();
  }
  typedef decltype(*kj::instance<T>().begin()) Element;
};

template <typename T, size_t s>
struct Mapper<T(&)[s]> {
  T* array;
  Mapper(T* array): array(array) {}
  template <typename Func>
  auto operator*(Func&& func) -> Array<decltype(func(*array))> {
    auto builder = heapArrayBuilder<decltype(func(*array))>(s);
    for (size_t i = 0; i < s; i++) {
      builder.add(func(array[i]));
    }
    return builder.finish();
  }
  typedef decltype(*array)& Element;
};

}




template <typename T>
struct ArrayDisposer::Dispose_<T, true> {
  static void dispose(T* firstElement, size_t elementCount, size_t capacity,
                      const ArrayDisposer& disposer) {
    disposer.disposeImpl(const_cast<RemoveConst<T>*>(firstElement),
                         sizeof(T), elementCount, capacity, nullptr);
  }
};
template <typename T>
struct ArrayDisposer::Dispose_<T, false> {
  static void destruct(void* ptr) {
    kj::dtor(*reinterpret_cast<T*>(ptr));
  }

  static void dispose(T* firstElement, size_t elementCount, size_t capacity,
                      const ArrayDisposer& disposer) {
    disposer.disposeImpl(const_cast<RemoveConst<T>*>(firstElement),
                         sizeof(T), elementCount, capacity, &destruct);
  }
};

template <typename T>
void ArrayDisposer::dispose(T* firstElement, size_t elementCount, size_t capacity) const {
  Dispose_<T>::dispose(firstElement, elementCount, capacity, *this);
}

namespace _ {

template <typename T>
struct HeapArrayDisposer::Allocate_<T, true, true> {
  static T* allocate(size_t elementCount, size_t capacity) {
    return reinterpret_cast<T*>(allocateImpl(
        sizeof(T), elementCount, capacity, nullptr, nullptr));
  }
};
template <typename T>
struct HeapArrayDisposer::Allocate_<T, false, true> {
  static void construct(void* ptr) {
    kj::ctor(*reinterpret_cast<T*>(ptr));
  }
  static T* allocate(size_t elementCount, size_t capacity) {
    return reinterpret_cast<T*>(allocateImpl(
        sizeof(T), elementCount, capacity, &construct, nullptr));
  }
};
template <typename T>
struct HeapArrayDisposer::Allocate_<T, false, false> {
  static void construct(void* ptr) {
    kj::ctor(*reinterpret_cast<T*>(ptr));
  }
  static void destruct(void* ptr) {
    kj::dtor(*reinterpret_cast<T*>(ptr));
  }
  static T* allocate(size_t elementCount, size_t capacity) {
    return reinterpret_cast<T*>(allocateImpl(
        sizeof(T), elementCount, capacity, &construct, &destruct));
  }
};

template <typename T>
T* HeapArrayDisposer::allocate(size_t count) {
  return Allocate_<T>::allocate(count, count);
}

template <typename T>
T* HeapArrayDisposer::allocateUninitialized(size_t count) {
  return Allocate_<T, true, true>::allocate(0, count);
}

template <typename Element, typename Iterator, bool move, bool = canMemcpy<Element>()>
struct CopyConstructArray_;

template <typename T, bool move>
struct CopyConstructArray_<T, T*, move, true> {
  static inline T* apply(T* __restrict__ pos, T* start, T* end) {
    if (end != start) {
      memcpy(pos, start, reinterpret_cast<byte*>(end) - reinterpret_cast<byte*>(start));
    }
    return pos + (end - start);
  }
};

template <typename T>
struct CopyConstructArray_<T, const T*, false, true> {
  static inline T* apply(T* __restrict__ pos, const T* start, const T* end) {
    if (end != start) {
      memcpy(pos, start, reinterpret_cast<const byte*>(end) - reinterpret_cast<const byte*>(start));
    }
    return pos + (end - start);
  }
};

template <typename T, typename Iterator, bool move>
struct CopyConstructArray_<T, Iterator, move, true> {
  static inline T* apply(T* __restrict__ pos, Iterator start, Iterator end) {



    while (start != end) {
      *pos++ = *start++;
    }
    return pos;
  }
};

template <typename T, typename Iterator>
struct CopyConstructArray_<T, Iterator, false, false> {
  struct ExceptionGuard {
    T* start;
    T* pos;
    inline explicit ExceptionGuard(T* pos): start(pos), pos(pos) {}
    ~ExceptionGuard() noexcept(false) {
      while (pos > start) {
        dtor(*--pos);
      }
    }
  };

  static T* apply(T* __restrict__ pos, Iterator start, Iterator end) {

    if (false) implicitCast<T>(*start);

    if (noexcept(T(*start))) {
      while (start != end) {
        ctor(*pos++, *start++);
      }
      return pos;
    } else {

      ExceptionGuard guard(pos);
      while (start != end) {
        ctor(*guard.pos, *start++);
        ++guard.pos;
      }
      guard.start = guard.pos;
      return guard.pos;
    }
  }
};

template <typename T, typename Iterator>
struct CopyConstructArray_<T, Iterator, true, false> {


  struct ExceptionGuard {
    T* start;
    T* pos;
    inline explicit ExceptionGuard(T* pos): start(pos), pos(pos) {}
    ~ExceptionGuard() noexcept(false) {
      while (pos > start) {
        dtor(*--pos);
      }
    }
  };

  static T* apply(T* __restrict__ pos, Iterator start, Iterator end) {

    if (false) implicitCast<T>(kj::mv(*start));

    if (noexcept(T(kj::mv(*start)))) {
      while (start != end) {
        ctor(*pos++, kj::mv(*start++));
      }
      return pos;
    } else {

      ExceptionGuard guard(pos);
      while (start != end) {
        ctor(*guard.pos, kj::mv(*start++));
        ++guard.pos;
      }
      guard.start = guard.pos;
      return guard.pos;
    }
  }
};

}

template <typename T>
template <typename Iterator, bool move>
void ArrayBuilder<T>::addAll(Iterator start, Iterator end) {
  pos = _::CopyConstructArray_<RemoveConst<T>, Decay<Iterator>, move>::apply(pos, start, end);
}

template <typename T>
Array<T> heapArray(const T* content, size_t size) {
  ArrayBuilder<T> builder = heapArrayBuilder<T>(size);
  builder.addAll(content, content + size);
  return builder.finish();
}

template <typename T>
Array<T> heapArray(T* content, size_t size) {
  ArrayBuilder<T> builder = heapArrayBuilder<T>(size);
  builder.addAll(content, content + size);
  return builder.finish();
}

template <typename T>
Array<T> heapArray(ArrayPtr<T> content) {
  ArrayBuilder<T> builder = heapArrayBuilder<T>(content.size());
  builder.addAll(content);
  return builder.finish();
}

template <typename T>
Array<T> heapArray(ArrayPtr<const T> content) {
  ArrayBuilder<T> builder = heapArrayBuilder<T>(content.size());
  builder.addAll(content);
  return builder.finish();
}

template <typename T, typename Iterator> Array<T>
heapArray(Iterator begin, Iterator end) {
  ArrayBuilder<T> builder = heapArrayBuilder<T>(end - begin);
  builder.addAll(begin, end);
  return builder.finish();
}

template <typename T>
inline Array<T> heapArray(std::initializer_list<T> init) {
  return heapArray<T>(init.begin(), init.end());
}


template <typename T, typename... Params>
inline Array<Decay<T>> arr(T&& param1, Params&&... params) {
  ArrayBuilder<Decay<T>> builder = heapArrayBuilder<Decay<T>>(sizeof...(params) + 1);
  (builder.add(kj::fwd<T>(param1)), ... , builder.add(kj::fwd<Params>(params)));
  return builder.finish();
}
template <typename T, typename... Params>
inline Array<Decay<T>> arrOf(Params&&... params) {
  ArrayBuilder<Decay<T>> builder = heapArrayBuilder<Decay<T>>(sizeof...(params));
  (... , builder.add(kj::fwd<Params>(params)));
  return builder.finish();
}


namespace _ {

template <typename... T>
struct ArrayDisposableOwnedBundle final: public ArrayDisposer, public OwnedBundle<T...> {
  ArrayDisposableOwnedBundle(T&&... values): OwnedBundle<T...>(kj::fwd<T>(values)...) {}
  void disposeImpl(void*, size_t, size_t, size_t, void (*)(void*)) const override { delete this; }
};

}

template <typename T>
template <typename... Attachments>
Array<T> Array<T>::attach(Attachments&&... attachments) {
  T* ptrCopy = ptr;
  auto sizeCopy = size_;

  if (__builtin_expect(ptrCopy != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 880, "ptrCopy != nullptr", "\"cannot attach to null pointer\"", "cannot attach to null pointer");




  if (ptrCopy == nullptr) ptrCopy = reinterpret_cast<T*>(1);

  auto bundle = new _::ArrayDisposableOwnedBundle<Array<T>, Attachments...>(
      kj::mv(*this), kj::fwd<Attachments>(attachments)...);
  return Array<T>(ptrCopy, sizeCopy, *bundle);
}

template <typename T>
template <typename... Attachments>
Array<T> ArrayPtr<T>::attach(Attachments&&... attachments) const {
  T* ptrCopy = ptr;

  if (__builtin_expect(ptrCopy != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/array.h", 897, "ptrCopy != nullptr", "\"cannot attach to null pointer\"", "cannot attach to null pointer");




  if (ptrCopy == nullptr) ptrCopy = reinterpret_cast<T*>(1);

  auto bundle = new _::ArrayDisposableOwnedBundle<Attachments...>(
      kj::fwd<Attachments>(attachments)...);
  return Array<T>(ptrCopy, size_, *bundle);
}

}


# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 2
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h"
       






# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h"

# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h" 3


namespace kj {
  class StringPtr;
  class String;

  class StringTree;
}

constexpr kj::StringPtr operator "" _kj(const char* str, size_t n);
# 53 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h" 3
namespace kj {
# 68 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h" 3
class StringPtr {
public:
  inline StringPtr(): content("", 1) {}
  inline StringPtr(decltype(nullptr)): content("", 1) {}
  inline StringPtr(const char* value): content(value, strlen(value) + 1) {}
  inline StringPtr(const char* value, size_t size): content(value, size + 1) {
    if (__builtin_expect(value[size] == '\0', true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h", 74, "value[size] == '\\0'", "\"StringPtr must be NUL-terminated.\"", "StringPtr must be NUL-terminated.");
  }
  inline StringPtr(const char* begin, const char* end): StringPtr(begin, end - begin) {}
  inline StringPtr(const String& value);


  inline StringPtr(const char8_t* value): StringPtr(reinterpret_cast<const char*>(value)) {}
  inline StringPtr(const char8_t* value, size_t size)
      : StringPtr(reinterpret_cast<const char*>(value), size) {}
  inline StringPtr(const char8_t* begin, const char8_t* end)
      : StringPtr(reinterpret_cast<const char*>(begin), reinterpret_cast<const char*>(end)) {}




  template <typename T, typename = decltype(instance<T>().c_str())>
  inline StringPtr(const T& t): StringPtr(t.c_str()) {}




  template <typename T, typename = decltype(instance<T>().c_str())>
  inline operator T() const { return cStr(); }





  inline constexpr operator ArrayPtr<const char>() const;
  inline constexpr ArrayPtr<const char> asArray() const;
  inline ArrayPtr<const byte> asBytes() const { return asArray().asBytes(); }


  inline const char* cStr() const { return content.begin(); }


  inline size_t size() const { return content.size() - 1; }


  inline char operator[](size_t index) const { return content[index]; }

  inline const char* begin() const { return content.begin(); }
  inline const char* end() const { return content.end() - 1; }

  inline bool operator==(decltype(nullptr)) const { return content.size() <= 1; }
  inline bool operator!=(decltype(nullptr)) const { return content.size() > 1; }

  inline bool operator==(const StringPtr& other) const;
  inline bool operator!=(const StringPtr& other) const { return !(*this == other); }
  inline bool operator< (const StringPtr& other) const;
  inline bool operator> (const StringPtr& other) const { return other < *this; }
  inline bool operator<=(const StringPtr& other) const { return !(other < *this); }
  inline bool operator>=(const StringPtr& other) const { return !(*this < other); }

  inline StringPtr slice(size_t start) const;
  inline ArrayPtr<const char> slice(size_t start, size_t end) const;



  inline bool startsWith(const StringPtr& other) const;
  inline bool endsWith(const StringPtr& other) const;

  inline Maybe<size_t> findFirst(char c) const;
  inline Maybe<size_t> findLast(char c) const;

  template <typename T>
  T parseAs() const;






private:
  inline explicit constexpr StringPtr(ArrayPtr<const char> content): content(content) {}

  ArrayPtr<const char> content;

  friend constexpr kj::StringPtr (::operator "" _kj)(const char* str, size_t n);
};






template <> char StringPtr::parseAs<char>() const;
template <> signed char StringPtr::parseAs<signed char>() const;
template <> unsigned char StringPtr::parseAs<unsigned char>() const;
template <> short StringPtr::parseAs<short>() const;
template <> unsigned short StringPtr::parseAs<unsigned short>() const;
template <> int StringPtr::parseAs<int>() const;
template <> unsigned StringPtr::parseAs<unsigned>() const;
template <> long StringPtr::parseAs<long>() const;
template <> unsigned long StringPtr::parseAs<unsigned long>() const;
template <> long long StringPtr::parseAs<long long>() const;
template <> unsigned long long StringPtr::parseAs<unsigned long long>() const;
template <> float StringPtr::parseAs<float>() const;
template <> double StringPtr::parseAs<double>() const;
# 184 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h" 3
class String {
public:
  String() = default;
  inline String(decltype(nullptr)): content(nullptr) {}
  inline String(char* value, size_t size, const ArrayDisposer& disposer);

  inline explicit String(Array<char> buffer);


  inline operator ArrayPtr<char>();
  inline operator ArrayPtr<const char>() const;
  inline ArrayPtr<char> asArray();
  inline ArrayPtr<const char> asArray() const;
  inline ArrayPtr<byte> asBytes() { return asArray().asBytes(); }
  inline ArrayPtr<const byte> asBytes() const { return asArray().asBytes(); }


  inline Array<char> releaseArray() { return kj::mv(content); }



  inline const char* cStr() const;

  inline size_t size() const;


  inline char operator[](size_t index) const;
  inline char& operator[](size_t index);

  inline char* begin();
  inline char* end();
  inline const char* begin() const;
  inline const char* end() const;

  inline bool operator==(decltype(nullptr)) const { return content.size() <= 1; }
  inline bool operator!=(decltype(nullptr)) const { return content.size() > 1; }

  inline bool operator==(const StringPtr& other) const { return StringPtr(*this) == other; }
  inline bool operator!=(const StringPtr& other) const { return StringPtr(*this) != other; }
  inline bool operator< (const StringPtr& other) const { return StringPtr(*this) < other; }
  inline bool operator> (const StringPtr& other) const { return StringPtr(*this) > other; }
  inline bool operator<=(const StringPtr& other) const { return StringPtr(*this) <= other; }
  inline bool operator>=(const StringPtr& other) const { return StringPtr(*this) >= other; }

  inline bool operator==(const String& other) const { return StringPtr(*this) == StringPtr(other); }
  inline bool operator!=(const String& other) const { return StringPtr(*this) != StringPtr(other); }
  inline bool operator< (const String& other) const { return StringPtr(*this) < StringPtr(other); }
  inline bool operator> (const String& other) const { return StringPtr(*this) > StringPtr(other); }
  inline bool operator<=(const String& other) const { return StringPtr(*this) <= StringPtr(other); }
  inline bool operator>=(const String& other) const { return StringPtr(*this) >= StringPtr(other); }




  inline bool startsWith(const StringPtr& other) const { return StringPtr(*this).startsWith(other);}
  inline bool endsWith(const StringPtr& other) const { return StringPtr(*this).endsWith(other); }

  inline StringPtr slice(size_t start) const { return StringPtr(*this).slice(start); }
  inline ArrayPtr<const char> slice(size_t start, size_t end) const {
    return StringPtr(*this).slice(start, end);
  }

  inline Maybe<size_t> findFirst(char c) const { return StringPtr(*this).findFirst(c); }
  inline Maybe<size_t> findLast(char c) const { return StringPtr(*this).findLast(c); }

  template <typename T>
  T parseAs() const { return StringPtr(*this).parseAs<T>(); }


private:
  Array<char> content;
};






String heapString(size_t size);



String heapString(const char* value);
String heapString(const char* value, size_t size);
String heapString(StringPtr value);
String heapString(const String& value);
String heapString(ArrayPtr<const char> value);






namespace _ {

inline size_t sum(std::initializer_list<size_t> nums) {
  size_t result = 0;
  for (auto num: nums) {
    result += num;
  }
  return result;
}

inline char* fill(char* ptr) { return ptr; }
inline char* fillLimited(char* ptr, char* limit) { return ptr; }

template <typename... Rest>
char* fill(char* __restrict__ target, const StringTree& first, Rest&&... rest);
template <typename... Rest>
char* fillLimited(char* __restrict__ target, char* limit, const StringTree& first, Rest&&... rest);




template <typename First, typename... Rest>
char* fill(char* __restrict__ target, const First& first, Rest&&... rest) {
  auto i = first.begin();
  auto end = first.end();
  while (i != end) {
    *target++ = *i++;
  }
  return fill(target, kj::fwd<Rest>(rest)...);
}

template <typename... Params>
String concat(Params&&... params) {



  String result = heapString(sum({params.size()...}));
  fill(result.begin(), kj::fwd<Params>(params)...);
  return result;
}

inline String concat(String&& arr) {
  return kj::mv(arr);
}

template <typename First, typename... Rest>
char* fillLimited(char* __restrict__ target, char* limit, const First& first, Rest&&... rest) {
  auto i = first.begin();
  auto end = first.end();
  while (i != end) {
    if (target == limit) return target;
    *target++ = *i++;
  }
  return fillLimited(target, limit, kj::fwd<Rest>(rest)...);
}

template <typename T>
class Delimited;


template <typename T, typename... Rest>
char* fill(char* __restrict__ target, Delimited<T>&& first, Rest&&... rest);
template <typename T, typename... Rest>
char* fillLimited(char* __restrict__ target, char* limit, Delimited<T>&& first,Rest&&... rest);
template <typename T, typename... Rest>
char* fill(char* __restrict__ target, Delimited<T>& first, Rest&&... rest);
template <typename T, typename... Rest>
char* fillLimited(char* __restrict__ target, char* limit, Delimited<T>& first,Rest&&... rest);


struct Stringifier {
# 360 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h" 3
  inline ArrayPtr<const char> operator*(ArrayPtr<const char> s) const { return s; }
  inline ArrayPtr<const char> operator*(ArrayPtr<char> s) const { return s; }
  inline ArrayPtr<const char> operator*(const Array<const char>& s) const { return s; }
  inline ArrayPtr<const char> operator*(const Array<char>& s) const { return s; }
  template<size_t n>
  inline ArrayPtr<const char> operator*(const CappedArray<char, n>& s) const { return s; }
  template<size_t n>
  inline ArrayPtr<const char> operator*(const FixedArray<char, n>& s) const { return s; }
  inline ArrayPtr<const char> operator*(const char* s) const { return arrayPtr(s, strlen(s)); }

  inline ArrayPtr<const char> operator*(const char8_t* s) const {
    return operator*(reinterpret_cast<const char*>(s));
  }

  inline ArrayPtr<const char> operator*(const String& s) const { return s.asArray(); }
  inline ArrayPtr<const char> operator*(const StringPtr& s) const { return s.asArray(); }

  inline Range<char> operator*(const Range<char>& r) const { return r; }
  inline Repeat<char> operator*(const Repeat<char>& r) const { return r; }

  inline FixedArray<char, 1> operator*(char c) const {
    FixedArray<char, 1> result;
    result[0] = c;
    return result;
  }

  StringPtr operator*(decltype(nullptr)) const;
  StringPtr operator*(bool b) const;

  CappedArray<char, 5> operator*(signed char i) const;
  CappedArray<char, 5> operator*(unsigned char i) const;
  CappedArray<char, sizeof(short) * 3 + 2> operator*(short i) const;
  CappedArray<char, sizeof(unsigned short) * 3 + 2> operator*(unsigned short i) const;
  CappedArray<char, sizeof(int) * 3 + 2> operator*(int i) const;
  CappedArray<char, sizeof(unsigned int) * 3 + 2> operator*(unsigned int i) const;
  CappedArray<char, sizeof(long) * 3 + 2> operator*(long i) const;
  CappedArray<char, sizeof(unsigned long) * 3 + 2> operator*(unsigned long i) const;
  CappedArray<char, sizeof(long long) * 3 + 2> operator*(long long i) const;
  CappedArray<char, sizeof(unsigned long long) * 3 + 2> operator*(unsigned long long i) const;
  CappedArray<char, 24> operator*(float f) const;
  CappedArray<char, 32> operator*(double f) const;
  CappedArray<char, sizeof(const void*) * 2 + 1> operator*(const void* s) const;


  template <typename T, typename Result = decltype(instance<T>().toString())>
  inline Result operator*(T&& value) const { return kj::fwd<T>(value).toString(); }

};
static constexpr Stringifier STR = Stringifier();

}

template <typename T>
auto toCharSequence(T&& value) -> decltype(_::STR * kj::fwd<T>(value)) {
# 422 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h" 3
  return _::STR * kj::fwd<T>(value);
}

CappedArray<char, sizeof(unsigned char) * 2 + 1> hex(unsigned char i);
CappedArray<char, sizeof(unsigned short) * 2 + 1> hex(unsigned short i);
CappedArray<char, sizeof(unsigned int) * 2 + 1> hex(unsigned int i);
CappedArray<char, sizeof(unsigned long) * 2 + 1> hex(unsigned long i);
CappedArray<char, sizeof(unsigned long long) * 2 + 1> hex(unsigned long long i);

template <typename... Params>
String str(Params&&... params) {






  return _::concat(toCharSequence(kj::fwd<Params>(params))...);
}

inline String str(String&& s) { return mv(s); }


template <typename T>
_::Delimited<T> delimited(T&& arr, kj::StringPtr delim);


template <typename T>
String strArray(T&& arr, const char* delim) {
  size_t delimLen = strlen(delim);
  size_t pieces_size = (kj::size(arr)); bool pieces_isOnStack = pieces_size <= (32); decltype(_::STR * arr[0]) pieces_stack[kj::max(1, pieces_isOnStack ? pieces_size : 0)]; ::kj::Array<decltype(_::STR * arr[0])> pieces_heap = pieces_isOnStack ? nullptr : kj::heapArray<decltype(_::STR * arr[0])>(pieces_size); ::kj::ArrayPtr<decltype(_::STR * arr[0])> pieces = pieces_isOnStack ? kj::arrayPtr(pieces_stack, pieces_size) : pieces_heap;
  size_t size = 0;
  for (size_t i = 0; i < kj::size(arr); i++) {
    if (i > 0) size += delimLen;
    pieces[i] = _::STR * arr[i];
    size += pieces[i].size();
  }

  String result = heapString(size);
  char* pos = result.begin();
  for (size_t i = 0; i < kj::size(arr); i++) {
    if (i > 0) {
      memcpy(pos, delim, delimLen);
      pos += delimLen;
    }
    pos = _::fill(pos, pieces[i]);
  }
  return result;
}

template <typename... Params>
StringPtr strPreallocated(ArrayPtr<char> buffer, Params&&... params) {
# 490 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h" 3
  char* end = _::fillLimited(buffer.begin(), buffer.end() - 1,
      toCharSequence(kj::fwd<Params>(params))...);
  *end = '\0';
  return StringPtr(buffer.begin(), end);
}

template <typename T, typename = decltype(toCharSequence(kj::instance<T&>()))>
inline _::Delimited<ArrayPtr<T>> operator*(const _::Stringifier&, ArrayPtr<T> arr) {
  return _::Delimited<ArrayPtr<T>>(arr, ", ");
}

template <typename T, typename = decltype(toCharSequence(kj::instance<const T&>()))>
inline _::Delimited<ArrayPtr<const T>> operator*(const _::Stringifier&, const Array<T>& arr) {
  return _::Delimited<ArrayPtr<const T>>(arr, ", ");
}
# 522 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h" 3
inline StringPtr::StringPtr(const String& value): content(value.cStr(), value.size() + 1) {}

inline constexpr StringPtr::operator ArrayPtr<const char>() const {
  return ArrayPtr<const char>(content.begin(), content.size() - 1);
}

inline constexpr ArrayPtr<const char> StringPtr::asArray() const {
  return ArrayPtr<const char>(content.begin(), content.size() - 1);
}

inline bool StringPtr::operator==(const StringPtr& other) const {
  return content.size() == other.content.size() &&
      memcmp(content.begin(), other.content.begin(), content.size() - 1) == 0;
}

inline bool StringPtr::operator<(const StringPtr& other) const {
  bool shorter = content.size() < other.content.size();
  int cmp = memcmp(content.begin(), other.content.begin(),
                   shorter ? content.size() : other.content.size());
  return cmp < 0 || (cmp == 0 && shorter);
}

inline StringPtr StringPtr::slice(size_t start) const {
  return StringPtr(content.slice(start, content.size()));
}
inline ArrayPtr<const char> StringPtr::slice(size_t start, size_t end) const {
  return content.slice(start, end);
}

inline bool StringPtr::startsWith(const StringPtr& other) const {
  return other.content.size() <= content.size() &&
      memcmp(content.begin(), other.content.begin(), other.size()) == 0;
}
inline bool StringPtr::endsWith(const StringPtr& other) const {
  return other.content.size() <= content.size() &&
      memcmp(end() - other.size(), other.content.begin(), other.size()) == 0;
}

inline Maybe<size_t> StringPtr::findFirst(char c) const {
  const char* pos = reinterpret_cast<const char*>(memchr(content.begin(), c, size()));
  if (pos == nullptr) {
    return nullptr;
  } else {
    return pos - content.begin();
  }
}

inline Maybe<size_t> StringPtr::findLast(char c) const {
  for (size_t i = size(); i > 0; --i) {
    if (content[i-1] == c) {
      return i-1;
    }
  }
  return nullptr;
}

inline String::operator ArrayPtr<char>() {
  return content == nullptr ? ArrayPtr<char>(nullptr) : content.slice(0, content.size() - 1);
}
inline String::operator ArrayPtr<const char>() const {
  return content == nullptr ? ArrayPtr<const char>(nullptr) : content.slice(0, content.size() - 1);
}

inline ArrayPtr<char> String::asArray() {
  return content == nullptr ? ArrayPtr<char>(nullptr) : content.slice(0, content.size() - 1);
}
inline ArrayPtr<const char> String::asArray() const {
  return content == nullptr ? ArrayPtr<const char>(nullptr) : content.slice(0, content.size() - 1);
}

inline const char* String::cStr() const { return content == nullptr ? "" : content.begin(); }

inline size_t String::size() const { return content == nullptr ? 0 : content.size() - 1; }

inline char String::operator[](size_t index) const { return content[index]; }
inline char& String::operator[](size_t index) { return content[index]; }

inline char* String::begin() { return content == nullptr ? nullptr : content.begin(); }
inline char* String::end() { return content == nullptr ? nullptr : content.end() - 1; }
inline const char* String::begin() const { return content == nullptr ? nullptr : content.begin(); }
inline const char* String::end() const { return content == nullptr ? nullptr : content.end() - 1; }

inline String::String(char* value, size_t size, const ArrayDisposer& disposer)
    : content(value, size + 1, disposer) {
  if (__builtin_expect(value[size] == '\0', true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h", 606, "value[size] == '\\0'", "\"String must be NUL-terminated.\"", "String must be NUL-terminated.");
}

inline String::String(Array<char> buffer): content(kj::mv(buffer)) {
  if (__builtin_expect(content.size() > 0 && content.back() == '\0', true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/string.h", 610, "content.size() > 0 && content.back() == '\\0'", "\"String must be NUL-terminated.\"", "String must be NUL-terminated.");
}

inline String heapString(const char* value) {
  return heapString(value, strlen(value));
}
inline String heapString(StringPtr value) {
  return heapString(value.begin(), value.size());
}
inline String heapString(const String& value) {
  return heapString(value.begin(), value.size());
}
inline String heapString(ArrayPtr<const char> value) {
  return heapString(value.begin(), value.size());
}

namespace _ {

template <typename T>
class Delimited {
public:
  Delimited(T array, kj::StringPtr delimiter)
      : array(kj::fwd<T>(array)), delimiter(delimiter) {}




  size_t size() {
    ensureStringifiedInitialized();

    size_t result = 0;
    bool first = true;
    for (auto& e: stringified) {
      if (first) {
        first = false;
      } else {
        result += delimiter.size();
      }
      result += e.size();
    }
    return result;
  }

  char* flattenTo(char* __restrict__ target) {
    ensureStringifiedInitialized();

    bool first = true;
    for (auto& elem: stringified) {
      if (first) {
        first = false;
      } else {
        target = fill(target, delimiter);
      }
      target = fill(target, elem);
    }
    return target;
  }

  char* flattenTo(char* __restrict__ target, char* limit) {



    bool first = true;
    for (auto&& elem: array) {
      if (target == limit) return target;
      if (first) {
        first = false;
      } else {
        target = fillLimited(target, limit, delimiter);
      }
      target = fillLimited(target, limit, kj::toCharSequence(elem));
    }
    return target;
  }

private:
  typedef decltype(toCharSequence(*instance<T>().begin())) StringifiedItem;
  T array;
  kj::StringPtr delimiter;
  Array<StringifiedItem> stringified;

  void ensureStringifiedInitialized() {
    if (array.size() > 0 && stringified.size() == 0) {
      stringified = ::kj::_::Mapper<decltype(::kj::_::refIfLvalue(array))>(array) * [&](typename ::kj::_::Mapper<decltype(::kj::_::refIfLvalue(array))>::Element e) { return toCharSequence(e); };
    }
  }
};

template <typename T, typename... Rest>
char* fill(char* __restrict__ target, Delimited<T>&& first, Rest&&... rest) {
  target = first.flattenTo(target);
  return fill(target, kj::fwd<Rest>(rest)...);
}
template <typename T, typename... Rest>
char* fillLimited(char* __restrict__ target, char* limit, Delimited<T>&& first, Rest&&... rest) {
  target = first.flattenTo(target, limit);
  return fillLimited(target, limit, kj::fwd<Rest>(rest)...);
}
template <typename T, typename... Rest>
char* fill(char* __restrict__ target, Delimited<T>& first, Rest&&... rest) {
  target = first.flattenTo(target);
  return fill(target, kj::fwd<Rest>(rest)...);
}
template <typename T, typename... Rest>
char* fillLimited(char* __restrict__ target, char* limit, Delimited<T>& first, Rest&&... rest) {
  target = first.flattenTo(target, limit);
  return fillLimited(target, limit, kj::fwd<Rest>(rest)...);
}

template <typename T>
inline Delimited<T>&& operator*(::kj::_::Stringifier, Delimited<T>&& delimited) { return kj::mv(delimited); }
template <typename T>
inline const Delimited<T>& operator*(::kj::_::Stringifier, const Delimited<T>& delimited) { return delimited; }

}

template <typename T>
_::Delimited<T> delimited(T&& arr, kj::StringPtr delim) {
  return _::Delimited<T>(kj::fwd<T>(arr), delim);
}

}

constexpr kj::StringPtr operator "" _kj(const char* str, size_t n) {
  return kj::StringPtr(kj::ArrayPtr<const char>(str, n + 1));
};


# 27 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 2
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/windows-sanity.h" 1
# 42 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/windows-sanity.h"
       
# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 2


# 29 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h"

# 29 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 3


namespace kj {

class ExceptionImpl;
template <typename T> class Function;

class Exception {





public:
  enum class Type {


    FAILED = 0,



    OVERLOADED = 1,







    DISCONNECTED = 2,



    UNIMPLEMENTED = 3






  };

  Exception(Type type, const char* file, int line, String description = nullptr) noexcept;
  Exception(Type type, String file, int line, String description = nullptr) noexcept;
  Exception(const Exception& other) noexcept;
  Exception(Exception&& other) = default;
  ~Exception() noexcept;

  const char* getFile() const { return file; }
  int getLine() const { return line; }
  Type getType() const { return type; }
  StringPtr getDescription() const { return description; }
  ArrayPtr<void* const> getStackTrace() const { return arrayPtr(trace, traceCount); }

  StringPtr getRemoteTrace() const { return remoteTrace; }
  void setRemoteTrace(kj::String&& value) { remoteTrace = kj::mv(value); }





  struct Context {


    const char* file;
    int line;
    String description;
    Maybe<Own<Context>> next;

    Context(const char* file, int line, String&& description, Maybe<Own<Context>>&& next)
        : file(file), line(line), description(mv(description)), next(mv(next)) {}
    Context(const Context& other) noexcept;
  };

  inline Maybe<const Context&> getContext() const {
    if (auto c = ::kj::_::readMaybe(context)) {
      return **c;
    } else {
      return nullptr;
    }
  }

  void wrapContext(const char* file, int line, String&& description);




  __attribute__((noinline)) void extendTrace(uint ignoreCount, uint limit = kj::maxValue);





  __attribute__((noinline)) void truncateCommonTrace();




  void addTrace(void* ptr);



  __attribute__((noinline)) void addTraceHere();


private:
  String ownFile;
  const char* file;
  int line;
  Type type;
  String description;
  Maybe<Own<Context>> context;
  String remoteTrace;
  void* trace[32];
  uint traceCount;

  friend class ExceptionImpl;
};

struct CanceledException { };





StringPtr operator*(::kj::_::Stringifier, Exception::Type type);
String operator*(::kj::_::Stringifier, const Exception& e);



enum class LogSeverity {
  INFO,


  WARNING,
  ERROR,
  FATAL,
  DBG


};

StringPtr operator*(::kj::_::Stringifier, LogSeverity severity);

class ExceptionCallback {
# 186 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 3
public:
  ExceptionCallback();
  ExceptionCallback(const ExceptionCallback&) = delete; ExceptionCallback& operator=(const ExceptionCallback&) = delete;
  virtual ~ExceptionCallback() noexcept(false);

  virtual void onRecoverableException(Exception&& exception);







  virtual void onFatalException(Exception&& exception);







  virtual void logMessage(LogSeverity severity, const char* file, int line, int contextDepth,
                          String&& text);





  enum class StackTraceMode {
    FULL,





    ADDRESS_ONLY,




    NONE





  };

  virtual StackTraceMode stackTraceMode();


  virtual Function<void(Function<void()>)> getThreadInitializer();




protected:
  ExceptionCallback& next;

private:
  ExceptionCallback(ExceptionCallback& next);

  class RootExceptionCallback;
  friend ExceptionCallback& getExceptionCallback();

  friend class Thread;
};

ExceptionCallback& getExceptionCallback();


__attribute__((noinline)) void throwFatalException(kj::Exception&& exception, uint ignoreCount = 0) __attribute__((noreturn));



__attribute__((noinline)) void throwRecoverableException(kj::Exception&& exception, uint ignoreCount = 0);





namespace _ { class Runnable; }

template <typename Func>
Maybe<Exception> runCatchingExceptions(Func&& func);







class UnwindDetector {
# 288 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 3
public:
  UnwindDetector();

  bool isUnwinding() const;



  template <typename Func>
  void catchExceptionsIfUnwinding(Func&& func) const;




private:
  uint uncaughtCount;

  void catchExceptionsAsSecondaryFaults(_::Runnable& runnable) const;
};

namespace _ {

class Runnable {
public:
  virtual void run() = 0;
};

template <typename Func>
class RunnableImpl: public Runnable {
public:
  RunnableImpl(Func&& func): func(kj::fwd<Func>(func)) {}
  void run() override {
    func();
  }
private:
  Func func;
};

Maybe<Exception> runCatchingExceptions(Runnable& runnable);

}

template <typename Func>
Maybe<Exception> runCatchingExceptions(Func&& func) {
  _::RunnableImpl<Func> runnable(kj::fwd<Func>(func));
  return _::runCatchingExceptions(runnable);
}

template <typename Func>
void UnwindDetector::catchExceptionsIfUnwinding(Func&& func) const {
  if (isUnwinding()) {
    _::RunnableImpl<Decay<Func>> runnable(kj::fwd<Func>(func));
    catchExceptionsAsSecondaryFaults(runnable);
  } else {
    func();
  }
}
# 357 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 3
__attribute__((noinline)) ArrayPtr<void* const> getStackTrace(ArrayPtr<void*> space, uint ignoreCount);
# 370 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 3
String stringifyStackTrace(ArrayPtr<void* const>);



String stringifyStackTraceAddresses(ArrayPtr<void* const> trace);
StringPtr stringifyStackTraceAddresses(ArrayPtr<void* const> trace, ArrayPtr<char> scratch);







String getStackTrace();


void printStackTraceOnCrash();




kj::StringPtr trimSourceFilename(kj::StringPtr filename);



kj::String getCaughtExceptionType();
# 426 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/exception.h" 3
kj::Exception getDestructionReason(void* traceSeparator,
    kj::Exception::Type defaultType, const char* defaultFile, int defaultLine,
    kj::StringPtr defaultDescription);







kj::ArrayPtr<void* const> computeRelativeTrace(
    kj::ArrayPtr<void* const> trace, kj::ArrayPtr<void* const> relativeTo);





}


# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-prelude.h" 2
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/tuple.h" 1
# 38 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/tuple.h"
       




# 42 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/tuple.h"

# 42 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/tuple.h" 3


namespace kj {
namespace _ {

template <size_t index, typename... T>
struct TypeByIndex_;
template <typename First, typename... Rest>
struct TypeByIndex_<0, First, Rest...> {
  typedef First Type;
};
template <size_t index, typename First, typename... Rest>
struct TypeByIndex_<index, First, Rest...>
    : public TypeByIndex_<index - 1, Rest...> {};
template <size_t index>
struct TypeByIndex_<index> {
  static_assert(index != index, "Index out-of-range.");
};
template <size_t index, typename... T>
using TypeByIndex = typename TypeByIndex_<index, T...>::Type;


template <size_t... s>
struct Indexes {};



template <size_t end, size_t... prefix>
struct MakeIndexes_: public MakeIndexes_<end - 1, end - 1, prefix...> {};
template <size_t... prefix>
struct MakeIndexes_<0, prefix...> {
  typedef Indexes<prefix...> Type;
};
template <size_t end>
using MakeIndexes = typename MakeIndexes_<end>::Type;


template <typename... T>
class Tuple;
template <size_t index, typename... U>
inline TypeByIndex<index, U...>& getImpl(Tuple<U...>& tuple);
template <size_t index, typename... U>
inline TypeByIndex<index, U...>&& getImpl(Tuple<U...>&& tuple);
template <size_t index, typename... U>
inline const TypeByIndex<index, U...>& getImpl(const Tuple<U...>& tuple);

template <uint index, typename T>
struct TupleElement {



  T value;
  TupleElement() = default;
  constexpr inline TupleElement(const T& value): value(value) {}
  constexpr inline TupleElement(T&& value): value(kj::mv(value)) {}
};

template <uint index, typename T>
struct TupleElement<index, T&> {


  T& value;
  constexpr inline TupleElement(T& value): value(value) {}
};

template <uint index, typename... T>
struct TupleElement<index, Tuple<T...>> {
  static_assert(sizeof(Tuple<T...>*) == 0,
                "Tuples cannot contain other tuples -- they should be flattened.");
};

template <typename Indexes, typename... Types>
struct TupleImpl;

template <size_t... indexes, typename... Types>
struct TupleImpl<Indexes<indexes...>, Types...>
    : public TupleElement<indexes, Types>... {



  static_assert(sizeof...(indexes) == sizeof...(Types), "Incorrect use of TupleImpl.");

  TupleImpl() = default;

  template <typename... Params>
  inline TupleImpl(Params&&... params)
      : TupleElement<indexes, Types>(kj::fwd<Params>(params))... {


    static_assert(sizeof...(params) == sizeof...(indexes),
                  "Wrong number of parameters to Tuple constructor.");
  }

  template <typename... U>
  constexpr inline TupleImpl(Tuple<U...>&& other)
      : TupleElement<indexes, Types>(kj::fwd<U>(getImpl<indexes>(other)))... {}
  template <typename... U>
  constexpr inline TupleImpl(Tuple<U...>& other)
      : TupleElement<indexes, Types>(getImpl<indexes>(other))... {}
  template <typename... U>
  constexpr inline TupleImpl(const Tuple<U...>& other)
      : TupleElement<indexes, Types>(getImpl<indexes>(other))... {}
};

struct MakeTupleFunc;
struct MakeRefTupleFunc;

template <typename... T>
class Tuple {


public:
  Tuple() = default;

  template <typename... U>
  constexpr inline Tuple(Tuple<U...>&& other): impl(kj::mv(other)) {}
  template <typename... U>
  constexpr inline Tuple(Tuple<U...>& other): impl(other) {}
  template <typename... U>
  constexpr inline Tuple(const Tuple<U...>& other): impl(other) {}

private:
  template <typename... Params>
  constexpr Tuple(Params&&... params): impl(kj::fwd<Params>(params)...) {}

  TupleImpl<MakeIndexes<sizeof...(T)>, T...> impl;

  template <size_t index, typename... U>
  friend inline TypeByIndex<index, U...>& getImpl(Tuple<U...>& tuple);
  template <size_t index, typename... U>
  friend inline TypeByIndex<index, U...>&& getImpl(Tuple<U...>&& tuple);
  template <size_t index, typename... U>
  friend inline const TypeByIndex<index, U...>& getImpl(const Tuple<U...>& tuple);
  friend struct MakeTupleFunc;
  friend struct MakeRefTupleFunc;
};

template <>
class Tuple<> {


};

template <typename T>
class Tuple<T>;


template <size_t index, typename... T>
inline TypeByIndex<index, T...>& getImpl(Tuple<T...>& tuple) {

  static_assert(index < sizeof...(T), "Tuple element index out-of-bounds.");
  return implicitCast<TupleElement<index, TypeByIndex<index, T...>>&>(tuple.impl).value;
}
template <size_t index, typename... T>
inline TypeByIndex<index, T...>&& getImpl(Tuple<T...>&& tuple) {

  static_assert(index < sizeof...(T), "Tuple element index out-of-bounds.");
  return kj::mv(implicitCast<TupleElement<index, TypeByIndex<index, T...>>&>(tuple.impl).value);
}
template <size_t index, typename... T>
inline const TypeByIndex<index, T...>& getImpl(const Tuple<T...>& tuple) {

  static_assert(index < sizeof...(T), "Tuple element index out-of-bounds.");
  return implicitCast<const TupleElement<index, TypeByIndex<index, T...>>&>(tuple.impl).value;
}
template <size_t index, typename T>
inline T&& getImpl(T&& value) {



  static_assert(index == 0, "Tuple element index out-of-bounds.");
  return kj::fwd<T>(value);
}


template <typename Func, typename SoFar, typename... T>
struct ExpandAndApplyResult_;




template <typename Func, typename First, typename... Rest, typename... T>
struct ExpandAndApplyResult_<Func, Tuple<T...>, First, Rest...>
    : public ExpandAndApplyResult_<Func, Tuple<T..., First>, Rest...> {};
template <typename Func, typename... FirstTypes, typename... Rest, typename... T>
struct ExpandAndApplyResult_<Func, Tuple<T...>, Tuple<FirstTypes...>, Rest...>
    : public ExpandAndApplyResult_<Func, Tuple<T...>, FirstTypes&&..., Rest...> {};
template <typename Func, typename... FirstTypes, typename... Rest, typename... T>
struct ExpandAndApplyResult_<Func, Tuple<T...>, Tuple<FirstTypes...>&, Rest...>
    : public ExpandAndApplyResult_<Func, Tuple<T...>, FirstTypes&..., Rest...> {};
template <typename Func, typename... FirstTypes, typename... Rest, typename... T>
struct ExpandAndApplyResult_<Func, Tuple<T...>, const Tuple<FirstTypes...>&, Rest...>
    : public ExpandAndApplyResult_<Func, Tuple<T...>, const FirstTypes&..., Rest...> {};
template <typename Func, typename... T>
struct ExpandAndApplyResult_<Func, Tuple<T...>> {
  typedef decltype(instance<Func>()(instance<T&&>()...)) Type;
};
template <typename Func, typename... T>
using ExpandAndApplyResult = typename ExpandAndApplyResult_<Func, Tuple<>, T...>::Type;


template <typename Func>
inline auto expandAndApply(Func&& func) -> ExpandAndApplyResult<Func> {
  return func();
}

template <typename Func, typename First, typename... Rest>
struct ExpandAndApplyFunc {
  Func&& func;
  First&& first;
  ExpandAndApplyFunc(Func&& func, First&& first)
      : func(kj::fwd<Func>(func)), first(kj::fwd<First>(first)) {}
  template <typename... T>
  auto operator()(T&&... params)
      -> decltype(this->func(kj::fwd<First>(first), kj::fwd<T>(params)...)) {
    return this->func(kj::fwd<First>(first), kj::fwd<T>(params)...);
  }
};

template <typename Func, typename First, typename... Rest>
inline auto expandAndApply(Func&& func, First&& first, Rest&&... rest)
    -> ExpandAndApplyResult<Func, First, Rest...> {

  return expandAndApply(
      ExpandAndApplyFunc<Func, First, Rest...>(kj::fwd<Func>(func), kj::fwd<First>(first)),
      kj::fwd<Rest>(rest)...);
}

template <typename Func, typename... FirstTypes, typename... Rest>
inline auto expandAndApply(Func&& func, Tuple<FirstTypes...>&& first, Rest&&... rest)
    -> ExpandAndApplyResult<Func, FirstTypes&&..., Rest...> {
  return expandAndApplyWithIndexes(MakeIndexes<sizeof...(FirstTypes)>(),
      kj::fwd<Func>(func), kj::mv(first), kj::fwd<Rest>(rest)...);
}

template <typename Func, typename... FirstTypes, typename... Rest>
inline auto expandAndApply(Func&& func, Tuple<FirstTypes...>& first, Rest&&... rest)
    -> ExpandAndApplyResult<Func, FirstTypes..., Rest...> {
  return expandAndApplyWithIndexes(MakeIndexes<sizeof...(FirstTypes)>(),
      kj::fwd<Func>(func), first, kj::fwd<Rest>(rest)...);
}

template <typename Func, typename... FirstTypes, typename... Rest>
inline auto expandAndApply(Func&& func, const Tuple<FirstTypes...>& first, Rest&&... rest)
    -> ExpandAndApplyResult<Func, FirstTypes..., Rest...> {
  return expandAndApplyWithIndexes(MakeIndexes<sizeof...(FirstTypes)>(),
      kj::fwd<Func>(func), first, kj::fwd<Rest>(rest)...);
}

template <typename Func, typename... FirstTypes, typename... Rest, size_t... indexes>
inline auto expandAndApplyWithIndexes(
    Indexes<indexes...>, Func&& func, Tuple<FirstTypes...>&& first, Rest&&... rest)
    -> ExpandAndApplyResult<Func, FirstTypes&&..., Rest...> {
  return expandAndApply(kj::fwd<Func>(func), kj::mv(getImpl<indexes>(first))...,
                        kj::fwd<Rest>(rest)...);
}

template <typename Func, typename... FirstTypes, typename... Rest, size_t... indexes>
inline auto expandAndApplyWithIndexes(
    Indexes<indexes...>, Func&& func, const Tuple<FirstTypes...>& first, Rest&&... rest)
    -> ExpandAndApplyResult<Func, FirstTypes..., Rest...> {
  return expandAndApply(kj::fwd<Func>(func), getImpl<indexes>(first)...,
                       kj::fwd<Rest>(rest)...);
}

struct MakeTupleFunc {
  template <typename... Params>
  Tuple<Decay<Params>...> operator()(Params&&... params) {
    return Tuple<Decay<Params>...>(kj::fwd<Params>(params)...);
  }
  template <typename Param>
  Decay<Param> operator()(Param&& param) {
    return kj::fwd<Param>(param);
  }
};

struct MakeRefTupleFunc {
  template <typename... Params>
  Tuple<Params...> operator()(Params&&... params) {
    return Tuple<Params...>(kj::fwd<Params>(params)...);
  }
  template <typename Param>
  Param operator()(Param&& param) {
    return kj::fwd<Param>(param);
  }
};

}

template <typename... T> struct Tuple_ { typedef _::Tuple<T...> Type; };
template <typename T> struct Tuple_<T> { typedef T Type; };

template <typename... T> using Tuple = typename Tuple_<T...>::Type;
# 343 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/tuple.h" 3
template <typename... Params>
inline auto tuple(Params&&... params)
    -> decltype(_::expandAndApply(_::MakeTupleFunc(), kj::fwd<Params>(params)...)) {


  return _::expandAndApply(_::MakeTupleFunc(), kj::fwd<Params>(params)...);
}

template <typename... Params>
inline auto refTuple(Params&&... params)
    -> decltype(_::expandAndApply(_::MakeRefTupleFunc(), kj::fwd<Params>(params)...)) {


  return _::expandAndApply(_::MakeRefTupleFunc(), kj::fwd<Params>(params)...);
}

template <size_t index, typename Tuple>
inline auto get(Tuple&& tuple) -> decltype(_::getImpl<index>(kj::fwd<Tuple>(tuple))) {


  return _::getImpl<index>(kj::fwd<Tuple>(tuple));
}

template <typename Func, typename... Params>
inline auto apply(Func&& func, Params&&... params)
    -> decltype(_::expandAndApply(kj::fwd<Func>(func), kj::fwd<Params>(params)...)) {

  return _::expandAndApply(kj::fwd<Func>(func), kj::fwd<Params>(params)...);
}

template <typename T> struct TupleSize_ { static constexpr size_t size = 1; };
template <typename... T> struct TupleSize_<_::Tuple<T...>> {
  static constexpr size_t size = sizeof...(T);
};

template <typename T>
constexpr size_t tupleSize() { return TupleSize_<T>::size; }


template <typename T, typename Tuple>
struct IndexOfType_;
template <typename T, typename Tuple>
struct HasType_ {
  static constexpr bool value = false;
};

template <typename T>
struct IndexOfType_<T, T> {
  static constexpr size_t value = 0;
};
template <typename T>
struct HasType_<T, T> {
  static constexpr bool value = true;
};

template <typename T, typename... U>
struct IndexOfType_<T, _::Tuple<T, U...>> {
  static constexpr size_t value = 0;
  static_assert(!HasType_<T, _::Tuple<U...>>::value,
      "requested type appears multiple times in tuple");
};
template <typename T, typename... U>
struct HasType_<T, _::Tuple<T, U...>> {
  static constexpr bool value = true;
};

template <typename T, typename U, typename... V>
struct IndexOfType_<T, _::Tuple<U, V...>> {
  static constexpr size_t value = IndexOfType_<T, _::Tuple<V...>>::value + 1;
};
template <typename T, typename U, typename... V>
struct HasType_<T, _::Tuple<U, V...>> {
  static constexpr bool value = HasType_<T, _::Tuple<V...>>::value;
};

template <typename T, typename U>
inline constexpr size_t indexOfType() {
  static_assert(HasType_<T, U>::value, "type not present");
  return IndexOfType_<T, U>::value;
}

template <size_t i, typename T>
struct TypeOfIndex_;
template <typename T>
struct TypeOfIndex_<0, T> {
  typedef T Type;
};
template <size_t i, typename T, typename... U>
struct TypeOfIndex_<i, _::Tuple<T, U...>>
    : public TypeOfIndex_<i - 1, _::Tuple<U...>> {};
template <typename T, typename... U>
struct TypeOfIndex_<0, _::Tuple<T, U...>> {
  typedef T Type;
};

template <size_t i, typename Tuple>
using TypeOfIndex = typename TypeOfIndex_<i, Tuple>::Type;

}


# 29 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-prelude.h" 2
# 56 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-prelude.h"

# 56 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-prelude.h"

# 56 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-prelude.h" 3


namespace kj {

class EventLoop;
template <typename T>
class Promise;
class WaitScope;
class TaskSet;

template <typename T>
Promise<Array<T>> joinPromises(Array<Promise<T>>&& promises);
Promise<void> joinPromises(Array<Promise<void>>&& promises);

namespace _ {

template <typename T>
Promise<T> chainPromiseType(T*);
template <typename T>
Promise<T> chainPromiseType(Promise<T>*);

template <typename T>
using ChainPromises = decltype(chainPromiseType((T*)nullptr));



template <typename T>
Promise<T> reducePromiseType(T*, ...);
template <typename T>
Promise<T> reducePromiseType(Promise<T>*, ...);
template <typename T, typename Reduced = decltype(T::reducePromise(kj::instance<Promise<T>>()))>
Reduced reducePromiseType(T*, bool);

template <typename T>
using ReducePromises = decltype(reducePromiseType((T*)nullptr, false));




template <typename T> struct UnwrapPromise_;
template <typename T> struct UnwrapPromise_<Promise<T>> { typedef T Type; };

template <typename T>
using UnwrapPromise = typename UnwrapPromise_<T>::Type;

class PropagateException {


public:
  class Bottom {
  public:
    Bottom(Exception&& exception): exception(kj::mv(exception)) {}

    Exception asException() { return kj::mv(exception); }

  private:
    Exception exception;
  };

  Bottom operator()(Exception&& e) {
    return Bottom(kj::mv(e));
  }
  Bottom operator()(const Exception& e) {
    return Bottom(kj::cp(e));
  }
};

template <typename Func, typename T>
struct ReturnType_ { typedef decltype(instance<Func>()(instance<T>())) Type; };
template <typename Func>
struct ReturnType_<Func, void> { typedef decltype(instance<Func>()()) Type; };

template <typename Func, typename T>
using ReturnType = typename ReturnType_<Func, T>::Type;



template <typename T> struct SplitTuplePromise_ { typedef Promise<T> Type; };
template <typename... T>
struct SplitTuplePromise_<kj::_::Tuple<T...>> {
  typedef kj::Tuple<ReducePromises<T>...> Type;
};

template <typename T>
using SplitTuplePromise = typename SplitTuplePromise_<T>::Type;



struct Void {};


template <typename T> struct FixVoid_ { typedef T Type; };
template <> struct FixVoid_<void> { typedef Void Type; };
template <typename T> using FixVoid = typename FixVoid_<T>::Type;


template <typename T> struct UnfixVoid_ { typedef T Type; };
template <> struct UnfixVoid_<Void> { typedef void Type; };
template <typename T> using UnfixVoid = typename UnfixVoid_<T>::Type;


template <typename In, typename Out>
struct MaybeVoidCaller {



  template <typename Func>
  static inline Out apply(Func& func, In&& in) {
    return func(kj::mv(in));
  }
};
template <typename In, typename Out>
struct MaybeVoidCaller<In&, Out> {
  template <typename Func>
  static inline Out apply(Func& func, In& in) {
    return func(in);
  }
};
template <typename Out>
struct MaybeVoidCaller<Void, Out> {
  template <typename Func>
  static inline Out apply(Func& func, Void&& in) {
    return func();
  }
};
template <typename In>
struct MaybeVoidCaller<In, Void> {
  template <typename Func>
  static inline Void apply(Func& func, In&& in) {
    func(kj::mv(in));
    return Void();
  }
};
template <typename In>
struct MaybeVoidCaller<In&, Void> {
  template <typename Func>
  static inline Void apply(Func& func, In& in) {
    func(in);
    return Void();
  }
};
template <>
struct MaybeVoidCaller<Void, Void> {
  template <typename Func>
  static inline Void apply(Func& func, Void&& in) {
    func();
    return Void();
  }
};

template <typename T>
inline T&& returnMaybeVoid(T&& t) {
  return kj::fwd<T>(t);
}
inline void returnMaybeVoid(Void&& v) {}

class ExceptionOrValue;
class PromiseNode;
class ChainPromiseNode;
template <typename T>
class ForkHub;
class FiberStack;
class FiberBase;

class Event;
class XThreadEvent;
class XThreadPaf;

class PromiseBase {
public:
  kj::String trace();


private:
  Own<PromiseNode> node;

  PromiseBase() = default;
  PromiseBase(Own<PromiseNode>&& node): node(kj::mv(node)) {}

  template <typename>
  friend class kj::Promise;
  friend class PromiseNode;
};

void detach(kj::Promise<void>&& promise);
void waitImpl(Own<_::PromiseNode>&& node, _::ExceptionOrValue& result, WaitScope& waitScope);
bool pollImpl(_::PromiseNode& node, WaitScope& waitScope);
Promise<void> yield();
Promise<void> yieldHarder();
Own<PromiseNode> neverDone();

class NeverDone {
public:
  template <typename T>
  operator Promise<T>() const;

  void wait(WaitScope& waitScope) const __attribute__((noreturn));
};

}
}


# 25 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 2

# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/refcount.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/refcount.h"
       
# 34 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/refcount.h"

# 34 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/refcount.h"

# 34 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/refcount.h" 3


namespace kj {




class Refcounted: private Disposer {
# 67 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/refcount.h" 3
public:
  Refcounted() = default;
  virtual ~Refcounted() noexcept(false);
  Refcounted(const Refcounted&) = delete; Refcounted& operator=(const Refcounted&) = delete;

  inline bool isShared() const { return refcount > 1; }



private:
  mutable uint refcount = 0;


  void disposeImpl(void* pointer) const override;
  template <typename T>
  static Own<T> addRefInternal(T* object);

  template <typename T>
  friend Own<T> addRef(T& object);
  template <typename T, typename... Params>
  friend Own<T> refcounted(Params&&... params);
};

template <typename T, typename... Params>
inline Own<T> refcounted(Params&&... params) {



  return Refcounted::addRefInternal(new T(kj::fwd<Params>(params)...));
}

template <typename T>
Own<T> addRef(T& object) {




  if (__builtin_expect(object.Refcounted::refcount > 0, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/refcount.h", 104, "object.Refcounted::refcount > 0", "\"Object not allocated with kj::refcounted().\"", "Object not allocated with kj::refcounted().");
  return Refcounted::addRefInternal(&object);
}

template <typename T>
Own<T> Refcounted::addRefInternal(T* object) {
  Refcounted* refcounted = object;
  ++refcounted->refcount;
  return Own<T>(object, *refcounted);
}
# 128 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/refcount.h" 3
class AtomicRefcounted: private kj::Disposer {
public:
  AtomicRefcounted() = default;
  virtual ~AtomicRefcounted() noexcept(false);
  AtomicRefcounted(const AtomicRefcounted&) = delete; AtomicRefcounted& operator=(const AtomicRefcounted&) = delete;

  inline bool isShared() const {



    return __atomic_load_n(&refcount, 2) > 1;

  }

private:



  mutable volatile uint refcount = 0;


  bool addRefWeakInternal() const;

  void disposeImpl(void* pointer) const override;
  template <typename T>
  static kj::Own<T> addRefInternal(T* object);
  template <typename T>
  static kj::Own<const T> addRefInternal(const T* object);

  template <typename T>
  friend kj::Own<T> atomicAddRef(T& object);
  template <typename T>
  friend kj::Own<const T> atomicAddRef(const T& object);
  template <typename T>
  friend kj::Maybe<kj::Own<const T>> atomicAddRefWeak(const T& object);
  template <typename T, typename... Params>
  friend kj::Own<T> atomicRefcounted(Params&&... params);
};

template <typename T, typename... Params>
inline kj::Own<T> atomicRefcounted(Params&&... params) {
  return AtomicRefcounted::addRefInternal(new T(kj::fwd<Params>(params)...));
}

template <typename T>
kj::Own<T> atomicAddRef(T& object) {
  if (__builtin_expect(object.AtomicRefcounted::refcount > 0, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/refcount.h", 174, "object.AtomicRefcounted::refcount > 0", "\"Object not allocated with kj::atomicRefcounted().\"", "Object not allocated with kj::atomicRefcounted().")
                                                          ;
  return AtomicRefcounted::addRefInternal(&object);
}

template <typename T>
kj::Own<const T> atomicAddRef(const T& object) {
  if (__builtin_expect(object.AtomicRefcounted::refcount > 0, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/refcount.h", 181, "object.AtomicRefcounted::refcount > 0", "\"Object not allocated with kj::atomicRefcounted().\"", "Object not allocated with kj::atomicRefcounted().")
                                                          ;
  return AtomicRefcounted::addRefInternal(&object);
}

template <typename T>
kj::Maybe<kj::Own<const T>> atomicAddRefWeak(const T& object) {







  const AtomicRefcounted* refcounted = &object;
  if (refcounted->addRefWeakInternal()) {
    return kj::Own<const T>(&object, *refcounted);
  } else {
    return nullptr;
  }
}

template <typename T>
kj::Own<T> AtomicRefcounted::addRefInternal(T* object) {
  AtomicRefcounted* refcounted = object;



  __atomic_add_fetch(&refcounted->refcount, 1, 0);

  return kj::Own<T>(object, *refcounted);
}

template <typename T>
kj::Own<const T> AtomicRefcounted::addRefInternal(const T* object) {
  const AtomicRefcounted* refcounted = object;



  __atomic_add_fetch(&refcounted->refcount, 1, 0);

  return kj::Own<const T>(object, *refcounted);
}

}


# 27 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 2


# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h"

# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3


namespace kj {

class EventLoop;
class WaitScope;

template <typename T>
class Promise;
template <typename T>
class ForkedPromise;
template <typename T>
class PromiseFulfiller;
template <typename T>
struct PromiseFulfillerPair;

template <typename Func>
class FunctionParam;

template <typename Func, typename T>
using PromiseForResult = _::ReducePromises<_::ReturnType<Func, T>>;







template <typename T>
class Promise: protected _::PromiseBase {
# 126 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
public:
  Promise(_::FixVoid<T> value);






  Promise(kj::Exception&& e);


  inline Promise(decltype(nullptr)) {}

  template <typename Func, typename ErrorFunc = _::PropagateException>
  PromiseForResult<Func, T> then(Func&& func, ErrorFunc&& errorHandler = _::PropagateException())
      __attribute__((warn_unused_result));
# 196 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  Promise<void> ignoreResult() __attribute__((warn_unused_result)) { return then([](T&&) {}); }




  template <typename ErrorFunc>
  Promise<T> catch_(ErrorFunc&& errorHandler) __attribute__((warn_unused_result));



  T wait(WaitScope& waitScope);
# 246 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  bool poll(WaitScope& waitScope);
# 261 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  ForkedPromise<T> fork() __attribute__((warn_unused_result));





  _::SplitTuplePromise<T> split();





  Promise<T> exclusiveJoin(Promise<T>&& other) __attribute__((warn_unused_result));






  template <typename... Attachments>
  Promise<T> attach(Attachments&&... attachments) __attribute__((warn_unused_result));





  template <typename ErrorFunc>
  Promise<T> eagerlyEvaluate(ErrorFunc&& errorHandler) __attribute__((warn_unused_result));
  Promise<T> eagerlyEvaluate(decltype(nullptr)) __attribute__((warn_unused_result));
# 300 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  template <typename ErrorFunc>
  void detach(ErrorFunc&& errorHandler);
# 313 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  kj::String trace();



private:
  Promise(bool, Own<_::PromiseNode>&& node): PromiseBase(kj::mv(node)) {}


  friend class _::PromiseNode;
};

template <typename T>
class ForkedPromise {



public:
  inline ForkedPromise(decltype(nullptr)) {}

  Promise<T> addBranch();


  bool hasBranches();


private:
  Own<_::ForkHub<_::FixVoid<T>>> hub;

  inline ForkedPromise(bool, Own<_::ForkHub<_::FixVoid<T>>>&& hub): hub(kj::mv(hub)) {}

  friend class Promise<T>;
  friend class EventLoop;
};

constexpr _::Void READY_NOW = _::Void();



constexpr _::NeverDone NEVER_DONE = _::NeverDone();




template <typename Func>
PromiseForResult<Func, void> evalLater(Func&& func) __attribute__((warn_unused_result));
# 374 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
template <typename Func>
PromiseForResult<Func, void> evalNow(Func&& func) __attribute__((warn_unused_result));




template <typename Func>
PromiseForResult<Func, void> evalLast(Func&& func) __attribute__((warn_unused_result));
# 399 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
ArrayPtr<void* const> getAsyncTrace(ArrayPtr<void*> space);
kj::String getAsyncTrace();




template <typename Func>
PromiseForResult<Func, void> retryOnDisconnect(Func&& func) __attribute__((warn_unused_result));






template <typename Func>
PromiseForResult<Func, WaitScope&> startFiber(size_t stackSize, Func&& func) __attribute__((warn_unused_result));
# 432 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
class FiberPool final {




public:
  explicit FiberPool(size_t stackSize);
  ~FiberPool() noexcept(false);
  FiberPool(const FiberPool&) = delete; FiberPool& operator=(const FiberPool&) = delete;

  void setMaxFreelist(size_t count);



  void useCoreLocalFreelists();





  template <typename Func>
  PromiseForResult<Func, WaitScope&> startFiber(Func&& func) const __attribute__((warn_unused_result));






  void runSynchronously(kj::FunctionParam<void()> func) const;
# 474 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  size_t getFreelistSize() const;


private:
  class Impl;
  Own<Impl> impl;

  friend class _::FiberStack;
  friend class _::FiberBase;
};

template <typename T>
Promise<Array<T>> joinPromises(Array<Promise<T>>&& promises);





template <typename Func, typename MovedParam>
class CaptureByMove {
public:
  inline CaptureByMove(Func&& func, MovedParam&& param)
      : func(kj::mv(func)), param(kj::mv(param)) {}

  template <typename... Params>
  inline auto operator()(Params&&... params)
      -> decltype(kj::instance<Func>()(kj::instance<MovedParam&&>(), kj::fwd<Params>(params)...)) {
    return func(kj::mv(param), kj::fwd<Params>(params)...);
  }

private:
  Func func;
  MovedParam param;
};

template <typename Func, typename MovedParam>
inline CaptureByMove<Func, Decay<MovedParam>> mvCapture(MovedParam&& param, Func&& func) {
# 521 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  return CaptureByMove<Func, Decay<MovedParam>>(kj::fwd<Func>(func), kj::mv(param));
}




class PromiseRejector {


public:
  virtual void reject(Exception&& exception) = 0;
  virtual bool isWaiting() = 0;
};

template <typename T>
class PromiseFulfiller: public PromiseRejector {



public:
  virtual void fulfill(T&& value) = 0;


  virtual void reject(Exception&& exception) = 0;


  virtual bool isWaiting() = 0;




  template <typename Func>
  bool rejectIfThrows(Func&& func);



};

template <>
class PromiseFulfiller<void>: public PromiseRejector {


public:
  virtual void fulfill(_::Void&& value = _::Void()) = 0;



  virtual void reject(Exception&& exception) = 0;
  virtual bool isWaiting() = 0;

  template <typename Func>
  bool rejectIfThrows(Func&& func);
};

template <typename T, typename Adapter, typename... Params>
_::ReducePromises<T> newAdaptedPromise(Params&&... adapterConstructorParams);
# 596 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
template <typename T>
struct PromiseFulfillerPair {
  _::ReducePromises<T> promise;
  Own<PromiseFulfiller<T>> fulfiller;
};

template <typename T>
PromiseFulfillerPair<T> newPromiseAndFulfiller();
# 616 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
template <typename T>
class CrossThreadPromiseFulfiller: public kj::PromiseFulfiller<T> {



public:
  virtual void fulfill(T&& value) const = 0;
  virtual void reject(Exception&& exception) const = 0;
  virtual bool isWaiting() const = 0;

  void fulfill(T&& value) override { return constThis()->fulfill(kj::fwd<T>(value)); }
  void reject(Exception&& exception) override { return constThis()->reject(kj::mv(exception)); }
  bool isWaiting() override { return constThis()->isWaiting(); }

private:
  const CrossThreadPromiseFulfiller* constThis() { return this; }
};

template <>
class CrossThreadPromiseFulfiller<void>: public kj::PromiseFulfiller<void> {



public:
  virtual void fulfill(_::Void&& value = _::Void()) const = 0;
  virtual void reject(Exception&& exception) const = 0;
  virtual bool isWaiting() const = 0;

  void fulfill(_::Void&& value) override { return constThis()->fulfill(kj::mv(value)); }
  void reject(Exception&& exception) override { return constThis()->reject(kj::mv(exception)); }
  bool isWaiting() override { return constThis()->isWaiting(); }

private:
  const CrossThreadPromiseFulfiller* constThis() { return this; }
};

template <typename T>
struct PromiseCrossThreadFulfillerPair {
  _::ReducePromises<T> promise;
  Own<CrossThreadPromiseFulfiller<T>> fulfiller;
};

template <typename T>
PromiseCrossThreadFulfillerPair<T> newPromiseAndCrossThreadFulfiller();
# 668 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
class Canceler {
# 691 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
public:
  inline Canceler() {}
  ~Canceler() noexcept(false);
  Canceler(const Canceler&) = delete; Canceler& operator=(const Canceler&) = delete;

  template <typename T>
  Promise<T> wrap(Promise<T> promise) {
    return newAdaptedPromise<T, AdapterImpl<T>>(*this, kj::mv(promise));
  }

  void cancel(StringPtr cancelReason);
  void cancel(const Exception& exception);




  void release();



  bool isEmpty() const { return list == nullptr; }



private:
  class AdapterBase {
  public:
    AdapterBase(Canceler& canceler);
    ~AdapterBase() noexcept(false);

    virtual void cancel(Exception&& e) = 0;

    void unlink();

  private:
    Maybe<Maybe<AdapterBase&>&> prev;
    Maybe<AdapterBase&> next;
    friend class Canceler;
  };

  template <typename T>
  class AdapterImpl: public AdapterBase {
  public:
    AdapterImpl(PromiseFulfiller<T>& fulfiller,
                Canceler& canceler, Promise<T> inner)
        : AdapterBase(canceler),
          fulfiller(fulfiller),
          inner(inner.then(
              [&fulfiller](T&& value) { fulfiller.fulfill(kj::mv(value)); },
              [&fulfiller](Exception&& e) { fulfiller.reject(kj::mv(e)); })
              .eagerlyEvaluate(nullptr)) {}

    void cancel(Exception&& e) override {
      fulfiller.reject(kj::mv(e));
      inner = nullptr;
    }

  private:
    PromiseFulfiller<T>& fulfiller;
    Promise<void> inner;
  };

  Maybe<AdapterBase&> list;
};

template <>
class Canceler::AdapterImpl<void>: public AdapterBase {
public:
  AdapterImpl(kj::PromiseFulfiller<void>& fulfiller,
              Canceler& canceler, kj::Promise<void> inner);
  void cancel(kj::Exception&& e) override;



private:
  kj::PromiseFulfiller<void>& fulfiller;
  kj::Promise<void> inner;
};




class TaskSet {
# 784 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
public:
  class ErrorHandler {
  public:
    virtual void taskFailed(kj::Exception&& exception) = 0;
  };

  TaskSet(ErrorHandler& errorHandler);



  ~TaskSet() noexcept(false);

  void add(Promise<void>&& promise);

  kj::String trace();


  bool isEmpty() { return tasks == nullptr; }


  Promise<void> onEmpty();



private:
  class Task;

  TaskSet::ErrorHandler& errorHandler;
  Maybe<Own<Task>> tasks;
  Maybe<Own<PromiseFulfiller<void>>> emptyFulfiller;
};




class Executor {






public:
  Executor(EventLoop& loop, Badge<EventLoop>);
  ~Executor() noexcept(false);

  virtual kj::Own<const Executor> addRef() const = 0;
# 841 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  bool isLive() const;
# 854 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  template <typename Func>
  PromiseForResult<Func, void> executeAsync(Func&& func) const;
# 896 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  template <typename Func>
  _::UnwrapPromise<PromiseForResult<Func, void>> executeSync(Func&& func) const;
# 909 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
private:
  struct Impl;
  Own<Impl> impl;


  friend class EventLoop;
  friend class _::XThreadEvent;
  friend class _::XThreadPaf;

  void send(_::XThreadEvent& event, bool sync) const;
  void wait();
  bool poll();

  EventLoop& getLoop() const;
};

const Executor& getCurrentThreadExecutor();






class EventPort {







public:
  virtual bool wait() = 0;
# 955 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  virtual bool poll() = 0;
# 966 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  virtual void setRunnable(bool runnable);





  virtual void wake() const;
# 982 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
};

class EventLoop {
# 1016 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
public:
  EventLoop();


  explicit EventLoop(EventPort& port);


  ~EventLoop() noexcept(false);

  void run(uint maxTurnCount = maxValue);




  bool isRunnable();


  const Executor& getExecutor();
# 1042 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
private:
  kj::Maybe<EventPort&> port;



  bool running = false;


  bool lastRunnableState = false;


  _::Event* head = nullptr;
  _::Event** tail = &head;
  _::Event** depthFirstInsertPoint = &head;
  _::Event** breadthFirstInsertPoint = &head;

  kj::Maybe<Own<Executor>> executor;


  Own<TaskSet> daemons;

  _::Event* currentlyFiring = nullptr;

  bool turn();
  void setRunnable(bool runnable);
  void enterScope();
  void leaveScope();

  void wait();
  void poll();

  friend void _::detach(kj::Promise<void>&& promise);
  friend void _::waitImpl(Own<_::PromiseNode>&& node, _::ExceptionOrValue& result,
                          WaitScope& waitScope);
  friend bool _::pollImpl(_::PromiseNode& node, WaitScope& waitScope);
  friend class _::Event;
  friend class WaitScope;
  friend class Executor;
  friend class _::XThreadEvent;
  friend class _::XThreadPaf;
  friend class _::FiberBase;
  friend class _::FiberStack;
  friend ArrayPtr<void* const> getAsyncTrace(ArrayPtr<void*> space);
};

class WaitScope {
# 1096 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
public:
  inline explicit WaitScope(EventLoop& loop): loop(loop) { loop.enterScope(); }
  inline ~WaitScope() { if (fiber == nullptr) loop.leaveScope(); }
  WaitScope(const WaitScope&) = delete; WaitScope& operator=(const WaitScope&) = delete;

  void poll();




  void setBusyPollInterval(uint count) { busyPollInterval = count; }





  void runEventCallbacksOnStackPool(kj::Maybe<const FiberPool&> pool) { runningStacksPool = pool; }
# 1131 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
  void cancelAllDetached();
# 1141 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 3
private:
  EventLoop& loop;
  uint busyPollInterval = kj::maxValue;

  kj::Maybe<_::FiberBase&> fiber;
  kj::Maybe<const FiberPool&> runningStacksPool;

  explicit WaitScope(EventLoop& loop, _::FiberBase& fiber)
      : loop(loop), fiber(fiber) {}

  template <typename Func>
  inline void runOnStackPool(Func&& func) {
    if (auto pool = ::kj::_::readMaybe(runningStacksPool)) {
      pool->runSynchronously(kj::fwd<Func>(func));
    } else {
      func();
    }
  }

  friend class EventLoop;
  friend class _::FiberBase;
  friend void _::waitImpl(Own<_::PromiseNode>&& node, _::ExceptionOrValue& result,
                          WaitScope& waitScope);
  friend bool _::pollImpl(_::PromiseNode& node, WaitScope& waitScope);
};

}


# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 1
# 27 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h"
       







# 34 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h"

# 34 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3


# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/list.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/list.h"
       




# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/list.h"

# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/list.h" 3


namespace kj {

template <typename T>
class ListLink;

template <typename T, typename MaybeConstT, ListLink<T> T::*link>
class ListIterator;

namespace _ {

void throwDoubleAdd() __attribute__((noreturn));
void throwRemovedNotPresent() __attribute__((noreturn));
void throwRemovedWrongList() __attribute__((noreturn));
void throwDestroyedWhileInList() __attribute__((noreturn));

}

template <typename T, ListLink<T> T::*link>
class List {
# 85 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/list.h" 3
public:
  List() = default;
  List(const List&) = delete; List& operator=(const List&) = delete;

  bool empty() const {
    return head == nullptr;
  }

  void add(T& element) {
    if ((element.*link).prev != nullptr) _::throwDoubleAdd();
    *tail = element;
    (element.*link).prev = tail;
    tail = &((element.*link).next);
  }

  void remove(T& element) {
    if ((element.*link).prev == nullptr) _::throwRemovedNotPresent();
    *((element.*link).prev) = (element.*link).next;
    if (auto n = ::kj::_::readMaybe((element.*link).next)) {
      (n->*link).prev = (element.*link).prev;
    } else {
      if (tail != &((element.*link).next)) _::throwRemovedWrongList();
      tail = (element.*link).prev;
    }
    (element.*link).next = nullptr;
    (element.*link).prev = nullptr;
  }

  typedef ListIterator<T, T, link> Iterator;
  typedef ListIterator<T, const T, link> ConstIterator;

  Iterator begin() { return Iterator(head); }
  Iterator end() { return Iterator(nullptr); }
  ConstIterator begin() const { return ConstIterator(head); }
  ConstIterator end() const { return ConstIterator(nullptr); }

  T& front() { return *begin(); }
  const T& front() const { return *begin(); }

private:
  Maybe<T&> head;
  Maybe<T&>* tail = &head;
};

template <typename T>
class ListLink {
public:
  ListLink(): next(nullptr), prev(nullptr) {}
  ~ListLink() noexcept {

    if (prev != nullptr) _::throwDestroyedWhileInList();
  }
  ListLink(const ListLink&) = delete; ListLink& operator=(const ListLink&) = delete;

  bool isLinked() const { return prev != nullptr; }

private:
  Maybe<T&> next;
  Maybe<T&>* prev;

  template <typename U, ListLink<U> U::*link>
  friend class List;
  template <typename U, typename MaybeConstU, ListLink<U> U::*link>
  friend class ListIterator;
};

template <typename T, typename MaybeConstT, ListLink<T> T::*link>
class ListIterator {
public:
  ListIterator() = default;

  MaybeConstT& operator*() {
    if (__builtin_expect(current != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/list.h", 157, "current != nullptr", "\"tried to dereference end of list\"", "tried to dereference end of list");
    return *_::readMaybe(current);
  }
  const T& operator*() const {
    if (__builtin_expect(current != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/list.h", 161, "current != nullptr", "\"tried to dereference end of list\"", "tried to dereference end of list");
    return *_::readMaybe(current);
  }
  MaybeConstT* operator->() {
    if (__builtin_expect(current != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/list.h", 165, "current != nullptr", "\"tried to dereference end of list\"", "tried to dereference end of list");
    return _::readMaybe(current);
  }
  const T* operator->() const {
    if (__builtin_expect(current != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/list.h", 169, "current != nullptr", "\"tried to dereference end of list\"", "tried to dereference end of list");
    return _::readMaybe(current);
  }

  inline ListIterator& operator++() {
    current = next;
    next = current.map([](MaybeConstT& obj) -> kj::Maybe<MaybeConstT&> { return (obj.*link).next; })
        .orDefault(nullptr);
    return *this;
  }
  inline ListIterator operator++(int) {
    ListIterator result = *this;
    ++*this;
    return result;
  }

  inline bool operator==(const ListIterator& other) const {
    return _::readMaybe(current) == _::readMaybe(other.current);
  }
  inline bool operator!=(const ListIterator& other) const {
    return _::readMaybe(current) != _::readMaybe(other.current);
  }

private:
  Maybe<MaybeConstT&> current;

  Maybe<MaybeConstT&> next;


  explicit ListIterator(Maybe<MaybeConstT&> start)
      : current(start),
        next(start.map([](MaybeConstT& obj) -> kj::Maybe<MaybeConstT&> { return (obj.*link).next; })
            .orDefault(nullptr)) {}
  friend class List<T, link>;
};

}


# 37 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 2 3

namespace kj {
namespace _ {

template <typename T>
class ExceptionOr;

class ExceptionOrValue {
public:
  ExceptionOrValue(bool, Exception&& exception): exception(kj::mv(exception)) {}
  ExceptionOrValue(const ExceptionOrValue&) = delete; ExceptionOrValue& operator=(const ExceptionOrValue&) = delete;

  void addException(Exception&& exception) {
    if (this->exception == nullptr) {
      this->exception = kj::mv(exception);
    }
  }

  template <typename T>
  ExceptionOr<T>& as() { return *static_cast<ExceptionOr<T>*>(this); }
  template <typename T>
  const ExceptionOr<T>& as() const { return *static_cast<const ExceptionOr<T>*>(this); }

  Maybe<Exception> exception;

protected:

  ExceptionOrValue() = default;
  ExceptionOrValue(ExceptionOrValue&& other) = default;
  ExceptionOrValue& operator=(ExceptionOrValue&& other) = default;
};

template <typename T>
class ExceptionOr: public ExceptionOrValue {
public:
  ExceptionOr() = default;
  ExceptionOr(T&& value): value(kj::mv(value)) {}
  ExceptionOr(bool, Exception&& exception): ExceptionOrValue(false, kj::mv(exception)) {}
  ExceptionOr(ExceptionOr&&) = default;
  ExceptionOr& operator=(ExceptionOr&&) = default;

  Maybe<T> value;
};

template <typename T>
inline T convertToReturn(ExceptionOr<T>&& result) {
  if (auto value = ::kj::_::readMaybe(result.value)) {
    if (auto exception = ::kj::_::readMaybe(result.exception)) {
      throwRecoverableException(kj::mv(*exception));
    }
    return _::returnMaybeVoid(kj::mv(*value));
  } else if (auto exception = ::kj::_::readMaybe(result.exception)) {
    throwFatalException(kj::mv(*exception));
  } else {

    ::kj::_::unreachable();;
  }
}

inline void convertToReturn(ExceptionOr<Void>&& result) {


  if (result.value != nullptr) {
    if (auto exception = ::kj::_::readMaybe(result.exception)) {
      throwRecoverableException(kj::mv(*exception));
    }
  } else if (auto exception = ::kj::_::readMaybe(result.exception)) {
    throwRecoverableException(kj::mv(*exception));
  } else {

    ::kj::_::unreachable();;
  }
}

class TraceBuilder {

public:
  TraceBuilder(ArrayPtr<void*> space)
      : start(space.begin()), current(space.begin()), limit(space.end()) {}

  inline void add(void* addr) {
    if (current < limit) {
      *current++ = addr;
    }
  }

  inline bool full() const { return current == limit; }

  ArrayPtr<void*> finish() {
    return arrayPtr(start, current);
  }

  String toString();

private:
  void** start;
  void** current;
  void** limit;
};

class Event {



public:
  Event();
  Event(kj::EventLoop& loop);
  ~Event() noexcept(false);
  Event(const Event&) = delete; Event& operator=(const Event&) = delete;

  void armDepthFirst();
# 162 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3
  void armBreadthFirst();


  void armLast();



  bool isNext();
# 178 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3
  void disarm();



  virtual void traceEvent(TraceBuilder& builder) = 0;
# 191 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3
  String traceEvent();


protected:
  virtual Maybe<Own<Event>> fire() = 0;




private:
  friend class kj::EventLoop;
  EventLoop& loop;
  Event* next;
  Event** prev;
  bool firing = false;
};

class PromiseNode {







public:
  virtual void onReady(Event* event) noexcept = 0;






  virtual void setSelfPointer(Own<PromiseNode>* selfPtr) noexcept;





  virtual void get(ExceptionOrValue& output) noexcept = 0;




  virtual void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) = 0;
# 257 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3
  template <typename T>
  static Own<PromiseNode> from(T&& promise) {

    return kj::mv(promise.node);
  }
  template <typename T>
  static PromiseNode& from(T& promise) {

    return *promise.node;
  }
  template <typename T>
  static T to(Own<PromiseNode>&& node) {

    return T(false, kj::mv(node));
  }

protected:
  class OnReadyEvent {


  public:
    void init(Event* newEvent);

    void arm();
    void armBreadthFirst();



    inline void traceEvent(TraceBuilder& builder) {
      if (event != nullptr && !builder.full()) event->traceEvent(builder);
    }

  private:
    Event* event = nullptr;
  };
};



template <typename T>
inline NeverDone::operator Promise<T>() const {
  return PromiseNode::to<Promise<T>>(neverDone());
}



class ImmediatePromiseNodeBase: public PromiseNode {
public:
  ImmediatePromiseNodeBase();
  ~ImmediatePromiseNodeBase() noexcept(false);

  void onReady(Event* event) noexcept override;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;
};

template <typename T>
class ImmediatePromiseNode final: public ImmediatePromiseNodeBase {


public:
  ImmediatePromiseNode(ExceptionOr<T>&& result): result(kj::mv(result)) {}

  void get(ExceptionOrValue& output) noexcept override {
    output.as<T>() = kj::mv(result);
  }

private:
  ExceptionOr<T> result;
};

class ImmediateBrokenPromiseNode final: public ImmediatePromiseNodeBase {
public:
  ImmediateBrokenPromiseNode(Exception&& exception);

  void get(ExceptionOrValue& output) noexcept override;

private:
  Exception exception;
};



class AttachmentPromiseNodeBase: public PromiseNode {
public:
  AttachmentPromiseNodeBase(Own<PromiseNode>&& dependency);

  void onReady(Event* event) noexcept override;
  void get(ExceptionOrValue& output) noexcept override;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;

private:
  Own<PromiseNode> dependency;

  void dropDependency();

  template <typename>
  friend class AttachmentPromiseNode;
};

template <typename Attachment>
class AttachmentPromiseNode final: public AttachmentPromiseNodeBase {



public:
  AttachmentPromiseNode(Own<PromiseNode>&& dependency, Attachment&& attachment)
      : AttachmentPromiseNodeBase(kj::mv(dependency)),
        attachment(kj::mv<Attachment>(attachment)) {}

  ~AttachmentPromiseNode() noexcept(false) {


    dropDependency();
  }

private:
  Attachment attachment;
};






#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wclass-memaccess"


template <typename T, typename ReturnType, typename... ParamTypes>
void* getMethodStartAddress(T& obj, ReturnType (T::*method)(ParamTypes...));
template <typename T, typename ReturnType, typename... ParamTypes>
void* getMethodStartAddress(const T& obj, ReturnType (T::*method)(ParamTypes...) const);
# 398 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3
class PtmfHelper {



  template <typename... ParamTypes>
  friend struct GetFunctorStartAddress;
  template <typename T, typename ReturnType, typename... ParamTypes>
  friend void* getMethodStartAddress(T& obj, ReturnType (T::*method)(ParamTypes...));
  template <typename T, typename ReturnType, typename... ParamTypes>
  friend void* getMethodStartAddress(const T& obj, ReturnType (T::*method)(ParamTypes...) const);



  void* ptr;
  ptrdiff_t adj;


  void* apply(const void* obj) {




    ptrdiff_t voff = (ptrdiff_t)ptr;
    if (voff & 1) {
      voff &= ~1;

      return *(void**)(*(char**)obj + voff);
    } else {
      return ptr;
    }
  }
# 445 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3
  template <typename R, typename C, typename... P, typename F>
  static PtmfHelper from(F p) { PtmfHelper result; static_assert(sizeof(p) == sizeof(result), "unknown ptmf layout"); memcpy(&result, &p, sizeof(result)); return result; }




  template <typename R, typename C, typename... P>
  static PtmfHelper from(R (C::*p)(NoInfer<P>...)) { PtmfHelper result; static_assert(sizeof(p) == sizeof(result), "unknown ptmf layout"); memcpy(&result, &p, sizeof(result)); return result; }
  template <typename R, typename C, typename... P>
  static PtmfHelper from(R (C::*p)(NoInfer<P>...) const) { PtmfHelper result; static_assert(sizeof(p) == sizeof(result), "unknown ptmf layout"); memcpy(&result, &p, sizeof(result)); return result; }






};


#pragma GCC diagnostic pop


template <typename T, typename ReturnType, typename... ParamTypes>
void* getMethodStartAddress(T& obj, ReturnType (T::*method)(ParamTypes...)) {
  return PtmfHelper::from<ReturnType, T, ParamTypes...>(method).apply(&obj);
}
template <typename T, typename ReturnType, typename... ParamTypes>
void* getMethodStartAddress(const T& obj, ReturnType (T::*method)(ParamTypes...) const) {
  return PtmfHelper::from<ReturnType, T, ParamTypes...>(method).apply(&obj);
}

template <typename... ParamTypes>
struct GetFunctorStartAddress {
# 489 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3
  template <typename Func>
  static void* apply(Func&& func) {
    typedef decltype(func(instance<ParamTypes>()...)) ReturnType;
    return PtmfHelper::from<ReturnType, Decay<Func>, ParamTypes...>(
        &Decay<Func>::operator()).apply(&func);
  }
};

template <>
struct GetFunctorStartAddress<Void&&>: public GetFunctorStartAddress<> {};



class TransformPromiseNodeBase: public PromiseNode {
public:
  TransformPromiseNodeBase(Own<PromiseNode>&& dependency, void* continuationTracePtr);

  void onReady(Event* event) noexcept override;
  void get(ExceptionOrValue& output) noexcept override;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;

private:
  Own<PromiseNode> dependency;
  void* continuationTracePtr;

  void dropDependency();
  void getDepResult(ExceptionOrValue& output);

  virtual void getImpl(ExceptionOrValue& output) = 0;

  template <typename, typename, typename, typename>
  friend class TransformPromiseNode;
};

template <typename T, typename DepT, typename Func, typename ErrorFunc>
class TransformPromiseNode final: public TransformPromiseNodeBase {



public:
  TransformPromiseNode(Own<PromiseNode>&& dependency, Func&& func, ErrorFunc&& errorHandler,
                       void* continuationTracePtr)
      : TransformPromiseNodeBase(kj::mv(dependency), continuationTracePtr),
        func(kj::fwd<Func>(func)), errorHandler(kj::fwd<ErrorFunc>(errorHandler)) {}

  ~TransformPromiseNode() noexcept(false) {



    dropDependency();
  }

private:
  Func func;
  ErrorFunc errorHandler;

  void getImpl(ExceptionOrValue& output) override {
    ExceptionOr<DepT> depResult;
    getDepResult(depResult);
    if (auto depException = ::kj::_::readMaybe(depResult.exception)) {
      output.as<T>() = handle(
          MaybeVoidCaller<Exception, FixVoid<ReturnType<ErrorFunc, Exception>>>::apply(
              errorHandler, kj::mv(*depException)));
    } else if (auto depValue = ::kj::_::readMaybe(depResult.value)) {
      output.as<T>() = handle(MaybeVoidCaller<DepT, T>::apply(func, kj::mv(*depValue)));
    }
  }

  ExceptionOr<T> handle(T&& value) {
    return kj::mv(value);
  }
  ExceptionOr<T> handle(PropagateException::Bottom&& value) {
    return ExceptionOr<T>(false, value.asException());
  }
};



class ForkHubBase;

class ForkBranchBase: public PromiseNode {
public:
  ForkBranchBase(Own<ForkHubBase>&& hub);
  ~ForkBranchBase() noexcept(false);

  void hubReady() noexcept;



  void onReady(Event* event) noexcept override;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;

protected:
  inline ExceptionOrValue& getHubResultRef();

  void releaseHub(ExceptionOrValue& output);


private:
  OnReadyEvent onReadyEvent;

  Own<ForkHubBase> hub;
  ForkBranchBase* next = nullptr;
  ForkBranchBase** prevPtr = nullptr;

  friend class ForkHubBase;
};

template <typename T> T copyOrAddRef(T& t) { return t; }
template <typename T> Own<T> copyOrAddRef(Own<T>& t) { return t->addRef(); }
template <typename T> Maybe<Own<T>> copyOrAddRef(Maybe<Own<T>>& t) {
  return t.map([](Own<T>& ptr) {
    return ptr->addRef();
  });
}

template <typename T>
class ForkBranch final: public ForkBranchBase {



public:
  ForkBranch(Own<ForkHubBase>&& hub): ForkBranchBase(kj::mv(hub)) {}

  void get(ExceptionOrValue& output) noexcept override {
    ExceptionOr<T>& hubResult = getHubResultRef().template as<T>();
    if (auto value = ::kj::_::readMaybe(hubResult.value)) {
      output.as<T>().value = copyOrAddRef(*value);
    } else {
      output.as<T>().value = nullptr;
    }
    output.exception = hubResult.exception;
    releaseHub(output);
  }
};

template <typename T, size_t index>
class SplitBranch final: public ForkBranchBase {



public:
  SplitBranch(Own<ForkHubBase>&& hub): ForkBranchBase(kj::mv(hub)) {}

  typedef kj::Decay<decltype(kj::get<index>(kj::instance<T>()))> Element;

  void get(ExceptionOrValue& output) noexcept override {
    ExceptionOr<T>& hubResult = getHubResultRef().template as<T>();
    if (auto value = ::kj::_::readMaybe(hubResult.value)) {
      output.as<Element>().value = kj::mv(kj::get<index>(*value));
    } else {
      output.as<Element>().value = nullptr;
    }
    output.exception = hubResult.exception;
    releaseHub(output);
  }
};



class ForkHubBase: public Refcounted, protected Event {
public:
  ForkHubBase(Own<PromiseNode>&& inner, ExceptionOrValue& resultRef);

  inline ExceptionOrValue& getResultRef() { return resultRef; }

private:
  Own<PromiseNode> inner;
  ExceptionOrValue& resultRef;

  ForkBranchBase* headBranch = nullptr;
  ForkBranchBase** tailBranch = &headBranch;


  Maybe<Own<Event>> fire() override;
  void traceEvent(TraceBuilder& builder) override;

  friend class ForkBranchBase;
};

template <typename T>
class ForkHub final: public ForkHubBase {




public:
  ForkHub(Own<PromiseNode>&& inner): ForkHubBase(kj::mv(inner), result) {}

  Promise<_::UnfixVoid<T>> addBranch() {
    return _::PromiseNode::to<Promise<_::UnfixVoid<T>>>(kj::heap<ForkBranch<T>>(addRef(*this)));
  }

  _::SplitTuplePromise<T> split() {
    return splitImpl(MakeIndexes<tupleSize<T>()>());
  }

private:
  ExceptionOr<T> result;

  template <size_t... indexes>
  _::SplitTuplePromise<T> splitImpl(Indexes<indexes...>) {
    return kj::tuple(addSplit<indexes>()...);
  }

  template <size_t index>
  ReducePromises<typename SplitBranch<T, index>::Element> addSplit() {
    return _::PromiseNode::to<ReducePromises<typename SplitBranch<T, index>::Element>>(
        maybeChain(kj::heap<SplitBranch<T, index>>(addRef(*this)),
                   implicitCast<typename SplitBranch<T, index>::Element*>(nullptr)));
  }
};

inline ExceptionOrValue& ForkBranchBase::getHubResultRef() {
  return hub->getResultRef();
}



class ChainPromiseNode final: public PromiseNode, public Event {





public:
  explicit ChainPromiseNode(Own<PromiseNode> inner);
  ~ChainPromiseNode() noexcept(false);

  void onReady(Event* event) noexcept override;
  void setSelfPointer(Own<PromiseNode>* selfPtr) noexcept override;
  void get(ExceptionOrValue& output) noexcept override;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;

private:
  enum State {
    STEP1,
    STEP2
  };

  State state;

  Own<PromiseNode> inner;



  Event* onReadyEvent = nullptr;
  Own<PromiseNode>* selfPtr = nullptr;

  Maybe<Own<Event>> fire() override;
  void traceEvent(TraceBuilder& builder) override;
};

template <typename T>
Own<PromiseNode> maybeChain(Own<PromiseNode>&& node, Promise<T>*) {
  return heap<ChainPromiseNode>(kj::mv(node));
}

template <typename T>
Own<PromiseNode>&& maybeChain(Own<PromiseNode>&& node, T*) {
  return kj::mv(node);
}

template <typename T, typename Result = decltype(T::reducePromise(instance<Promise<T>>()))>
inline Result maybeReduce(Promise<T>&& promise, bool) {
  return T::reducePromise(kj::mv(promise));
}

template <typename T>
inline Promise<T> maybeReduce(Promise<T>&& promise, ...) {
  return kj::mv(promise);
}



class ExclusiveJoinPromiseNode final: public PromiseNode {
public:
  ExclusiveJoinPromiseNode(Own<PromiseNode> left, Own<PromiseNode> right);
  ~ExclusiveJoinPromiseNode() noexcept(false);

  void onReady(Event* event) noexcept override;
  void get(ExceptionOrValue& output) noexcept override;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;

private:
  class Branch: public Event {
  public:
    Branch(ExclusiveJoinPromiseNode& joinNode, Own<PromiseNode> dependency);
    ~Branch() noexcept(false);

    bool get(ExceptionOrValue& output);


    Maybe<Own<Event>> fire() override;
    void traceEvent(TraceBuilder& builder) override;

  private:
    ExclusiveJoinPromiseNode& joinNode;
    Own<PromiseNode> dependency;

    friend class ExclusiveJoinPromiseNode;
  };

  Branch left;
  Branch right;
  OnReadyEvent onReadyEvent;
};



class ArrayJoinPromiseNodeBase: public PromiseNode {
public:
  ArrayJoinPromiseNodeBase(Array<Own<PromiseNode>> promises,
                           ExceptionOrValue* resultParts, size_t partSize);
  ~ArrayJoinPromiseNodeBase() noexcept(false);

  void onReady(Event* event) noexcept override final;
  void get(ExceptionOrValue& output) noexcept override final;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override final;

protected:
  virtual void getNoError(ExceptionOrValue& output) noexcept = 0;


private:
  uint countLeft;
  OnReadyEvent onReadyEvent;

  class Branch final: public Event {
  public:
    Branch(ArrayJoinPromiseNodeBase& joinNode, Own<PromiseNode> dependency,
           ExceptionOrValue& output);
    ~Branch() noexcept(false);

    Maybe<Own<Event>> fire() override;
    void traceEvent(TraceBuilder& builder) override;

    Maybe<Exception> getPart();


  private:
    ArrayJoinPromiseNodeBase& joinNode;
    Own<PromiseNode> dependency;
    ExceptionOrValue& output;

    friend class ArrayJoinPromiseNodeBase;
  };

  Array<Branch> branches;
};

template <typename T>
class ArrayJoinPromiseNode final: public ArrayJoinPromiseNodeBase {
public:
  ArrayJoinPromiseNode(Array<Own<PromiseNode>> promises,
                       Array<ExceptionOr<T>> resultParts)
      : ArrayJoinPromiseNodeBase(kj::mv(promises), resultParts.begin(), sizeof(ExceptionOr<T>)),
        resultParts(kj::mv(resultParts)) {}

protected:
  void getNoError(ExceptionOrValue& output) noexcept override {
    auto builder = heapArrayBuilder<T>(resultParts.size());
    for (auto& part: resultParts) {
      if (__builtin_expect(part.value != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h", 852, "part.value != nullptr", "\"Bug in KJ promise framework:  Promise result had neither value no exception.\"", "Bug in KJ promise framework:  Promise result had neither value no exception.")
                                                                                                ;
      builder.add(kj::mv(*_::readMaybe(part.value)));
    }
    output.as<Array<T>>() = builder.finish();
  }

private:
  Array<ExceptionOr<T>> resultParts;
};

template <>
class ArrayJoinPromiseNode<void> final: public ArrayJoinPromiseNodeBase {
public:
  ArrayJoinPromiseNode(Array<Own<PromiseNode>> promises,
                       Array<ExceptionOr<_::Void>> resultParts);
  ~ArrayJoinPromiseNode();

protected:
  void getNoError(ExceptionOrValue& output) noexcept override;

private:
  Array<ExceptionOr<_::Void>> resultParts;
};



class EagerPromiseNodeBase: public PromiseNode, protected Event {



public:
  EagerPromiseNodeBase(Own<PromiseNode>&& dependency, ExceptionOrValue& resultRef);

  void onReady(Event* event) noexcept override;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;

private:
  Own<PromiseNode> dependency;
  OnReadyEvent onReadyEvent;

  ExceptionOrValue& resultRef;

  Maybe<Own<Event>> fire() override;
  void traceEvent(TraceBuilder& builder) override;
};

template <typename T>
class EagerPromiseNode final: public EagerPromiseNodeBase {
public:
  EagerPromiseNode(Own<PromiseNode>&& dependency)
      : EagerPromiseNodeBase(kj::mv(dependency), result) {}

  void get(ExceptionOrValue& output) noexcept override {
    output.as<T>() = kj::mv(result);
  }

private:
  ExceptionOr<T> result;
};

template <typename T>
Own<PromiseNode> spark(Own<PromiseNode>&& node) {


  return heap<EagerPromiseNode<T>>(kj::mv(node));
}



class AdapterPromiseNodeBase: public PromiseNode {
public:
  void onReady(Event* event) noexcept override;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;

protected:
  inline void setReady() {
    onReadyEvent.arm();
  }

private:
  OnReadyEvent onReadyEvent;
};

template <typename T, typename Adapter>
class AdapterPromiseNode final: public AdapterPromiseNodeBase,
                                private PromiseFulfiller<UnfixVoid<T>> {


public:
  template <typename... Params>
  AdapterPromiseNode(Params&&... params)
      : adapter(static_cast<PromiseFulfiller<UnfixVoid<T>>&>(*this), kj::fwd<Params>(params)...) {}

  void get(ExceptionOrValue& output) noexcept override {
    if (__builtin_expect(!isWaiting(), true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h", 947, "!isWaiting()", "");
    output.as<T>() = kj::mv(result);
  }

private:
  ExceptionOr<T> result;
  bool waiting = true;
  Adapter adapter;

  void fulfill(T&& value) override {
    if (waiting) {
      waiting = false;
      result = ExceptionOr<T>(kj::mv(value));
      setReady();
    }
  }

  void reject(Exception&& exception) override {
    if (waiting) {
      waiting = false;
      result = ExceptionOr<T>(false, kj::mv(exception));
      setReady();
    }
  }

  bool isWaiting() override {
    return waiting;
  }
};



class FiberBase: public PromiseNode, private Event {


public:
  explicit FiberBase(size_t stackSize, _::ExceptionOrValue& result);
  explicit FiberBase(const FiberPool& pool, _::ExceptionOrValue& result);
  ~FiberBase() noexcept(false);

  void start() { armDepthFirst(); }


  class WaitDoneEvent;

  void onReady(_::Event* event) noexcept override;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;

protected:
  bool isFinished() { return state == FINISHED; }
  void destroy();

private:
  enum { WAITING, RUNNING, CANCELED, FINISHED } state;

  _::PromiseNode* currentInner = nullptr;
  OnReadyEvent onReadyEvent;
  Own<FiberStack> stack;
  _::ExceptionOrValue& result;

  void run();
  virtual void runImpl(WaitScope& waitScope) = 0;

  Maybe<Own<Event>> fire() override;
  void traceEvent(TraceBuilder& builder) override;


  friend class FiberStack;
  friend void _::waitImpl(Own<_::PromiseNode>&& node, _::ExceptionOrValue& result,
                          WaitScope& waitScope);
  friend bool _::pollImpl(_::PromiseNode& node, WaitScope& waitScope);
};

template <typename Func>
class Fiber final: public FiberBase {
public:
  explicit Fiber(size_t stackSize, Func&& func): FiberBase(stackSize, result), func(kj::fwd<Func>(func)) {}
  explicit Fiber(const FiberPool& pool, Func&& func): FiberBase(pool, result), func(kj::fwd<Func>(func)) {}
  ~Fiber() noexcept(false) { destroy(); }

  typedef FixVoid<decltype(kj::instance<Func&>()(kj::instance<WaitScope&>()))> ResultType;

  void get(ExceptionOrValue& output) noexcept override {
    if (__builtin_expect(isFinished(), true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h", 1030, "isFinished()", "");
    output.as<ResultType>() = kj::mv(result);
  }

private:
  Func func;
  ExceptionOr<ResultType> result;

  void runImpl(WaitScope& waitScope) override {
    result.template as<ResultType>() =
        MaybeVoidCaller<WaitScope&, ResultType>::apply(func, waitScope);
  }
};

}



template <typename T>
Promise<T>::Promise(_::FixVoid<T> value)
    : PromiseBase(heap<_::ImmediatePromiseNode<_::FixVoid<T>>>(kj::mv(value))) {}

template <typename T>
Promise<T>::Promise(kj::Exception&& exception)
    : PromiseBase(heap<_::ImmediateBrokenPromiseNode>(kj::mv(exception))) {}

template <typename T>
template <typename Func, typename ErrorFunc>
PromiseForResult<Func, T> Promise<T>::then(Func&& func, ErrorFunc&& errorHandler) {
  typedef _::FixVoid<_::ReturnType<Func, T>> ResultT;

  void* continuationTracePtr = _::GetFunctorStartAddress<_::FixVoid<T>&&>::apply(func);
  Own<_::PromiseNode> intermediate =
      heap<_::TransformPromiseNode<ResultT, _::FixVoid<T>, Func, ErrorFunc>>(
          kj::mv(node), kj::fwd<Func>(func), kj::fwd<ErrorFunc>(errorHandler),
          continuationTracePtr);
  auto result = _::PromiseNode::to<_::ChainPromises<_::ReturnType<Func, T>>>(
      _::maybeChain(kj::mv(intermediate), implicitCast<ResultT*>(nullptr)));
  return _::maybeReduce(kj::mv(result), false);
}

namespace _ {

template <typename T>
struct IdentityFunc {
  inline T operator()(T&& value) const {
    return kj::mv(value);
  }
};
template <typename T>
struct IdentityFunc<Promise<T>> {
  inline Promise<T> operator()(T&& value) const {
    return kj::mv(value);
  }
};
template <>
struct IdentityFunc<void> {
  inline void operator()() const {}
};
template <>
struct IdentityFunc<Promise<void>> {
  Promise<void> operator()() const;



};

}

template <typename T>
template <typename ErrorFunc>
Promise<T> Promise<T>::catch_(ErrorFunc&& errorHandler) {




  typedef _::IdentityFunc<decltype(errorHandler(instance<Exception&&>()))> Func;
  typedef _::FixVoid<_::ReturnType<Func, T>> ResultT;



  void* continuationTracePtr = _::GetFunctorStartAddress<kj::Exception&&>::apply(errorHandler);
  Own<_::PromiseNode> intermediate =
      heap<_::TransformPromiseNode<ResultT, _::FixVoid<T>, Func, ErrorFunc>>(
          kj::mv(node), Func(), kj::fwd<ErrorFunc>(errorHandler), continuationTracePtr);
  auto result = _::PromiseNode::to<_::ChainPromises<_::ReturnType<Func, T>>>(
      _::maybeChain(kj::mv(intermediate), implicitCast<ResultT*>(nullptr)));
  return _::maybeReduce(kj::mv(result), false);
}

template <typename T>
T Promise<T>::wait(WaitScope& waitScope) {
  _::ExceptionOr<_::FixVoid<T>> result;
  _::waitImpl(kj::mv(node), result, waitScope);
  return convertToReturn(kj::mv(result));
}

template <typename T>
bool Promise<T>::poll(WaitScope& waitScope) {
  return _::pollImpl(*node, waitScope);
}

template <typename T>
ForkedPromise<T> Promise<T>::fork() {
  return ForkedPromise<T>(false, refcounted<_::ForkHub<_::FixVoid<T>>>(kj::mv(node)));
}

template <typename T>
Promise<T> ForkedPromise<T>::addBranch() {
  return hub->addBranch();
}

template <typename T>
bool ForkedPromise<T>::hasBranches() {
  return hub->isShared();
}

template <typename T>
_::SplitTuplePromise<T> Promise<T>::split() {
  return refcounted<_::ForkHub<_::FixVoid<T>>>(kj::mv(node))->split();
}

template <typename T>
Promise<T> Promise<T>::exclusiveJoin(Promise<T>&& other) {
  return Promise(false, heap<_::ExclusiveJoinPromiseNode>(kj::mv(node), kj::mv(other.node)));
}

template <typename T>
template <typename... Attachments>
Promise<T> Promise<T>::attach(Attachments&&... attachments) {
  return Promise(false, kj::heap<_::AttachmentPromiseNode<Tuple<Attachments...>>>(
      kj::mv(node), kj::tuple(kj::fwd<Attachments>(attachments)...)));
}

template <typename T>
template <typename ErrorFunc>
Promise<T> Promise<T>::eagerlyEvaluate(ErrorFunc&& errorHandler) {

  return Promise(false, _::spark<_::FixVoid<T>>(then(
      _::IdentityFunc<decltype(errorHandler(instance<Exception&&>()))>(),
      kj::fwd<ErrorFunc>(errorHandler)).node));
}

template <typename T>
Promise<T> Promise<T>::eagerlyEvaluate(decltype(nullptr)) {
  return Promise(false, _::spark<_::FixVoid<T>>(kj::mv(node)));
}

template <typename T>
kj::String Promise<T>::trace() {
  return PromiseBase::trace();
}

template <typename Func>
inline PromiseForResult<Func, void> evalLater(Func&& func) {
  return _::yield().then(kj::fwd<Func>(func), _::PropagateException());
}

template <typename Func>
inline PromiseForResult<Func, void> evalLast(Func&& func) {
  return _::yieldHarder().then(kj::fwd<Func>(func), _::PropagateException());
}

template <typename Func>
inline PromiseForResult<Func, void> evalNow(Func&& func) {
  PromiseForResult<Func, void> result = nullptr;
  if (auto e = ::kj::_::readMaybe(kj::runCatchingExceptions([&]() { result = func(); })))

      {
    result = kj::mv(*e);
  }
  return result;
}

template <typename Func>
struct RetryOnDisconnect_ {
  static inline PromiseForResult<Func, void> apply(Func&& func) {
    return evalLater([func = kj::mv(func)]() mutable -> PromiseForResult<Func, void> {
      auto promise = evalNow(func);
      return promise.catch_([func = kj::mv(func)](kj::Exception&& e) mutable -> PromiseForResult<Func, void> {
        if (e.getType() == kj::Exception::Type::DISCONNECTED) {
          return func();
        } else {
          return kj::mv(e);
        }
      });
    });
  }
};
template <typename Func>
struct RetryOnDisconnect_<Func&> {


  static inline PromiseForResult<Func, void> apply(Func& func) {
    auto promise = evalLater(func);
    return promise.catch_([&func](kj::Exception&& e) -> PromiseForResult<Func, void> {
      if (e.getType() == kj::Exception::Type::DISCONNECTED) {
        return func();
      } else {
        return kj::mv(e);
      }
    });
  }
};

template <typename Func>
inline PromiseForResult<Func, void> retryOnDisconnect(Func&& func) {
  return RetryOnDisconnect_<Func>::apply(kj::fwd<Func>(func));
}

template <typename Func>
inline PromiseForResult<Func, WaitScope&> startFiber(size_t stackSize, Func&& func) {
  typedef _::FixVoid<_::ReturnType<Func, WaitScope&>> ResultT;

  Own<_::FiberBase> intermediate = kj::heap<_::Fiber<Func>>(stackSize, kj::fwd<Func>(func));
  intermediate->start();
  auto result = _::PromiseNode::to<_::ChainPromises<_::ReturnType<Func, WaitScope&>>>(
      _::maybeChain(kj::mv(intermediate), implicitCast<ResultT*>(nullptr)));
  return _::maybeReduce(kj::mv(result), false);
}

template <typename Func>
inline PromiseForResult<Func, WaitScope&> FiberPool::startFiber(Func&& func) const {
  typedef _::FixVoid<_::ReturnType<Func, WaitScope&>> ResultT;

  Own<_::FiberBase> intermediate = kj::heap<_::Fiber<Func>>(*this, kj::fwd<Func>(func));
  intermediate->start();
  auto result = _::PromiseNode::to<_::ChainPromises<_::ReturnType<Func, WaitScope&>>>(
      _::maybeChain(kj::mv(intermediate), implicitCast<ResultT*>(nullptr)));
  return _::maybeReduce(kj::mv(result), false);
}

template <typename T>
template <typename ErrorFunc>
void Promise<T>::detach(ErrorFunc&& errorHandler) {
  return _::detach(then([](T&&) {}, kj::fwd<ErrorFunc>(errorHandler)));
}

template <>
template <typename ErrorFunc>
void Promise<void>::detach(ErrorFunc&& errorHandler) {
  return _::detach(then([]() {}, kj::fwd<ErrorFunc>(errorHandler)));
}

template <typename T>
Promise<Array<T>> joinPromises(Array<Promise<T>>&& promises) {
  return _::PromiseNode::to<Promise<Array<T>>>(kj::heap<_::ArrayJoinPromiseNode<T>>(
      ::kj::_::Mapper<decltype(::kj::_::refIfLvalue(promises))>(promises) * [&](typename ::kj::_::Mapper<decltype(::kj::_::refIfLvalue(promises))>::Element p) { return _::PromiseNode::from(kj::mv(p)); },
      heapArray<_::ExceptionOr<T>>(promises.size())));
}



namespace _ {

class WeakFulfillerBase: protected kj::Disposer {
protected:
  WeakFulfillerBase(): inner(nullptr) {}
  virtual ~WeakFulfillerBase() noexcept(false) {}

  template <typename T>
  inline PromiseFulfiller<T>* getInner() {
    return static_cast<PromiseFulfiller<T>*>(inner);
  };
  template <typename T>
  inline void setInner(PromiseFulfiller<T>* ptr) {
    inner = ptr;
  };

private:
  mutable PromiseRejector* inner;

  void disposeImpl(void* pointer) const override;
};

template <typename T>
class WeakFulfiller final: public PromiseFulfiller<T>, public WeakFulfillerBase {
# 1320 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3
public:
  WeakFulfiller(const WeakFulfiller&) = delete; WeakFulfiller& operator=(const WeakFulfiller&) = delete;

  static kj::Own<WeakFulfiller> make() {
    WeakFulfiller* ptr = new WeakFulfiller;
    return Own<WeakFulfiller>(ptr, *ptr);
  }

  void fulfill(FixVoid<T>&& value) override {
    if (getInner<T>() != nullptr) {
      getInner<T>()->fulfill(kj::mv(value));
    }
  }

  void reject(Exception&& exception) override {
    if (getInner<T>() != nullptr) {
      getInner<T>()->reject(kj::mv(exception));
    }
  }

  bool isWaiting() override {
    return getInner<T>() != nullptr && getInner<T>()->isWaiting();
  }

  void attach(PromiseFulfiller<T>& newInner) {
    setInner<T>(&newInner);
  }

  void detach(PromiseFulfiller<T>& from) {
    if (getInner<T>() == nullptr) {

      delete this;
    } else {
      if (__builtin_expect(getInner<T>() == &from, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h", 1353, "getInner<T>() == &from", "");
      setInner<T>(nullptr);
    }
  }

private:
  WeakFulfiller() {}
};

template <typename T>
class PromiseAndFulfillerAdapter {
public:
  PromiseAndFulfillerAdapter(PromiseFulfiller<T>& fulfiller,
                             WeakFulfiller<T>& wrapper)
      : fulfiller(fulfiller), wrapper(wrapper) {
    wrapper.attach(fulfiller);
  }

  ~PromiseAndFulfillerAdapter() noexcept(false) {
    wrapper.detach(fulfiller);
  }

private:
  PromiseFulfiller<T>& fulfiller;
  WeakFulfiller<T>& wrapper;
};

}

template <typename T>
template <typename Func>
bool PromiseFulfiller<T>::rejectIfThrows(Func&& func) {
  if (auto exception = ::kj::_::readMaybe(kj::runCatchingExceptions(kj::mv(func)))) {
    reject(kj::mv(*exception));
    return false;
  } else {
    return true;
  }
}

template <typename Func>
bool PromiseFulfiller<void>::rejectIfThrows(Func&& func) {
  if (auto exception = ::kj::_::readMaybe(kj::runCatchingExceptions(kj::mv(func)))) {
    reject(kj::mv(*exception));
    return false;
  } else {
    return true;
  }
}

template <typename T, typename Adapter, typename... Params>
_::ReducePromises<T> newAdaptedPromise(Params&&... adapterConstructorParams) {
  Own<_::PromiseNode> intermediate(
      heap<_::AdapterPromiseNode<_::FixVoid<T>, Adapter>>(
          kj::fwd<Params>(adapterConstructorParams)...));
  return _::PromiseNode::to<_::ReducePromises<T>>(
      _::maybeChain(kj::mv(intermediate), implicitCast<T*>(nullptr)));
}

template <typename T>
PromiseFulfillerPair<T> newPromiseAndFulfiller() {
  auto wrapper = _::WeakFulfiller<T>::make();

  Own<_::PromiseNode> intermediate(
      heap<_::AdapterPromiseNode<_::FixVoid<T>, _::PromiseAndFulfillerAdapter<T>>>(*wrapper));
  auto promise = _::PromiseNode::to<_::ReducePromises<T>>(
      _::maybeChain(kj::mv(intermediate), implicitCast<T*>(nullptr)));

  return PromiseFulfillerPair<T> { kj::mv(promise), kj::mv(wrapper) };
}




namespace _ {

class XThreadEvent: private Event,
                    public PromiseNode {
public:
  XThreadEvent(ExceptionOrValue& result, const Executor& targetExecutor, void* funcTracePtr);

  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;

protected:
  void ensureDoneOrCanceled();




  virtual kj::Maybe<Own<PromiseNode>> execute() = 0;




  void onReady(Event* event) noexcept override;

private:
  ExceptionOrValue& result;
  void* funcTracePtr;

  kj::Own<const Executor> targetExecutor;
  Maybe<const Executor&> replyExecutor;

  kj::Maybe<Own<PromiseNode>> promiseNode;


  ListLink<XThreadEvent> targetLink;



  enum {
    UNUSED,


    QUEUED,



    EXECUTING,




    CANCELING,



    DONE




  } state = UNUSED;


  ListLink<XThreadEvent> replyLink;






  OnReadyEvent onReadyEvent;


  friend class kj::Executor;

  void done();



  void sendReply();



  void setDoneState();







  void setDisconnected();


  class DelayedDoneHack;


  Maybe<Own<Event>> fire() override;



  void traceEvent(TraceBuilder& builder) override;
};

template <typename Func, typename = _::FixVoid<_::ReturnType<Func, void>>>
class XThreadEventImpl final: public XThreadEvent {

public:
  XThreadEventImpl(Func&& func, const Executor& target)
      : XThreadEvent(result, target, GetFunctorStartAddress<>::apply(func)),
        func(kj::fwd<Func>(func)) {}
  ~XThreadEventImpl() noexcept(false) { ensureDoneOrCanceled(); }

  typedef _::FixVoid<_::ReturnType<Func, void>> ResultT;

  kj::Maybe<Own<_::PromiseNode>> execute() override {
    result.value = MaybeVoidCaller<Void, FixVoid<decltype(func())>>::apply(func, Void());
    return nullptr;
  }


  void get(ExceptionOrValue& output) noexcept override {
    output.as<ResultT>() = kj::mv(result);
  }

private:
  Func func;
  ExceptionOr<ResultT> result;
  friend Executor;
};

template <typename Func, typename T>
class XThreadEventImpl<Func, Promise<T>> final: public XThreadEvent {

public:
  XThreadEventImpl(Func&& func, const Executor& target)
      : XThreadEvent(result, target, GetFunctorStartAddress<>::apply(func)),
        func(kj::fwd<Func>(func)) {}
  ~XThreadEventImpl() noexcept(false) { ensureDoneOrCanceled(); }

  typedef _::FixVoid<_::UnwrapPromise<PromiseForResult<Func, void>>> ResultT;

  kj::Maybe<Own<_::PromiseNode>> execute() override {
    auto result = _::PromiseNode::from(func());
    if (__builtin_expect(result.get() != nullptr, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h", 1569, "result.get() != nullptr", "");
    return kj::mv(result);
  }


  void get(ExceptionOrValue& output) noexcept override {
    output.as<ResultT>() = kj::mv(result);
  }

private:
  Func func;
  ExceptionOr<ResultT> result;
  friend Executor;
};

}

template <typename Func>
_::UnwrapPromise<PromiseForResult<Func, void>> Executor::executeSync(Func&& func) const {
  _::XThreadEventImpl<Func> event(kj::fwd<Func>(func), *this);
  send(event, true);
  return convertToReturn(kj::mv(event.result));
}

template <typename Func>
PromiseForResult<Func, void> Executor::executeAsync(Func&& func) const {
  auto event = kj::heap<_::XThreadEventImpl<Func>>(kj::fwd<Func>(func), *this);
  send(*event, false);
  return _::PromiseNode::to<PromiseForResult<Func, void>>(kj::mv(event));
}



namespace _ {

template <typename T>
class XThreadFulfiller;

class XThreadPaf: public PromiseNode {
public:
  XThreadPaf();
  virtual ~XThreadPaf() noexcept(false);

  class Disposer: public kj::Disposer {
  public:
    void disposeImpl(void* pointer) const override;
  };
  static const Disposer DISPOSER;


  void onReady(Event* event) noexcept override;
  void tracePromise(TraceBuilder& builder, bool stopAtNextEvent) override;

private:
  enum {
    WAITING,





    FULFILLING,
# 1641 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3
    FULFILLED,




    DISPATCHED,




    CANCELED



  } state;

  const Executor& executor;



  ListLink<XThreadPaf> link;



  OnReadyEvent onReadyEvent;

  class FulfillScope;

  static kj::Exception unfulfilledException();


  template <typename T>
  friend class XThreadFulfiller;
  friend Executor;
};

template <typename T>
class XThreadPafImpl final: public XThreadPaf {
public:

  void get(ExceptionOrValue& output) noexcept override {
    output.as<FixVoid<T>>() = kj::mv(result);
  }

private:
  ExceptionOr<FixVoid<T>> result;

  friend class XThreadFulfiller<T>;
};

class XThreadPaf::FulfillScope {





public:
  FulfillScope(XThreadPaf** pointer);


  ~FulfillScope() noexcept(false);

  FulfillScope(const FulfillScope&) = delete; FulfillScope& operator=(const FulfillScope&) = delete;

  bool shouldFulfill() { return obj != nullptr; }

  template <typename T>
  XThreadPafImpl<T>* getTarget() { return static_cast<XThreadPafImpl<T>*>(obj); }

private:
  XThreadPaf* obj;
};

template <typename T>
class XThreadFulfiller final: public CrossThreadPromiseFulfiller<T> {
public:
  XThreadFulfiller(XThreadPafImpl<T>* target): target(target) {}

  ~XThreadFulfiller() noexcept(false) {
    if (target != nullptr) {
      reject(XThreadPaf::unfulfilledException());
    }
  }
  void fulfill(FixVoid<T>&& value) const override {
    XThreadPaf::FulfillScope scope(&target);
    if (scope.shouldFulfill()) {
      scope.getTarget<T>()->result = kj::mv(value);
    }
  }
  void reject(Exception&& exception) const override {
    XThreadPaf::FulfillScope scope(&target);
    if (scope.shouldFulfill()) {
      scope.getTarget<T>()->result.addException(kj::mv(exception));
    }
  }
  bool isWaiting() const override {
    if (auto t = ::kj::_::readMaybe(target)) {




      return __atomic_load_n(&t->state, 0) == XThreadPaf::WAITING;

    } else {
      return false;
    }
  }

private:
  mutable XThreadPaf* target;
};

template <typename T>
class XThreadFulfiller<kj::Promise<T>> {
public:
  static_assert(sizeof(T) < 0,
      "newCrosssThreadPromiseAndFulfiller<Promise<T>>() is not currently supported");



};

}

template <typename T>
PromiseCrossThreadFulfillerPair<T> newPromiseAndCrossThreadFulfiller() {
  kj::Own<_::XThreadPafImpl<T>> node(new _::XThreadPafImpl<T>, _::XThreadPaf::DISPOSER);
  auto fulfiller = kj::heap<_::XThreadFulfiller<T>>(node);
  return { _::PromiseNode::to<_::ReducePromises<T>>(kj::mv(node)), kj::mv(fulfiller) };
}

}
# 2061 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-inl.h" 3

# 1171 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async.h" 2 3


# 23 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/async-coroutine-test.c++" 2

# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
       
# 38 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/vector.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/vector.h"
       




# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/vector.h"

# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/vector.h" 3


namespace kj {

template <typename T>
class Vector {
# 40 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/vector.h" 3
public:
  inline Vector() = default;
  inline explicit Vector(size_t capacity): builder(heapArrayBuilder<T>(capacity)) {}
  inline Vector(Array<T>&& array): builder(kj::mv(array)) {}

  inline operator ArrayPtr<T>() { return builder; }
  inline operator ArrayPtr<const T>() const { return builder; }
  inline ArrayPtr<T> asPtr() { return builder.asPtr(); }
  inline ArrayPtr<const T> asPtr() const { return builder.asPtr(); }

  inline size_t size() const { return builder.size(); }
  inline bool empty() const { return size() == 0; }
  inline size_t capacity() const { return builder.capacity(); }
  inline T& operator[](size_t index) { return builder[index]; }
  inline const T& operator[](size_t index) const { return builder[index]; }

  inline const T* begin() const { return builder.begin(); }
  inline const T* end() const { return builder.end(); }
  inline const T& front() const { return builder.front(); }
  inline const T& back() const { return builder.back(); }
  inline T* begin() { return builder.begin(); }
  inline T* end() { return builder.end(); }
  inline T& front() { return builder.front(); }
  inline T& back() { return builder.back(); }

  inline Array<T> releaseAsArray() {

    if (!builder.isFull()) {
      setCapacity(size());
    }
    return builder.finish();
  }

  template <typename U>
  inline bool operator==(const U& other) const { return asPtr() == other; }
  template <typename U>
  inline bool operator!=(const U& other) const { return asPtr() != other; }

  inline ArrayPtr<T> slice(size_t start, size_t end) {
    return asPtr().slice(start, end);
  }
  inline ArrayPtr<const T> slice(size_t start, size_t end) const {
    return asPtr().slice(start, end);
  }

  template <typename... Params>
  inline T& add(Params&&... params) {
    if (builder.isFull()) grow();
    return builder.add(kj::fwd<Params>(params)...);
  }

  template <typename Iterator>
  inline void addAll(Iterator begin, Iterator end) {
    size_t needed = builder.size() + (end - begin);
    if (needed > builder.capacity()) grow(needed);
    builder.addAll(begin, end);
  }

  template <typename Container>
  inline void addAll(Container&& container) {
    addAll(container.begin(), container.end());
  }

  inline void removeLast() {
    builder.removeLast();
  }

  inline void resize(size_t size) {
    if (size > builder.capacity()) grow(size);
    builder.resize(size);
  }

  inline void operator=(decltype(nullptr)) {
    builder = nullptr;
  }

  inline void clear() {
    builder.clear();
  }

  inline void truncate(size_t size) {
    builder.truncate(size);
  }

  inline void reserve(size_t size) {
    if (size > builder.capacity()) {
      setCapacity(size);
    }
  }

private:
  ArrayBuilder<T> builder;

  void grow(size_t minCapacity = 0) {
    setCapacity(kj::max(minCapacity, capacity() == 0 ? 4 : capacity() * 2));
  }
  void setCapacity(size_t newSize) {
    if (builder.size() > newSize) {
      builder.truncate(newSize);
    }
    ArrayBuilder<T> newBuilder = heapArrayBuilder<T>(newSize);
    newBuilder.addAll(kj::mv(builder));
    builder = kj::mv(newBuilder);
  }
};

template <typename T>
inline auto operator*(::kj::_::Stringifier, const Vector<T>& v) -> decltype(toCharSequence(v.asPtr())) {
  return toCharSequence(v.asPtr());
}

}


# 39 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 2

# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/one-of.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/one-of.h"
       




# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/one-of.h"

# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/one-of.h" 3


namespace kj {

namespace _ {

template <uint i, template<uint> class Fail, typename Key, typename... Variants>
struct TypeIndex_;
template <uint i, template<uint> class Fail, typename Key, typename First, typename... Rest>
struct TypeIndex_<i, Fail, Key, First, Rest...> {
  static constexpr uint value = TypeIndex_<i + 1, Fail, Key, Rest...>::value;
};
template <uint i, template<uint> class Fail, typename Key, typename... Rest>
struct TypeIndex_<i, Fail, Key, Key, Rest...> { static constexpr uint value = i; };
template <uint i, template<uint> class Fail, typename Key>
struct TypeIndex_<i, Fail, Key>: public Fail<i> {};

template <uint i>
struct OneOfFailError_ {
  static_assert(i == -1, "type does not match any in OneOf");
};
template <uint i>
struct OneOfFailZero_ {
  static constexpr int value = 0;
};

template <uint i>
struct SuccessIfNotZero {
  typedef int Success;
};
template <>
struct SuccessIfNotZero<0> {};

enum class Variants0 {};
enum class Variants1 { _variant0 };
enum class Variants2 { _variant0, _variant1 };
enum class Variants3 { _variant0, _variant1, _variant2 };
enum class Variants4 { _variant0, _variant1, _variant2, _variant3 };
enum class Variants5 { _variant0, _variant1, _variant2, _variant3, _variant4 };
enum class Variants6 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5 };
enum class Variants7 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6 };
enum class Variants8 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                       _variant7 };
enum class Variants9 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                       _variant7, _variant8 };
enum class Variants10 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9 };
enum class Variants11 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9, _variant10 };
enum class Variants12 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9, _variant10, _variant11 };
enum class Variants13 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9, _variant10, _variant11, _variant12 };
enum class Variants14 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9, _variant10, _variant11, _variant12,
                        _variant13 };
enum class Variants15 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9, _variant10, _variant11, _variant12,
                        _variant13, _variant14 };
enum class Variants16 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9, _variant10, _variant11, _variant12,
                        _variant13, _variant14, _variant15 };
enum class Variants17 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9, _variant10, _variant11, _variant12,
                        _variant13, _variant14, _variant15, _variant16 };
enum class Variants18 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9, _variant10, _variant11, _variant12,
                        _variant13, _variant14, _variant15, _variant16, _variant17 };
enum class Variants19 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9, _variant10, _variant11, _variant12,
                        _variant13, _variant14, _variant15, _variant16, _variant17, _variant18 };
enum class Variants20 { _variant0, _variant1, _variant2, _variant3, _variant4, _variant5, _variant6,
                        _variant7, _variant8, _variant9, _variant10, _variant11, _variant12,
                        _variant13, _variant14, _variant15, _variant16, _variant17, _variant18,
                        _variant19 };

template <uint i> struct Variants_;
template <> struct Variants_<0> { typedef Variants0 Type; };
template <> struct Variants_<1> { typedef Variants1 Type; };
template <> struct Variants_<2> { typedef Variants2 Type; };
template <> struct Variants_<3> { typedef Variants3 Type; };
template <> struct Variants_<4> { typedef Variants4 Type; };
template <> struct Variants_<5> { typedef Variants5 Type; };
template <> struct Variants_<6> { typedef Variants6 Type; };
template <> struct Variants_<7> { typedef Variants7 Type; };
template <> struct Variants_<8> { typedef Variants8 Type; };
template <> struct Variants_<9> { typedef Variants9 Type; };
template <> struct Variants_<10> { typedef Variants10 Type; };
template <> struct Variants_<11> { typedef Variants11 Type; };
template <> struct Variants_<12> { typedef Variants12 Type; };
template <> struct Variants_<13> { typedef Variants13 Type; };
template <> struct Variants_<14> { typedef Variants14 Type; };
template <> struct Variants_<15> { typedef Variants15 Type; };
template <> struct Variants_<16> { typedef Variants16 Type; };
template <> struct Variants_<17> { typedef Variants17 Type; };
template <> struct Variants_<18> { typedef Variants18 Type; };
template <> struct Variants_<19> { typedef Variants19 Type; };
template <> struct Variants_<20> { typedef Variants20 Type; };

template <uint i>
using Variants = typename Variants_<i>::Type;

}

template <typename... Variants>
class OneOf {
  template <typename Key>
  static inline constexpr uint typeIndex() {
    return _::TypeIndex_<1, _::OneOfFailError_, Key, Variants...>::value;
  }


  template <typename Key>
  static inline constexpr uint typeIndexOrZero() {
    return _::TypeIndex_<1, _::OneOfFailZero_, Key, Variants...>::value;
  }

  template <uint i, typename... OtherVariants>
  struct HasAll;



public:
  inline OneOf(): tag(0) {}

  OneOf(const OneOf& other) { copyFrom(other); }
  OneOf(OneOf& other) { copyFrom(other); }
  OneOf(OneOf&& other) { moveFrom(other); }


  template <typename... OtherVariants, typename = typename HasAll<1, OtherVariants...>::Success>
  OneOf(const OneOf<OtherVariants...>& other) { copyFromSubset(other); }
  template <typename... OtherVariants, typename = typename HasAll<1, OtherVariants...>::Success>
  OneOf(OneOf<OtherVariants...>& other) { copyFromSubset(other); }
  template <typename... OtherVariants, typename = typename HasAll<1, OtherVariants...>::Success>
  OneOf(OneOf<OtherVariants...>&& other) { moveFromSubset(other); }


  template <typename T, typename = typename HasAll<0, Decay<T>>::Success>
  OneOf(T&& other): tag(typeIndex<Decay<T>>()) {
    ctor(*reinterpret_cast<Decay<T>*>(space), kj::fwd<T>(other));
  }


  ~OneOf() { destroy(); }

  OneOf& operator=(const OneOf& other) { if (tag != 0) destroy(); copyFrom(other); return *this; }
  OneOf& operator=(OneOf&& other) { if (tag != 0) destroy(); moveFrom(other); return *this; }

  inline bool operator==(decltype(nullptr)) const { return tag == 0; }
  inline bool operator!=(decltype(nullptr)) const { return tag != 0; }

  template <typename T>
  bool is() const {
    return tag == typeIndex<T>();
  }

  template <typename T>
  T& get() {
    if (__builtin_expect(is<T>(), true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/one-of.h", 185, "is<T>()", "\"Must check OneOf::is<T>() before calling get<T>().\"", "Must check OneOf::is<T>() before calling get<T>().");
    return *reinterpret_cast<T*>(space);
  }
  template <typename T>
  const T& get() const {
    if (__builtin_expect(is<T>(), true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/one-of.h", 190, "is<T>()", "\"Must check OneOf::is<T>() before calling get<T>().\"", "Must check OneOf::is<T>() before calling get<T>().");
    return *reinterpret_cast<const T*>(space);
  }

  template <typename T, typename... Params>
  T& init(Params&&... params) {
    if (tag != 0) destroy();
    ctor(*reinterpret_cast<T*>(space), kj::fwd<Params>(params)...);
    tag = typeIndex<T>();
    return *reinterpret_cast<T*>(space);
  }

  template <typename T>
  Maybe<T&> tryGet() {
    if (is<T>()) {
      return *reinterpret_cast<T*>(space);
    } else {
      return nullptr;
    }
  }
  template <typename T>
  Maybe<const T&> tryGet() const {
    if (is<T>()) {
      return *reinterpret_cast<const T*>(space);
    } else {
      return nullptr;
    }
  }

  template <uint i>
  void allHandled() __attribute__((noreturn));




  typedef _::Variants<sizeof...(Variants)> Tag;

  Tag which() const {
    if (__builtin_expect(tag != 0, true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/one-of.h", 228, "tag != 0", "\"Can't KJ_SWITCH_ONEOF() on uninitialized value.\"", "Can't KJ_SWITCH_ONEOF() on uninitialized value.");
    return static_cast<Tag>(tag - 1);
  }

  template <typename T>
  static constexpr Tag tagFor() {
    return static_cast<Tag>(typeIndex<T>() - 1);
  }

  OneOf* _switchSubject() & { return this; }
  const OneOf* _switchSubject() const& { return this; }
  _::NullableValue<OneOf> _switchSubject() && { return kj::mv(*this); }

private:
  uint tag;

  static inline constexpr size_t maxSize(size_t a) {
    return a;
  }
  template <typename... Rest>
  static inline constexpr size_t maxSize(size_t a, size_t b, Rest... rest) {
    return maxSize(kj::max(a, b), rest...);
  }




  static constexpr auto spaceSize = maxSize(sizeof(Variants)...);



  union {
    byte space[spaceSize];

    void* forceAligned;

  };

  template <typename... T>
  inline void doAll(T... t) {}

  template <typename T>
  inline bool destroyVariant() {
    if (tag == typeIndex<T>()) {
      tag = 0;
      dtor(*reinterpret_cast<T*>(space));
    }
    return false;
  }
  void destroy() {
    doAll(destroyVariant<Variants>()...);
  }

  template <typename T>
  inline bool copyVariantFrom(const OneOf& other) {
    if (other.is<T>()) {
      ctor(*reinterpret_cast<T*>(space), other.get<T>());
    }
    return false;
  }
  void copyFrom(const OneOf& other) {


    tag = other.tag;
    doAll(copyVariantFrom<Variants>(other)...);
  }

  template <typename T>
  inline bool copyVariantFrom(OneOf& other) {
    if (other.is<T>()) {
      ctor(*reinterpret_cast<T*>(space), other.get<T>());
    }
    return false;
  }
  void copyFrom(OneOf& other) {


    tag = other.tag;
    doAll(copyVariantFrom<Variants>(other)...);
  }

  template <typename T>
  inline bool moveVariantFrom(OneOf& other) {
    if (other.is<T>()) {
      ctor(*reinterpret_cast<T*>(space), kj::mv(other.get<T>()));
    }
    return false;
  }
  void moveFrom(OneOf& other) {


    tag = other.tag;
    doAll(moveVariantFrom<Variants>(other)...);
  }

  template <typename T, typename... OtherVariants>
  inline bool copySubsetVariantFrom(const OneOf<OtherVariants...>& other) {
    if (other.template is<T>()) {
      tag = typeIndex<Decay<T>>();
      ctor(*reinterpret_cast<T*>(space), other.template get<T>());
    }
    return false;
  }
  template <typename... OtherVariants>
  void copyFromSubset(const OneOf<OtherVariants...>& other) {
    doAll(copySubsetVariantFrom<OtherVariants>(other)...);
  }

  template <typename T, typename... OtherVariants>
  inline bool copySubsetVariantFrom(OneOf<OtherVariants...>& other) {
    if (other.template is<T>()) {
      tag = typeIndex<Decay<T>>();
      ctor(*reinterpret_cast<T*>(space), other.template get<T>());
    }
    return false;
  }
  template <typename... OtherVariants>
  void copyFromSubset(OneOf<OtherVariants...>& other) {
    doAll(copySubsetVariantFrom<OtherVariants>(other)...);
  }

  template <typename T, typename... OtherVariants>
  inline bool moveSubsetVariantFrom(OneOf<OtherVariants...>& other) {
    if (other.template is<T>()) {
      tag = typeIndex<Decay<T>>();
      ctor(*reinterpret_cast<T*>(space), kj::mv(other.template get<T>()));
    }
    return false;
  }
  template <typename... OtherVariants>
  void moveFromSubset(OneOf<OtherVariants...>& other) {
    doAll(moveSubsetVariantFrom<OtherVariants>(other)...);
  }
};

template <typename... Variants>
template <uint i, typename First, typename... Rest>
struct OneOf<Variants...>::HasAll<i, First, Rest...>
    : public HasAll<typeIndexOrZero<First>(), Rest...> {};
template <typename... Variants>
template <uint i>
struct OneOf<Variants...>::HasAll<i>: public _::SuccessIfNotZero<i> {};

template <typename... Variants>
template <uint i>
void OneOf<Variants...>::allHandled() {




  static_assert(i == sizeof...(Variants), "new OneOf variants need to be handled here");
  ::kj::_::unreachable();;
}
# 441 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/one-of.h" 3
}


# 41 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 2
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h"
       


# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/function.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/function.h"
       




# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/function.h"

# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/function.h" 3


namespace kj {

template <typename Signature>
class Function;
# 86 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/function.h" 3
template <typename Signature>
class ConstFunction;


template <typename Signature>
class FunctionParam;







template <typename Return, typename... Params>
class Function<Return(Params...)> {
public:
  template <typename F>
  inline Function(F&& f): impl(heap<Impl<F>>(kj::fwd<F>(f))) {}
  Function() = default;



  Function(const Function&) = delete; Function& operator=(const Function&) = delete;
  Function(Function&) = delete;
  Function& operator=(Function&) = delete;
  template <typename T> Function(const Function<T>&) = delete;
  template <typename T> Function& operator=(const Function<T>&) = delete;
  template <typename T> Function(const ConstFunction<T>&) = delete;
  template <typename T> Function& operator=(const ConstFunction<T>&) = delete;
  Function(Function&&) = default;
  Function& operator=(Function&&) = default;

  inline Return operator()(Params... params) {
    return (*impl)(kj::fwd<Params>(params)...);
  }

  Function reference() {




    return *impl;
  }

private:
  class Iface {
  public:
    virtual Return operator()(Params... params) = 0;
  };

  template <typename F>
  class Impl final: public Iface {
  public:
    explicit Impl(F&& f): f(kj::fwd<F>(f)) {}

    Return operator()(Params... params) override {
      return f(kj::fwd<Params>(params)...);
    }

  private:
    F f;
  };

  Own<Iface> impl;
};

template <typename Return, typename... Params>
class ConstFunction<Return(Params...)> {
public:
  template <typename F>
  inline ConstFunction(F&& f): impl(heap<Impl<F>>(kj::fwd<F>(f))) {}
  ConstFunction() = default;



  ConstFunction(const ConstFunction&) = delete; ConstFunction& operator=(const ConstFunction&) = delete;
  ConstFunction(ConstFunction&) = delete;
  ConstFunction& operator=(ConstFunction&) = delete;
  template <typename T> ConstFunction(const ConstFunction<T>&) = delete;
  template <typename T> ConstFunction& operator=(const ConstFunction<T>&) = delete;
  template <typename T> ConstFunction(const Function<T>&) = delete;
  template <typename T> ConstFunction& operator=(const Function<T>&) = delete;
  ConstFunction(ConstFunction&&) = default;
  ConstFunction& operator=(ConstFunction&&) = default;

  inline Return operator()(Params... params) const {
    return (*impl)(kj::fwd<Params>(params)...);
  }

  ConstFunction reference() const {




    return *impl;
  }

private:
  class Iface {
  public:
    virtual Return operator()(Params... params) const = 0;
  };

  template <typename F>
  class Impl final: public Iface {
  public:
    explicit Impl(F&& f): f(kj::fwd<F>(f)) {}

    Return operator()(Params... params) const override {
      return f(kj::fwd<Params>(params)...);
    }

  private:
    F f;
  };

  Own<Iface> impl;
};

template <typename Return, typename... Params>
class FunctionParam<Return(Params...)> {
public:
  template <typename Func>
  FunctionParam(Func&& func) {
    typedef Wrapper<Decay<Func>> WrapperType;



    static_assert(sizeof(WrapperType) == sizeof(space),
        "expected WrapperType to be two pointers");




    ctor(*reinterpret_cast<WrapperType*>(space), func);
  }

  FunctionParam(const FunctionParam& other) = default;
  FunctionParam(FunctionParam&& other) = default;


  inline Return operator()(Params... params) {
    return (*reinterpret_cast<WrapperBase*>(space))(kj::fwd<Params>(params)...);
  }

private:
  alignas(void*) char space[2 * sizeof(void*)];

  class WrapperBase {
  public:
    virtual Return operator()(Params... params) = 0;
  };

  template <typename Func>
  class Wrapper: public WrapperBase {
  public:
    Wrapper(Func& func): func(func) {}

    inline Return operator()(Params... params) override {
      return func(kj::fwd<Params>(params)...);
    }

  private:
    Func& func;
  };
};

namespace _ {

template <typename T, typename Func, typename ConstFunc>
class BoundMethod {
public:
  BoundMethod(T&& t, Func&& func, ConstFunc&& constFunc)
      : t(kj::fwd<T>(t)), func(kj::mv(func)), constFunc(kj::mv(constFunc)) {}

  template <typename... Params>
  auto operator()(Params&&... params) {
    return func(t, kj::fwd<Params>(params)...);
  }
  template <typename... Params>
  auto operator()(Params&&... params) const {
    return constFunc(t, kj::fwd<Params>(params)...);
  }

private:
  T t;
  Func func;
  ConstFunc constFunc;
};

template <typename T, typename Func, typename ConstFunc>
BoundMethod<T, Func, ConstFunc> boundMethod(T&& t, Func&& func, ConstFunc&& constFunc) {
  return { kj::fwd<T>(t), kj::fwd<Func>(func), kj::fwd<ConstFunc>(constFunc) };
}

}
# 291 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/function.h" 3
}


# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 2
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/thread.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/thread.h"
       






# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/thread.h"

# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/thread.h" 3


namespace kj {

class Thread {




public:
  explicit Thread(Function<void()> func);
  Thread(const Thread&) = delete; Thread& operator=(const Thread&) = delete;

  ~Thread() noexcept(false);


  void sendSignal(int signo);



  void detach();


private:
  struct ThreadState {
    ThreadState(Function<void()> func);

    Function<void()> func;
    Function<void(Function<void()>)> initializer;
    kj::Maybe<kj::Exception> exception;

    unsigned int refcount;


    void unref();
  };
  ThreadState* state;




  unsigned long long threadId;

  bool detached = false;




  static void* runThread(void* ptr);

};

}


# 27 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 2
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/timer.h" 1
# 23 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/timer.h"
       

# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/time.h" 1
# 23 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/time.h"
       

# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h" 1
# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h"
       


# 1 "/usr/include/c++/v1/inttypes.h" 1 3 4
# 238 "/usr/include/c++/v1/inttypes.h" 3 4
# 1 "/usr/include/c++/v1/__config" 1 3 4
# 239 "/usr/include/c++/v1/inttypes.h" 2 3 4


       
# 242 "/usr/include/c++/v1/inttypes.h" 3
# 251 "/usr/include/c++/v1/inttypes.h" 3
# 1 "/usr/include/inttypes.h" 1 3 4
# 27 "/usr/include/inttypes.h" 3 4
# 1 "/usr/include/c++/v1/stdint.h" 1 3 4
# 106 "/usr/include/c++/v1/stdint.h" 3 4
# 1 "/usr/include/c++/v1/__config" 1 3 4
# 107 "/usr/include/c++/v1/stdint.h" 2 3 4


       
# 110 "/usr/include/c++/v1/stdint.h" 3
# 123 "/usr/include/c++/v1/stdint.h" 3
# 1 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stdint.h" 1 3 4
# 9 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stdint.h" 3 4
# 1 "/usr/include/stdint.h" 1 3 4
# 26 "/usr/include/stdint.h" 3 4
# 1 "/usr/include/bits/libc-header-start.h" 1 3 4
# 27 "/usr/include/stdint.h" 2 3 4
# 1 "/usr/include/bits/types.h" 1 3 4
# 27 "/usr/include/bits/types.h" 3 4
# 1 "/usr/include/bits/wordsize.h" 1 3 4
# 28 "/usr/include/bits/types.h" 2 3 4
# 1 "/usr/include/bits/timesize.h" 1 3 4
# 29 "/usr/include/bits/types.h" 2 3 4


typedef unsigned char __u_char;
typedef unsigned short int __u_short;
typedef unsigned int __u_int;
typedef unsigned long int __u_long;


typedef signed char __int8_t;
typedef unsigned char __uint8_t;
typedef signed short int __int16_t;
typedef unsigned short int __uint16_t;
typedef signed int __int32_t;
typedef unsigned int __uint32_t;

typedef signed long int __int64_t;
typedef unsigned long int __uint64_t;






typedef __int8_t __int_least8_t;
typedef __uint8_t __uint_least8_t;
typedef __int16_t __int_least16_t;
typedef __uint16_t __uint_least16_t;
typedef __int32_t __int_least32_t;
typedef __uint32_t __uint_least32_t;
typedef __int64_t __int_least64_t;
typedef __uint64_t __uint_least64_t;



typedef long int __quad_t;
typedef unsigned long int __u_quad_t;







typedef long int __intmax_t;
typedef unsigned long int __uintmax_t;
# 141 "/usr/include/bits/types.h" 3 4
# 1 "/usr/include/bits/typesizes.h" 1 3 4
# 142 "/usr/include/bits/types.h" 2 3 4
# 1 "/usr/include/bits/time64.h" 1 3 4
# 143 "/usr/include/bits/types.h" 2 3 4


typedef unsigned long int __dev_t;
typedef unsigned int __uid_t;
typedef unsigned int __gid_t;
typedef unsigned long int __ino_t;
typedef unsigned long int __ino64_t;
typedef unsigned int __mode_t;
typedef unsigned long int __nlink_t;
typedef long int __off_t;
typedef long int __off64_t;
typedef int __pid_t;
typedef struct { int __val[2]; } __fsid_t;
typedef long int __clock_t;
typedef unsigned long int __rlim_t;
typedef unsigned long int __rlim64_t;
typedef unsigned int __id_t;
typedef long int __time_t;
typedef unsigned int __useconds_t;
typedef long int __suseconds_t;
typedef long int __suseconds64_t;

typedef int __daddr_t;
typedef int __key_t;


typedef int __clockid_t;


typedef void * __timer_t;


typedef long int __blksize_t;




typedef long int __blkcnt_t;
typedef long int __blkcnt64_t;


typedef unsigned long int __fsblkcnt_t;
typedef unsigned long int __fsblkcnt64_t;


typedef unsigned long int __fsfilcnt_t;
typedef unsigned long int __fsfilcnt64_t;


typedef long int __fsword_t;

typedef long int __ssize_t;


typedef long int __syscall_slong_t;

typedef unsigned long int __syscall_ulong_t;



typedef __off64_t __loff_t;
typedef char *__caddr_t;


typedef long int __intptr_t;


typedef unsigned int __socklen_t;




typedef int __sig_atomic_t;
# 28 "/usr/include/stdint.h" 2 3 4
# 1 "/usr/include/bits/wchar.h" 1 3 4
# 29 "/usr/include/stdint.h" 2 3 4
# 1 "/usr/include/bits/wordsize.h" 1 3 4
# 30 "/usr/include/stdint.h" 2 3 4




# 1 "/usr/include/bits/stdint-intn.h" 1 3 4
# 24 "/usr/include/bits/stdint-intn.h" 3 4
typedef __int8_t int8_t;
typedef __int16_t int16_t;
typedef __int32_t int32_t;
typedef __int64_t int64_t;
# 35 "/usr/include/stdint.h" 2 3 4


# 1 "/usr/include/bits/stdint-uintn.h" 1 3 4
# 24 "/usr/include/bits/stdint-uintn.h" 3 4
typedef __uint8_t uint8_t;
typedef __uint16_t uint16_t;
typedef __uint32_t uint32_t;
typedef __uint64_t uint64_t;
# 38 "/usr/include/stdint.h" 2 3 4





typedef __int_least8_t int_least8_t;
typedef __int_least16_t int_least16_t;
typedef __int_least32_t int_least32_t;
typedef __int_least64_t int_least64_t;


typedef __uint_least8_t uint_least8_t;
typedef __uint_least16_t uint_least16_t;
typedef __uint_least32_t uint_least32_t;
typedef __uint_least64_t uint_least64_t;





typedef signed char int_fast8_t;

typedef long int int_fast16_t;
typedef long int int_fast32_t;
typedef long int int_fast64_t;
# 71 "/usr/include/stdint.h" 3 4
typedef unsigned char uint_fast8_t;

typedef unsigned long int uint_fast16_t;
typedef unsigned long int uint_fast32_t;
typedef unsigned long int uint_fast64_t;
# 87 "/usr/include/stdint.h" 3 4
typedef long int intptr_t;


typedef unsigned long int uintptr_t;
# 101 "/usr/include/stdint.h" 3 4
typedef __intmax_t intmax_t;
typedef __uintmax_t uintmax_t;
# 10 "/usr/lib/gcc/x86_64-pc-linux-gnu/11.1.0/include/stdint.h" 2 3 4
# 124 "/usr/include/c++/v1/stdint.h" 2 3
# 28 "/usr/include/inttypes.h" 2 3 4
# 266 "/usr/include/inttypes.h" 3 4
extern "C" {




typedef struct
  {
    long int quot;
    long int rem;
  } imaxdiv_t;
# 290 "/usr/include/inttypes.h" 3 4
extern intmax_t imaxabs (intmax_t __n) noexcept (true) __attribute__ ((__const__));


extern imaxdiv_t imaxdiv (intmax_t __numer, intmax_t __denom)
      noexcept (true) __attribute__ ((__const__));


extern intmax_t strtoimax (const char *__restrict __nptr,
      char **__restrict __endptr, int __base) noexcept (true);


extern uintmax_t strtoumax (const char *__restrict __nptr,
       char ** __restrict __endptr, int __base) noexcept (true);


extern intmax_t wcstoimax (const wchar_t *__restrict __nptr,
      wchar_t **__restrict __endptr, int __base)
     noexcept (true);


extern uintmax_t wcstoumax (const wchar_t *__restrict __nptr,
       wchar_t ** __restrict __endptr, int __base)
     noexcept (true);

}
# 252 "/usr/include/c++/v1/inttypes.h" 2 3
# 30 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h" 2


# 31 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h"

# 31 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h" 3


namespace kj {




template <typename UnderlyingType, typename Label>
struct Id {
# 52 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h" 3
  UnderlyingType value;

  inline constexpr Id(): value(0) {}
  inline constexpr explicit Id(int value): value(value) {}

  inline constexpr bool operator==(const Id& other) const { return value == other.value; }
  inline constexpr bool operator!=(const Id& other) const { return value != other.value; }
  inline constexpr bool operator<=(const Id& other) const { return value <= other.value; }
  inline constexpr bool operator>=(const Id& other) const { return value >= other.value; }
  inline constexpr bool operator< (const Id& other) const { return value < other.value; }
  inline constexpr bool operator> (const Id& other) const { return value > other.value; }
};




struct Unsafe_ {};
constexpr Unsafe_ unsafe = Unsafe_();


template <uint64_t maxN, typename T>
class Bounded;
template <uint value>
class BoundedConst;

template <typename T> constexpr bool isIntegral() { return false; }
template <> constexpr bool isIntegral<char>() { return true; }
template <> constexpr bool isIntegral<signed char>() { return true; }
template <> constexpr bool isIntegral<short>() { return true; }
template <> constexpr bool isIntegral<int>() { return true; }
template <> constexpr bool isIntegral<long>() { return true; }
template <> constexpr bool isIntegral<long long>() { return true; }
template <> constexpr bool isIntegral<unsigned char>() { return true; }
template <> constexpr bool isIntegral<unsigned short>() { return true; }
template <> constexpr bool isIntegral<unsigned int>() { return true; }
template <> constexpr bool isIntegral<unsigned long>() { return true; }
template <> constexpr bool isIntegral<unsigned long long>() { return true; }

template <typename T>
struct IsIntegralOrBounded_ { static constexpr bool value = isIntegral<T>(); };
template <uint64_t m, typename T>
struct IsIntegralOrBounded_<Bounded<m, T>> { static constexpr bool value = true; };
template <uint v>
struct IsIntegralOrBounded_<BoundedConst<v>> { static constexpr bool value = true; };

template <typename T>
inline constexpr bool isIntegralOrBounded() { return IsIntegralOrBounded_<T>::value; }

template <typename Number, typename Unit1, typename Unit2>
class UnitRatio {






  static_assert(isIntegralOrBounded<Number>(),
      "Underlying type for UnitRatio must be integer.");

public:
  inline UnitRatio() {}

  constexpr UnitRatio(Number unit1PerUnit2, decltype(unsafe)): unit1PerUnit2(unit1PerUnit2) {}



  template <typename OtherNumber>
  inline constexpr UnitRatio(const UnitRatio<OtherNumber, Unit1, Unit2>& other)
      : unit1PerUnit2(other.unit1PerUnit2) {}

  template <typename OtherNumber>
  inline constexpr UnitRatio<decltype(Number()+OtherNumber()), Unit1, Unit2>
      operator+(UnitRatio<OtherNumber, Unit1, Unit2> other) const {
    return UnitRatio<decltype(Number()+OtherNumber()), Unit1, Unit2>(
        unit1PerUnit2 + other.unit1PerUnit2, unsafe);
  }
  template <typename OtherNumber>
  inline constexpr UnitRatio<decltype(Number()-OtherNumber()), Unit1, Unit2>
      operator-(UnitRatio<OtherNumber, Unit1, Unit2> other) const {
    return UnitRatio<decltype(Number()-OtherNumber()), Unit1, Unit2>(
        unit1PerUnit2 - other.unit1PerUnit2, unsafe);
  }

  template <typename OtherNumber, typename Unit3>
  inline constexpr UnitRatio<decltype(Number()*OtherNumber()), Unit3, Unit2>
      operator*(UnitRatio<OtherNumber, Unit3, Unit1> other) const {

    return UnitRatio<decltype(Number()*OtherNumber()), Unit3, Unit2>(
        unit1PerUnit2 * other.unit1PerUnit2, unsafe);
  }
  template <typename OtherNumber, typename Unit3>
  inline constexpr UnitRatio<decltype(Number()*OtherNumber()), Unit1, Unit3>
      operator*(UnitRatio<OtherNumber, Unit2, Unit3> other) const {

    return UnitRatio<decltype(Number()*OtherNumber()), Unit1, Unit3>(
        unit1PerUnit2 * other.unit1PerUnit2, unsafe);
  }

  template <typename OtherNumber, typename Unit3>
  inline constexpr UnitRatio<decltype(Number()*OtherNumber()), Unit3, Unit2>
      operator/(UnitRatio<OtherNumber, Unit1, Unit3> other) const {

    return UnitRatio<decltype(Number()*OtherNumber()), Unit3, Unit2>(
        unit1PerUnit2 / other.unit1PerUnit2, unsafe);
  }
  template <typename OtherNumber, typename Unit3>
  inline constexpr UnitRatio<decltype(Number()*OtherNumber()), Unit1, Unit3>
      operator/(UnitRatio<OtherNumber, Unit3, Unit2> other) const {

    return UnitRatio<decltype(Number()*OtherNumber()), Unit1, Unit3>(
        unit1PerUnit2 / other.unit1PerUnit2, unsafe);
  }

  template <typename OtherNumber>
  inline decltype(Number() / OtherNumber())
      operator/(UnitRatio<OtherNumber, Unit1, Unit2> other) const {
    return unit1PerUnit2 / other.unit1PerUnit2;
  }

  template <typename OtherNumber>
  inline constexpr bool operator==(const UnitRatio<OtherNumber, Unit1, Unit2>& other) const {
    return unit1PerUnit2 == other.unit1PerUnit2;
  }
  template <typename OtherNumber>
  inline constexpr bool operator!=(const UnitRatio<OtherNumber, Unit1, Unit2>& other) const {
    return unit1PerUnit2 != other.unit1PerUnit2;
  }

private:
  Number unit1PerUnit2;

  template <typename OtherNumber, typename OtherUnit>
  friend class Quantity;
  template <typename OtherNumber, typename OtherUnit1, typename OtherUnit2>
  friend class UnitRatio;

  template <typename N1, typename N2, typename U1, typename U2, typename>
  friend inline constexpr UnitRatio<decltype(N1() * N2()), U1, U2>
      operator*(N1, UnitRatio<N2, U1, U2>);
};

template <typename N1, typename N2, typename U1, typename U2,
          typename = EnableIf<isIntegralOrBounded<N1>() && isIntegralOrBounded<N2>()>>
inline constexpr UnitRatio<decltype(N1() * N2()), U1, U2>
    operator*(N1 n, UnitRatio<N2, U1, U2> r) {
  return UnitRatio<decltype(N1() * N2()), U1, U2>(n * r.unit1PerUnit2, unsafe);
}

template <typename Number, typename Unit>
class Quantity {
# 244 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h" 3
  static_assert(isIntegralOrBounded<Number>(),
      "Underlying type for Quantity must be integer.");

public:
  inline constexpr Quantity() = default;

  inline constexpr Quantity(MaxValue_): value(maxValue) {}
  inline constexpr Quantity(MinValue_): value(minValue) {}





  inline constexpr Quantity(Number value, decltype(unsafe)): value(value) {}



  template <typename OtherNumber>
  inline constexpr Quantity(const Quantity<OtherNumber, Unit>& other)
      : value(other.value) {}

  template <typename OtherNumber>
  inline Quantity& operator=(const Quantity<OtherNumber, Unit>& other) {
    value = other.value;
    return *this;
  }

  template <typename OtherNumber>
  inline constexpr Quantity<decltype(Number() + OtherNumber()), Unit>
      operator+(const Quantity<OtherNumber, Unit>& other) const {
    return Quantity<decltype(Number() + OtherNumber()), Unit>(value + other.value, unsafe);
  }
  template <typename OtherNumber>
  inline constexpr Quantity<decltype(Number() - OtherNumber()), Unit>
      operator-(const Quantity<OtherNumber, Unit>& other) const {
    return Quantity<decltype(Number() - OtherNumber()), Unit>(value - other.value, unsafe);
  }
  template <typename OtherNumber, typename = EnableIf<isIntegralOrBounded<OtherNumber>()>>
  inline constexpr Quantity<decltype(Number() * OtherNumber()), Unit>
      operator*(OtherNumber other) const {
    return Quantity<decltype(Number() * other), Unit>(value * other, unsafe);
  }
  template <typename OtherNumber, typename = EnableIf<isIntegralOrBounded<OtherNumber>()>>
  inline constexpr Quantity<decltype(Number() / OtherNumber()), Unit>
      operator/(OtherNumber other) const {
    return Quantity<decltype(Number() / other), Unit>(value / other, unsafe);
  }
  template <typename OtherNumber>
  inline constexpr decltype(Number() / OtherNumber())
      operator/(const Quantity<OtherNumber, Unit>& other) const {
    return value / other.value;
  }
  template <typename OtherNumber>
  inline constexpr Quantity<decltype(Number() % OtherNumber()), Unit>
      operator%(const Quantity<OtherNumber, Unit>& other) const {
    return Quantity<decltype(Number() % OtherNumber()), Unit>(value % other.value, unsafe);
  }

  template <typename OtherNumber, typename OtherUnit>
  inline constexpr Quantity<decltype(Number() * OtherNumber()), OtherUnit>
      operator*(UnitRatio<OtherNumber, OtherUnit, Unit> ratio) const {
    return Quantity<decltype(Number() * OtherNumber()), OtherUnit>(
        value * ratio.unit1PerUnit2, unsafe);
  }
  template <typename OtherNumber, typename OtherUnit>
  inline constexpr Quantity<decltype(Number() / OtherNumber()), OtherUnit>
      operator/(UnitRatio<OtherNumber, Unit, OtherUnit> ratio) const {
    return Quantity<decltype(Number() / OtherNumber()), OtherUnit>(
        value / ratio.unit1PerUnit2, unsafe);
  }
  template <typename OtherNumber, typename OtherUnit>
  inline constexpr Quantity<decltype(Number() % OtherNumber()), Unit>
      operator%(UnitRatio<OtherNumber, Unit, OtherUnit> ratio) const {
    return Quantity<decltype(Number() % OtherNumber()), Unit>(
        value % ratio.unit1PerUnit2, unsafe);
  }
  template <typename OtherNumber, typename OtherUnit>
  inline constexpr UnitRatio<decltype(Number() / OtherNumber()), Unit, OtherUnit>
      operator/(Quantity<OtherNumber, OtherUnit> other) const {
    return UnitRatio<decltype(Number() / OtherNumber()), Unit, OtherUnit>(
        value / other.value, unsafe);
  }

  template <typename OtherNumber>
  inline constexpr bool operator==(const Quantity<OtherNumber, Unit>& other) const {
    return value == other.value;
  }
  template <typename OtherNumber>
  inline constexpr bool operator!=(const Quantity<OtherNumber, Unit>& other) const {
    return value != other.value;
  }
  template <typename OtherNumber>
  inline constexpr bool operator<=(const Quantity<OtherNumber, Unit>& other) const {
    return value <= other.value;
  }
  template <typename OtherNumber>
  inline constexpr bool operator>=(const Quantity<OtherNumber, Unit>& other) const {
    return value >= other.value;
  }
  template <typename OtherNumber>
  inline constexpr bool operator<(const Quantity<OtherNumber, Unit>& other) const {
    return value < other.value;
  }
  template <typename OtherNumber>
  inline constexpr bool operator>(const Quantity<OtherNumber, Unit>& other) const {
    return value > other.value;
  }

  template <typename OtherNumber>
  inline Quantity& operator+=(const Quantity<OtherNumber, Unit>& other) {
    value += other.value;
    return *this;
  }
  template <typename OtherNumber>
  inline Quantity& operator-=(const Quantity<OtherNumber, Unit>& other) {
    value -= other.value;
    return *this;
  }
  template <typename OtherNumber>
  inline Quantity& operator*=(OtherNumber other) {
    value *= other;
    return *this;
  }
  template <typename OtherNumber>
  inline Quantity& operator/=(OtherNumber other) {
    value /= other.value;
    return *this;
  }

private:
  Number value;

  template <typename OtherNumber, typename OtherUnit>
  friend class Quantity;

  template <typename Number1, typename Number2, typename Unit2, typename>
  friend inline constexpr auto operator*(Number1 a, Quantity<Number2, Unit2> b)
      -> Quantity<decltype(Number1() * Number2()), Unit2>;
};

template <typename T> struct Unit_ {
  static inline constexpr T get() { return T(1); }
};
template <typename T, typename U>
struct Unit_<Quantity<T, U>> {
  static inline constexpr Quantity<decltype(Unit_<T>::get()), U> get() {
    return Quantity<decltype(Unit_<T>::get()), U>(Unit_<T>::get(), unsafe);
  }
};

template <typename T>
inline constexpr auto unit() -> decltype(Unit_<T>::get()) { return Unit_<T>::get(); }



template <typename Number1, typename Number2, typename Unit,
          typename = EnableIf<isIntegralOrBounded<Number1>()>>
inline constexpr auto operator*(Number1 a, Quantity<Number2, Unit> b)
    -> Quantity<decltype(Number1() * Number2()), Unit> {
  return Quantity<decltype(Number1() * Number2()), Unit>(a * b.value, unsafe);
}

template <typename Number1, typename Number2, typename Unit, typename Unit2>
inline constexpr auto operator*(UnitRatio<Number1, Unit2, Unit> ratio,
    Quantity<Number2, Unit> measure)
    -> decltype(measure * ratio) {
  return measure * ratio;
}




template <typename T, typename Label>
class Absolute {
# 430 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h" 3
public:
  inline constexpr Absolute(MaxValue_): value(maxValue) {}
  inline constexpr Absolute(MinValue_): value(minValue) {}





  inline constexpr Absolute operator+(const T& other) const { return Absolute(value + other); }
  inline constexpr Absolute operator-(const T& other) const { return Absolute(value - other); }
  inline constexpr T operator-(const Absolute& other) const { return value - other.value; }

  inline Absolute& operator+=(const T& other) { value += other; return *this; }
  inline Absolute& operator-=(const T& other) { value -= other; return *this; }

  inline constexpr bool operator==(const Absolute& other) const { return value == other.value; }
  inline constexpr bool operator!=(const Absolute& other) const { return value != other.value; }
  inline constexpr bool operator<=(const Absolute& other) const { return value <= other.value; }
  inline constexpr bool operator>=(const Absolute& other) const { return value >= other.value; }
  inline constexpr bool operator< (const Absolute& other) const { return value < other.value; }
  inline constexpr bool operator> (const Absolute& other) const { return value > other.value; }

private:
  T value;

  explicit constexpr Absolute(T value): value(value) {}

  template <typename U>
  friend inline constexpr U origin();
};

template <typename T, typename Label>
inline constexpr Absolute<T, Label> operator+(const T& a, const Absolute<T, Label>& b) {
  return b + a;
}

template <typename T> struct UnitOf_ { typedef T Type; };
template <typename T, typename Label> struct UnitOf_<Absolute<T, Label>> { typedef T Type; };
template <typename T>
using UnitOf = typename UnitOf_<T>::Type;


template <typename T>
inline constexpr T origin() { return T(0 * unit<UnitOf<T>>()); }






template <uint64_t n, uint accum = 0>
struct BitCount_ {
  static constexpr uint value = BitCount_<(n >> 1), accum + 1>::value;
};
template <uint accum>
struct BitCount_<0, accum> {
  static constexpr uint value = accum;
};

template <uint64_t n>
inline constexpr uint bitCount() { return BitCount_<n>::value; }


template <uint bitCountBitCount> struct AtLeastUInt_ {
  static_assert(bitCountBitCount < 7, "don't know how to represent integers over 64 bits");
};
template <> struct AtLeastUInt_<0> { typedef uint8_t Type; };
template <> struct AtLeastUInt_<1> { typedef uint8_t Type; };
template <> struct AtLeastUInt_<2> { typedef uint8_t Type; };
template <> struct AtLeastUInt_<3> { typedef uint8_t Type; };
template <> struct AtLeastUInt_<4> { typedef uint16_t Type; };
template <> struct AtLeastUInt_<5> { typedef uint32_t Type; };
template <> struct AtLeastUInt_<6> { typedef uint64_t Type; };

template <uint bits>
using AtLeastUInt = typename AtLeastUInt_<bitCount<max(bits, 1) - 1>()>::Type;




template <uint value>
class BoundedConst {


public:
  BoundedConst() = default;

  inline constexpr uint unwrap() const { return value; }
# 532 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h" 3
  template <uint other> inline constexpr BoundedConst<(value + other)> operator +(BoundedConst<other>) const { static_assert(value + other >= value, "overflow in BoundedConst arithmetic"); return BoundedConst<(value + other)>(); }
  template <uint other> inline constexpr BoundedConst<(value - other)> operator -(BoundedConst<other>) const { static_assert(value - other <= value, "overflow in BoundedConst arithmetic"); return BoundedConst<(value - other)>(); }
  template <uint other> inline constexpr BoundedConst<(value * other)> operator *(BoundedConst<other>) const { static_assert(value * other / other == value, "overflow in BoundedConst arithmetic"); return BoundedConst<(value * other)>(); }
  template <uint other> inline constexpr BoundedConst<(value / other)> operator /(BoundedConst<other>) const { static_assert(true, "overflow in BoundedConst arithmetic"); return BoundedConst<(value / other)>(); }
  template <uint other> inline constexpr BoundedConst<(value % other)> operator %(BoundedConst<other>) const { static_assert(true, "overflow in BoundedConst arithmetic"); return BoundedConst<(value % other)>(); }
  template <uint other> inline constexpr BoundedConst<(value << other)> operator <<(BoundedConst<other>) const { static_assert(value << other >= value, "overflow in BoundedConst arithmetic"); return BoundedConst<(value << other)>(); }
  template <uint other> inline constexpr BoundedConst<(value >> other)> operator >>(BoundedConst<other>) const { static_assert(true, "overflow in BoundedConst arithmetic"); return BoundedConst<(value >> other)>(); }
  template <uint other> inline constexpr BoundedConst<(value & other)> operator &(BoundedConst<other>) const { static_assert(true, "overflow in BoundedConst arithmetic"); return BoundedConst<(value & other)>(); }
  template <uint other> inline constexpr BoundedConst<(value | other)> operator |(BoundedConst<other>) const { static_assert(true, "overflow in BoundedConst arithmetic"); return BoundedConst<(value | other)>(); }

  template <uint other> inline constexpr bool operator ==(BoundedConst<other>) const { return value == other; }
  template <uint other> inline constexpr bool operator !=(BoundedConst<other>) const { return value != other; }
  template <uint other> inline constexpr bool operator <(BoundedConst<other>) const { return value < other; }
  template <uint other> inline constexpr bool operator >(BoundedConst<other>) const { return value > other; }
  template <uint other> inline constexpr bool operator <=(BoundedConst<other>) const { return value <= other; }
  template <uint other> inline constexpr bool operator >=(BoundedConst<other>) const { return value >= other; }


};

template <uint64_t m, typename T>
struct Unit_<Bounded<m, T>> {
  static inline constexpr BoundedConst<1> get() { return BoundedConst<1>(); }
};

template <uint value>
struct Unit_<BoundedConst<value>> {
  static inline constexpr BoundedConst<1> get() { return BoundedConst<1>(); }
};

template <uint value>
inline constexpr BoundedConst<value> bounded() {
  return BoundedConst<value>();
}

template <uint64_t a, uint64_t b>
static constexpr uint64_t boundedAdd() {
  static_assert(a + b >= a, "possible overflow detected");
  return a + b;
}
template <uint64_t a, uint64_t b>
static constexpr uint64_t boundedSub() {
  static_assert(a - b <= a, "possible underflow detected");
  return a - b;
}
template <uint64_t a, uint64_t b>
static constexpr uint64_t boundedMul() {
  static_assert(a * b / b == a, "possible overflow detected");
  return a * b;
}
template <uint64_t a, uint64_t b>
static constexpr uint64_t boundedLShift() {
  static_assert(a << b >= a, "possible overflow detected");
  return a << b;
}

template <uint a, uint b>
inline constexpr BoundedConst<kj::min(a, b)> min(BoundedConst<a>, BoundedConst<b>) {
  return bounded<kj::min(a, b)>();
}
template <uint a, uint b>
inline constexpr BoundedConst<kj::max(a, b)> max(BoundedConst<a>, BoundedConst<b>) {
  return bounded<kj::max(a, b)>();
}





template <uint64_t maxN, typename T>
class Bounded {
public:
  static_assert(maxN <= T(kj::maxValue), "possible overflow detected");

  Bounded() = default;

  Bounded(const Bounded& other) = default;
  template <typename OtherInt, typename = EnableIf<isIntegral<OtherInt>()>>
  inline constexpr Bounded(OtherInt value): value(value) {
    static_assert(OtherInt(maxValue) <= maxN, "possible overflow detected");
  }
  template <uint64_t otherMax, typename OtherT>
  inline constexpr Bounded(const Bounded<otherMax, OtherT>& other)
      : value(other.value) {
    static_assert(otherMax <= maxN, "possible overflow detected");
  }
  template <uint otherValue>
  inline constexpr Bounded(BoundedConst<otherValue>)
      : value(otherValue) {
    static_assert(otherValue <= maxN, "overflow detected");
  }

  Bounded& operator=(const Bounded& other) = default;
  template <typename OtherInt, typename = EnableIf<isIntegral<OtherInt>()>>
  Bounded& operator=(OtherInt other) {
    static_assert(OtherInt(maxValue) <= maxN, "possible overflow detected");
    value = other;
    return *this;
  }
  template <uint64_t otherMax, typename OtherT>
  inline Bounded& operator=(const Bounded<otherMax, OtherT>& other) {
    static_assert(otherMax <= maxN, "possible overflow detected");
    value = other.value;
    return *this;
  }
  template <uint otherValue>
  inline Bounded& operator=(BoundedConst<otherValue>) {
    static_assert(otherValue <= maxN, "overflow detected");
    value = otherValue;
    return *this;
  }

  inline constexpr T unwrap() const { return value; }
# 658 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h" 3
  template <uint64_t otherMax, typename otherT> inline constexpr Bounded<(boundedAdd<maxN, otherMax>()), decltype(T() + otherT())> operator +(const Bounded<otherMax, otherT>& other) const { return Bounded<(boundedAdd<maxN, otherMax>()), decltype(T() + otherT())>(value + other.value, unsafe); }
  template <uint64_t otherMax, typename otherT> inline constexpr Bounded<(boundedMul<maxN, otherMax>()), decltype(T() * otherT())> operator *(const Bounded<otherMax, otherT>& other) const { return Bounded<(boundedMul<maxN, otherMax>()), decltype(T() * otherT())>(value * other.value, unsafe); }
  template <uint64_t otherMax, typename otherT> inline constexpr Bounded<maxN, decltype(T() / otherT())> operator /(const Bounded<otherMax, otherT>& other) const { return Bounded<maxN, decltype(T() / otherT())>(value / other.value, unsafe); }
  template <uint64_t otherMax, typename otherT> inline constexpr Bounded<otherMax - 1, decltype(T() % otherT())> operator %(const Bounded<otherMax, otherT>& other) const { return Bounded<otherMax - 1, decltype(T() % otherT())>(value % other.value, unsafe); }




  template <uint64_t otherMax, typename OtherT> inline constexpr bool operator ==(const Bounded<otherMax, OtherT>& other) const { return value == other.value; }
  template <uint64_t otherMax, typename OtherT> inline constexpr bool operator !=(const Bounded<otherMax, OtherT>& other) const { return value != other.value; }
  template <uint64_t otherMax, typename OtherT> inline constexpr bool operator <(const Bounded<otherMax, OtherT>& other) const { return value < other.value; }
  template <uint64_t otherMax, typename OtherT> inline constexpr bool operator >(const Bounded<otherMax, OtherT>& other) const { return value > other.value; }
  template <uint64_t otherMax, typename OtherT> inline constexpr bool operator <=(const Bounded<otherMax, OtherT>& other) const { return value <= other.value; }
  template <uint64_t otherMax, typename OtherT> inline constexpr bool operator >=(const Bounded<otherMax, OtherT>& other) const { return value >= other.value; }




  template <uint64_t newMax, typename ErrorFunc>
  inline Bounded<newMax, T> assertMax(ErrorFunc&& func) const {

    static_assert(newMax < maxN, "this bounded size assertion is redundant");
    if (__builtin_expect(value > newMax, false)) func();
    return Bounded<newMax, T>(value, unsafe);
  }

  template <uint64_t otherMax, typename OtherT, typename ErrorFunc>
  inline Bounded<maxN, decltype(T() - OtherT())> subtractChecked(
      const Bounded<otherMax, OtherT>& other, ErrorFunc&& func) const {

    if (__builtin_expect(value < other.value, false)) func();
    return Bounded<maxN, decltype(T() - OtherT())>(value - other.value, unsafe);
  }

  template <uint otherValue, typename ErrorFunc>
  inline Bounded<maxN - otherValue, T> subtractChecked(
      BoundedConst<otherValue>, ErrorFunc&& func) const {

    static_assert(otherValue <= maxN, "underflow detected");
    if (__builtin_expect(value < otherValue, false)) func();
    return Bounded<maxN - otherValue, T>(value - otherValue, unsafe);
  }

  template <uint64_t otherMax, typename OtherT>
  inline Maybe<Bounded<maxN, decltype(T() - OtherT())>> trySubtract(
      const Bounded<otherMax, OtherT>& other) const {

    if (value < other.value) {
      return nullptr;
    } else {
      return Bounded<maxN, decltype(T() - OtherT())>(value - other.value, unsafe);
    }
  }

  template <uint otherValue>
  inline Maybe<Bounded<maxN - otherValue, T>> trySubtract(BoundedConst<otherValue>) const {

    if (value < otherValue) {
      return nullptr;
    } else {
      return Bounded<maxN - otherValue, T>(value - otherValue, unsafe);
    }
  }

  inline constexpr Bounded(T value, decltype(unsafe)): value(value) {}
  template <uint64_t otherMax, typename OtherT>
  inline constexpr Bounded(Bounded<otherMax, OtherT> value, decltype(unsafe))
      : value(value.value) {}




private:
  T value;

  template <uint64_t, typename>
  friend class Bounded;
};

template <typename Number>
inline constexpr Bounded<Number(kj::maxValue), Number> bounded(Number value) {
  return Bounded<Number(kj::maxValue), Number>(value, unsafe);
}

inline constexpr Bounded<1, uint8_t> bounded(bool value) {
  return Bounded<1, uint8_t>(value, unsafe);
}

template <uint bits, typename Number>
inline constexpr Bounded<maxValueForBits<bits>(), Number> assumeBits(Number value) {
  return Bounded<maxValueForBits<bits>(), Number>(value, unsafe);
}

template <uint bits, uint64_t maxN, typename T>
inline constexpr Bounded<maxValueForBits<bits>(), T> assumeBits(Bounded<maxN, T> value) {
  return Bounded<maxValueForBits<bits>(), T>(value, unsafe);
}

template <uint bits, typename Number, typename Unit>
inline constexpr auto assumeBits(Quantity<Number, Unit> value)
    -> Quantity<decltype(assumeBits<bits>(value / unit<Quantity<Number, Unit>>())), Unit> {
  return Quantity<decltype(assumeBits<bits>(value / unit<Quantity<Number, Unit>>())), Unit>(
      assumeBits<bits>(value / unit<Quantity<Number, Unit>>()), unsafe);
}

template <uint64_t maxN, typename Number>
inline constexpr Bounded<maxN, Number> assumeMax(Number value) {
  return Bounded<maxN, Number>(value, unsafe);
}

template <uint64_t newMaxN, uint64_t maxN, typename T>
inline constexpr Bounded<newMaxN, T> assumeMax(Bounded<maxN, T> value) {
  return Bounded<newMaxN, T>(value, unsafe);
}

template <uint64_t maxN, typename Number, typename Unit>
inline constexpr auto assumeMax(Quantity<Number, Unit> value)
    -> Quantity<decltype(assumeMax<maxN>(value / unit<Quantity<Number, Unit>>())), Unit> {
  return Quantity<decltype(assumeMax<maxN>(value / unit<Quantity<Number, Unit>>())), Unit>(
      assumeMax<maxN>(value / unit<Quantity<Number, Unit>>()), unsafe);
}

template <uint maxN, typename Number>
inline constexpr Bounded<maxN, Number> assumeMax(BoundedConst<maxN>, Number value) {
  return assumeMax<maxN>(value);
}

template <uint newMaxN, uint64_t maxN, typename T>
inline constexpr Bounded<newMaxN, T> assumeMax(BoundedConst<maxN>, Bounded<maxN, T> value) {
  return assumeMax<maxN>(value);
}

template <uint maxN, typename Number, typename Unit>
inline constexpr auto assumeMax(Quantity<BoundedConst<maxN>, Unit>, Quantity<Number, Unit> value)
    -> decltype(assumeMax<maxN>(value)) {
  return assumeMax<maxN>(value);
}

template <uint64_t newMax, uint64_t maxN, typename T, typename ErrorFunc>
inline Bounded<newMax, T> assertMax(Bounded<maxN, T> value, ErrorFunc&& errorFunc) {


  static_assert(newMax < maxN, "this bounded size assertion is redundant");
  return value.template assertMax<newMax>(kj::fwd<ErrorFunc>(errorFunc));
}

template <uint64_t newMax, uint64_t maxN, typename T, typename Unit, typename ErrorFunc>
inline Quantity<Bounded<newMax, T>, Unit> assertMax(
    Quantity<Bounded<maxN, T>, Unit> value, ErrorFunc&& errorFunc) {


  static_assert(newMax < maxN, "this bounded size assertion is redundant");
  return (value / unit<decltype(value)>()).template assertMax<newMax>(
      kj::fwd<ErrorFunc>(errorFunc)) * unit<decltype(value)>();
}

template <uint newMax, uint64_t maxN, typename T, typename ErrorFunc>
inline Bounded<newMax, T> assertMax(
    BoundedConst<newMax>, Bounded<maxN, T> value, ErrorFunc&& errorFunc) {
  return assertMax<newMax>(value, kj::mv(errorFunc));
}

template <uint newMax, uint64_t maxN, typename T, typename Unit, typename ErrorFunc>
inline Quantity<Bounded<newMax, T>, Unit> assertMax(
    Quantity<BoundedConst<newMax>, Unit>,
    Quantity<Bounded<maxN, T>, Unit> value, ErrorFunc&& errorFunc) {
  return assertMax<newMax>(value, kj::mv(errorFunc));
}

template <uint64_t newBits, uint64_t maxN, typename T, typename ErrorFunc = ThrowOverflow>
inline Bounded<maxValueForBits<newBits>(), T> assertMaxBits(
    Bounded<maxN, T> value, ErrorFunc&& errorFunc = ErrorFunc()) {


  return assertMax<maxValueForBits<newBits>()>(value, kj::fwd<ErrorFunc>(errorFunc));
}

template <uint64_t newBits, uint64_t maxN, typename T, typename Unit,
          typename ErrorFunc = ThrowOverflow>
inline Quantity<Bounded<maxValueForBits<newBits>(), T>, Unit> assertMaxBits(
    Quantity<Bounded<maxN, T>, Unit> value, ErrorFunc&& errorFunc = ErrorFunc()) {


  return assertMax<maxValueForBits<newBits>()>(value, kj::fwd<ErrorFunc>(errorFunc));
}

template <typename newT, uint64_t maxN, typename T>
inline constexpr Bounded<maxN, newT> upgradeBound(Bounded<maxN, T> value) {
  return value;
}

template <typename newT, uint64_t maxN, typename T, typename Unit>
inline constexpr Quantity<Bounded<maxN, newT>, Unit> upgradeBound(
    Quantity<Bounded<maxN, T>, Unit> value) {
  return value;
}

template <uint64_t maxN, typename T, typename Other, typename ErrorFunc>
inline auto subtractChecked(Bounded<maxN, T> value, Other other, ErrorFunc&& errorFunc)
    -> decltype(value.subtractChecked(other, kj::fwd<ErrorFunc>(errorFunc))) {
  return value.subtractChecked(other, kj::fwd<ErrorFunc>(errorFunc));
}

template <typename T, typename U, typename Unit, typename ErrorFunc>
inline auto subtractChecked(Quantity<T, Unit> value, Quantity<U, Unit> other, ErrorFunc&& errorFunc)
    -> Quantity<decltype(subtractChecked(T(), U(), kj::fwd<ErrorFunc>(errorFunc))), Unit> {
  return subtractChecked(value / unit<Quantity<T, Unit>>(),
                         other / unit<Quantity<U, Unit>>(),
                         kj::fwd<ErrorFunc>(errorFunc))
      * unit<Quantity<T, Unit>>();
}

template <uint64_t maxN, typename T, typename Other>
inline auto trySubtract(Bounded<maxN, T> value, Other other)
    -> decltype(value.trySubtract(other)) {
  return value.trySubtract(other);
}

template <typename T, typename U, typename Unit>
inline auto trySubtract(Quantity<T, Unit> value, Quantity<U, Unit> other)
    -> Maybe<Quantity<decltype(subtractChecked(T(), U(), int())), Unit>> {
  return trySubtract(value / unit<Quantity<T, Unit>>(),
                     other / unit<Quantity<U, Unit>>())
      .map([](decltype(subtractChecked(T(), U(), int())) x) {
    return x * unit<Quantity<T, Unit>>();
  });
}

template <uint64_t aN, uint64_t bN, typename A, typename B>
inline constexpr Bounded<kj::min(aN, bN), WiderType<A, B>>
min(Bounded<aN, A> a, Bounded<bN, B> b) {
  return Bounded<kj::min(aN, bN), WiderType<A, B>>(kj::min(a.unwrap(), b.unwrap()), unsafe);
}
template <uint64_t aN, uint64_t bN, typename A, typename B>
inline constexpr Bounded<kj::max(aN, bN), WiderType<A, B>>
max(Bounded<aN, A> a, Bounded<bN, B> b) {
  return Bounded<kj::max(aN, bN), WiderType<A, B>>(kj::max(a.unwrap(), b.unwrap()), unsafe);
}
# 928 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h" 3
template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<((boundedAdd<maxN, cvalue>())), decltype(T() + uint())> operator +( Bounded<maxN, T> value, BoundedConst<cvalue>) { return Bounded<((boundedAdd<maxN, cvalue>())), decltype(T() + uint())>(value.unwrap() + cvalue, unsafe); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<((boundedAdd<maxN, cvalue>())), decltype(uint() + T())> operator +( BoundedConst<cvalue>, Bounded<maxN, T> value) { return Bounded<((boundedAdd<maxN, cvalue>())), decltype(uint() + T())>(cvalue + value.unwrap(), unsafe); }

template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<((boundedMul<maxN, cvalue>())), decltype(T() * uint())> operator *( Bounded<maxN, T> value, BoundedConst<cvalue>) { return Bounded<((boundedMul<maxN, cvalue>())), decltype(T() * uint())>(value.unwrap() * cvalue, unsafe); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<((boundedAdd<maxN, cvalue>())), decltype(uint() * T())> operator *( BoundedConst<cvalue>, Bounded<maxN, T> value) { return Bounded<((boundedAdd<maxN, cvalue>())), decltype(uint() * T())>(cvalue * value.unwrap(), unsafe); }

template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<(maxN / cvalue), decltype(T() / uint())> operator /( Bounded<maxN, T> value, BoundedConst<cvalue>) { return Bounded<(maxN / cvalue), decltype(T() / uint())>(value.unwrap() / cvalue, unsafe); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<(cvalue), decltype(uint() / T())> operator /( BoundedConst<cvalue>, Bounded<maxN, T> value) { return Bounded<(cvalue), decltype(uint() / T())>(cvalue / value.unwrap(), unsafe); }

template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<(cvalue - 1), decltype(T() % uint())> operator %( Bounded<maxN, T> value, BoundedConst<cvalue>) { return Bounded<(cvalue - 1), decltype(T() % uint())>(value.unwrap() % cvalue, unsafe); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<(maxN - 1), decltype(uint() % T())> operator %( BoundedConst<cvalue>, Bounded<maxN, T> value) { return Bounded<(maxN - 1), decltype(uint() % T())>(cvalue % value.unwrap(), unsafe); }

template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<((boundedLShift<maxN, cvalue>())), decltype(T() << uint())> operator <<( Bounded<maxN, T> value, BoundedConst<cvalue>) { return Bounded<((boundedLShift<maxN, cvalue>())), decltype(T() << uint())>(value.unwrap() << cvalue, unsafe); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<((boundedLShift<cvalue, maxN>())), decltype(uint() << T())> operator <<( BoundedConst<cvalue>, Bounded<maxN, T> value) { return Bounded<((boundedLShift<cvalue, maxN>())), decltype(uint() << T())>(cvalue << value.unwrap(), unsafe); }

template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<(maxN >> cvalue), decltype(T() >> uint())> operator >>( Bounded<maxN, T> value, BoundedConst<cvalue>) { return Bounded<(maxN >> cvalue), decltype(T() >> uint())>(value.unwrap() >> cvalue, unsafe); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<(cvalue >> maxN), decltype(uint() >> T())> operator >>( BoundedConst<cvalue>, Bounded<maxN, T> value) { return Bounded<(cvalue >> maxN), decltype(uint() >> T())>(cvalue >> value.unwrap(), unsafe); }

template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<(maxValueForBits<bitCount<maxN>()>() & cvalue), decltype(T() & uint())> operator &( Bounded<maxN, T> value, BoundedConst<cvalue>) { return Bounded<(maxValueForBits<bitCount<maxN>()>() & cvalue), decltype(T() & uint())>(value.unwrap() & cvalue, unsafe); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<(maxValueForBits<bitCount<maxN>()>() & cvalue), decltype(uint() & T())> operator &( BoundedConst<cvalue>, Bounded<maxN, T> value) { return Bounded<(maxValueForBits<bitCount<maxN>()>() & cvalue), decltype(uint() & T())>(cvalue & value.unwrap(), unsafe); }

template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<(maxN | cvalue), decltype(T() | uint())> operator |( Bounded<maxN, T> value, BoundedConst<cvalue>) { return Bounded<(maxN | cvalue), decltype(T() | uint())>(value.unwrap() | cvalue, unsafe); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr Bounded<(maxN | cvalue), decltype(uint() | T())> operator |( BoundedConst<cvalue>, Bounded<maxN, T> value) { return Bounded<(maxN | cvalue), decltype(uint() | T())>(cvalue | value.unwrap(), unsafe); }

template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator ==(Bounded<maxN, T> value, BoundedConst<cvalue>) { return value.unwrap() == cvalue; } template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator ==(BoundedConst<cvalue>, Bounded<maxN, T> value) { return cvalue == value.unwrap(); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator !=(Bounded<maxN, T> value, BoundedConst<cvalue>) { return value.unwrap() != cvalue; } template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator !=(BoundedConst<cvalue>, Bounded<maxN, T> value) { return cvalue != value.unwrap(); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator <(Bounded<maxN, T> value, BoundedConst<cvalue>) { return value.unwrap() < cvalue; } template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator <(BoundedConst<cvalue>, Bounded<maxN, T> value) { return cvalue < value.unwrap(); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator >(Bounded<maxN, T> value, BoundedConst<cvalue>) { return value.unwrap() > cvalue; } template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator >(BoundedConst<cvalue>, Bounded<maxN, T> value) { return cvalue > value.unwrap(); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator <=(Bounded<maxN, T> value, BoundedConst<cvalue>) { return value.unwrap() <= cvalue; } template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator <=(BoundedConst<cvalue>, Bounded<maxN, T> value) { return cvalue <= value.unwrap(); }
template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator >=(Bounded<maxN, T> value, BoundedConst<cvalue>) { return value.unwrap() >= cvalue; } template <uint64_t maxN, uint cvalue, typename T> inline constexpr bool operator >=(BoundedConst<cvalue>, Bounded<maxN, T> value) { return cvalue >= value.unwrap(); }





template <uint64_t maxN, uint cvalue, typename T>
inline constexpr Bounded<cvalue, decltype(uint() - T())>
    operator-(BoundedConst<cvalue>, Bounded<maxN, T> value) {






  static_assert(cvalue >= maxN, "possible underflow detected");
  return Bounded<cvalue, decltype(uint() - T())>(cvalue - value.unwrap(), unsafe);
}

template <uint64_t aN, uint b, typename A>
inline constexpr Bounded<kj::min(aN, b), A> min(Bounded<aN, A> a, BoundedConst<b>) {
  return Bounded<kj::min(aN, b), A>(kj::min(b, a.unwrap()), unsafe);
}
template <uint64_t aN, uint b, typename A>
inline constexpr Bounded<kj::min(aN, b), A> min(BoundedConst<b>, Bounded<aN, A> a) {
  return Bounded<kj::min(aN, b), A>(kj::min(a.unwrap(), b), unsafe);
}
template <uint64_t aN, uint b, typename A>
inline constexpr Bounded<kj::max(aN, b), A> max(Bounded<aN, A> a, BoundedConst<b>) {
  return Bounded<kj::max(aN, b), A>(kj::max(b, a.unwrap()), unsafe);
}
template <uint64_t aN, uint b, typename A>
inline constexpr Bounded<kj::max(aN, b), A> max(BoundedConst<b>, Bounded<aN, A> a) {
  return Bounded<kj::max(aN, b), A>(kj::max(a.unwrap(), b), unsafe);
}







template <uint64_t maxN, typename T>
class SafeUnwrapper {
public:
  inline explicit constexpr SafeUnwrapper(Bounded<maxN, T> value): value(value.unwrap()) {}

  template <typename U, typename = EnableIf<isIntegral<U>()>>
  inline constexpr operator U() const {
    static_assert(maxN <= U(maxValue), "possible truncation detected");
    return value;
  }

  inline constexpr operator bool() const {
    static_assert(maxN <= 1, "possible truncation detected");
    return value;
  }

private:
  T value;
};

template <uint64_t maxN, typename T>
inline constexpr SafeUnwrapper<maxN, T> unbound(Bounded<maxN, T> bounded) {


  return SafeUnwrapper<maxN, T>(bounded);
}

template <uint64_t value>
class SafeConstUnwrapper {
public:
  template <typename T, typename = EnableIf<isIntegral<T>()>>
  inline constexpr operator T() const {
    static_assert(value <= T(maxValue), "this operation will truncate");
    return value;
  }

  inline constexpr operator bool() const {
    static_assert(value <= 1, "this operation will truncate");
    return value;
  }
};

template <uint value>
inline constexpr SafeConstUnwrapper<value> unbound(BoundedConst<value>) {
  return SafeConstUnwrapper<value>();
}

template <typename T, typename U>
inline constexpr T unboundAs(U value) {
  return unbound(value);
}

template <uint64_t requestedMax, uint64_t maxN, typename T>
inline constexpr T unboundMax(Bounded<maxN, T> value) {

  static_assert(maxN <= requestedMax, "possible overflow detected");
  return value.unwrap();
}

template <uint64_t requestedMax, uint value>
inline constexpr uint unboundMax(BoundedConst<value>) {

  static_assert(value <= requestedMax, "overflow detected");
  return value;
}

template <uint bits, typename T>
inline constexpr auto unboundMaxBits(T value) ->
    decltype(unboundMax<maxValueForBits<bits>()>(value)) {

  return unboundMax<maxValueForBits<bits>()>(value);
}
# 1090 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/units.h" 3
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator +(T a, SafeUnwrapper<maxN, U> b) -> decltype(a + (T)b) { return a + (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator +(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b + a) { return (AtLeastUInt<sizeof(T)*8>)b + a; } template <uint64_t value, typename T> inline constexpr auto operator +(T a, SafeConstUnwrapper<value> b) -> decltype(a + (T)b) { return a + (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator +(SafeConstUnwrapper<value> b, T a) -> decltype((T)b + a) { return (AtLeastUInt<sizeof(T)*8>)b + a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator -(T a, SafeUnwrapper<maxN, U> b) -> decltype(a - (T)b) { return a - (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator -(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b - a) { return (AtLeastUInt<sizeof(T)*8>)b - a; } template <uint64_t value, typename T> inline constexpr auto operator -(T a, SafeConstUnwrapper<value> b) -> decltype(a - (T)b) { return a - (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator -(SafeConstUnwrapper<value> b, T a) -> decltype((T)b - a) { return (AtLeastUInt<sizeof(T)*8>)b - a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator *(T a, SafeUnwrapper<maxN, U> b) -> decltype(a * (T)b) { return a * (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator *(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b * a) { return (AtLeastUInt<sizeof(T)*8>)b * a; } template <uint64_t value, typename T> inline constexpr auto operator *(T a, SafeConstUnwrapper<value> b) -> decltype(a * (T)b) { return a * (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator *(SafeConstUnwrapper<value> b, T a) -> decltype((T)b * a) { return (AtLeastUInt<sizeof(T)*8>)b * a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator /(T a, SafeUnwrapper<maxN, U> b) -> decltype(a / (T)b) { return a / (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator /(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b / a) { return (AtLeastUInt<sizeof(T)*8>)b / a; } template <uint64_t value, typename T> inline constexpr auto operator /(T a, SafeConstUnwrapper<value> b) -> decltype(a / (T)b) { return a / (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator /(SafeConstUnwrapper<value> b, T a) -> decltype((T)b / a) { return (AtLeastUInt<sizeof(T)*8>)b / a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator %(T a, SafeUnwrapper<maxN, U> b) -> decltype(a % (T)b) { return a % (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator %(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b % a) { return (AtLeastUInt<sizeof(T)*8>)b % a; } template <uint64_t value, typename T> inline constexpr auto operator %(T a, SafeConstUnwrapper<value> b) -> decltype(a % (T)b) { return a % (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator %(SafeConstUnwrapper<value> b, T a) -> decltype((T)b % a) { return (AtLeastUInt<sizeof(T)*8>)b % a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator <<(T a, SafeUnwrapper<maxN, U> b) -> decltype(a << (T)b) { return a << (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator <<(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b << a) { return (AtLeastUInt<sizeof(T)*8>)b << a; } template <uint64_t value, typename T> inline constexpr auto operator <<(T a, SafeConstUnwrapper<value> b) -> decltype(a << (T)b) { return a << (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator <<(SafeConstUnwrapper<value> b, T a) -> decltype((T)b << a) { return (AtLeastUInt<sizeof(T)*8>)b << a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator >>(T a, SafeUnwrapper<maxN, U> b) -> decltype(a >> (T)b) { return a >> (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator >>(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b >> a) { return (AtLeastUInt<sizeof(T)*8>)b >> a; } template <uint64_t value, typename T> inline constexpr auto operator >>(T a, SafeConstUnwrapper<value> b) -> decltype(a >> (T)b) { return a >> (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator >>(SafeConstUnwrapper<value> b, T a) -> decltype((T)b >> a) { return (AtLeastUInt<sizeof(T)*8>)b >> a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator &(T a, SafeUnwrapper<maxN, U> b) -> decltype(a & (T)b) { return a & (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator &(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b & a) { return (AtLeastUInt<sizeof(T)*8>)b & a; } template <uint64_t value, typename T> inline constexpr auto operator &(T a, SafeConstUnwrapper<value> b) -> decltype(a & (T)b) { return a & (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator &(SafeConstUnwrapper<value> b, T a) -> decltype((T)b & a) { return (AtLeastUInt<sizeof(T)*8>)b & a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator |(T a, SafeUnwrapper<maxN, U> b) -> decltype(a | (T)b) { return a | (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator |(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b | a) { return (AtLeastUInt<sizeof(T)*8>)b | a; } template <uint64_t value, typename T> inline constexpr auto operator |(T a, SafeConstUnwrapper<value> b) -> decltype(a | (T)b) { return a | (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator |(SafeConstUnwrapper<value> b, T a) -> decltype((T)b | a) { return (AtLeastUInt<sizeof(T)*8>)b | a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator ==(T a, SafeUnwrapper<maxN, U> b) -> decltype(a == (T)b) { return a == (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator ==(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b == a) { return (AtLeastUInt<sizeof(T)*8>)b == a; } template <uint64_t value, typename T> inline constexpr auto operator ==(T a, SafeConstUnwrapper<value> b) -> decltype(a == (T)b) { return a == (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator ==(SafeConstUnwrapper<value> b, T a) -> decltype((T)b == a) { return (AtLeastUInt<sizeof(T)*8>)b == a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator !=(T a, SafeUnwrapper<maxN, U> b) -> decltype(a != (T)b) { return a != (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator !=(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b != a) { return (AtLeastUInt<sizeof(T)*8>)b != a; } template <uint64_t value, typename T> inline constexpr auto operator !=(T a, SafeConstUnwrapper<value> b) -> decltype(a != (T)b) { return a != (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator !=(SafeConstUnwrapper<value> b, T a) -> decltype((T)b != a) { return (AtLeastUInt<sizeof(T)*8>)b != a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator <=(T a, SafeUnwrapper<maxN, U> b) -> decltype(a <= (T)b) { return a <= (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator <=(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b <= a) { return (AtLeastUInt<sizeof(T)*8>)b <= a; } template <uint64_t value, typename T> inline constexpr auto operator <=(T a, SafeConstUnwrapper<value> b) -> decltype(a <= (T)b) { return a <= (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator <=(SafeConstUnwrapper<value> b, T a) -> decltype((T)b <= a) { return (AtLeastUInt<sizeof(T)*8>)b <= a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator >=(T a, SafeUnwrapper<maxN, U> b) -> decltype(a >= (T)b) { return a >= (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator >=(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b >= a) { return (AtLeastUInt<sizeof(T)*8>)b >= a; } template <uint64_t value, typename T> inline constexpr auto operator >=(T a, SafeConstUnwrapper<value> b) -> decltype(a >= (T)b) { return a >= (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator >=(SafeConstUnwrapper<value> b, T a) -> decltype((T)b >= a) { return (AtLeastUInt<sizeof(T)*8>)b >= a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator <(T a, SafeUnwrapper<maxN, U> b) -> decltype(a < (T)b) { return a < (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator <(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b < a) { return (AtLeastUInt<sizeof(T)*8>)b < a; } template <uint64_t value, typename T> inline constexpr auto operator <(T a, SafeConstUnwrapper<value> b) -> decltype(a < (T)b) { return a < (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator <(SafeConstUnwrapper<value> b, T a) -> decltype((T)b < a) { return (AtLeastUInt<sizeof(T)*8>)b < a; }
template <uint64_t maxN, typename T, typename U> inline constexpr auto operator >(T a, SafeUnwrapper<maxN, U> b) -> decltype(a > (T)b) { return a > (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t maxN, typename T, typename U> inline constexpr auto operator >(SafeUnwrapper<maxN, U> b, T a) -> decltype((T)b > a) { return (AtLeastUInt<sizeof(T)*8>)b > a; } template <uint64_t value, typename T> inline constexpr auto operator >(T a, SafeConstUnwrapper<value> b) -> decltype(a > (T)b) { return a > (AtLeastUInt<sizeof(T)*8>)b; } template <uint64_t value, typename T> inline constexpr auto operator >(SafeConstUnwrapper<value> b, T a) -> decltype((T)b > a) { return (AtLeastUInt<sizeof(T)*8>)b > a; }





template <uint64_t maxN, typename T>
class Range<Bounded<maxN, T>> {
public:
  inline constexpr Range(Bounded<maxN, T> begin, Bounded<maxN, T> end)
      : inner(unbound(begin), unbound(end)) {}
  inline explicit constexpr Range(Bounded<maxN, T> end)
      : inner(unbound(end)) {}

  class Iterator {
  public:
    Iterator() = default;
    inline explicit Iterator(typename Range<T>::Iterator inner): inner(inner) {}

    inline Bounded<maxN, T> operator* () const { return Bounded<maxN, T>(*inner, unsafe); }
    inline Iterator& operator++() { ++inner; return *this; }

    inline bool operator==(const Iterator& other) const { return inner == other.inner; }
    inline bool operator!=(const Iterator& other) const { return inner != other.inner; }

  private:
    typename Range<T>::Iterator inner;
  };

  inline Iterator begin() const { return Iterator(inner.begin()); }
  inline Iterator end() const { return Iterator(inner.end()); }

private:
  Range<T> inner;
};

template <typename T, typename U>
class Range<Quantity<T, U>> {
public:
  inline constexpr Range(Quantity<T, U> begin, Quantity<T, U> end)
      : inner(begin / unit<Quantity<T, U>>(), end / unit<Quantity<T, U>>()) {}
  inline explicit constexpr Range(Quantity<T, U> end)
      : inner(end / unit<Quantity<T, U>>()) {}

  class Iterator {
  public:
    Iterator() = default;
    inline explicit Iterator(typename Range<T>::Iterator inner): inner(inner) {}

    inline Quantity<T, U> operator* () const { return *inner * unit<Quantity<T, U>>(); }
    inline Iterator& operator++() { ++inner; return *this; }

    inline bool operator==(const Iterator& other) const { return inner == other.inner; }
    inline bool operator!=(const Iterator& other) const { return inner != other.inner; }

  private:
    typename Range<T>::Iterator inner;
  };

  inline Iterator begin() const { return Iterator(inner.begin()); }
  inline Iterator end() const { return Iterator(inner.end()); }

private:
  Range<T> inner;
};

template <uint value>
inline constexpr Range<Bounded<value, uint>> zeroTo(BoundedConst<value> end) {
  return Range<Bounded<value, uint>>(end);
}

template <uint value, typename Unit>
inline constexpr Range<Quantity<Bounded<value, uint>, Unit>>
    zeroTo(Quantity<BoundedConst<value>, Unit> end) {
  return Range<Quantity<Bounded<value, uint>, Unit>>(end);
}

}


# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/time.h" 2




# 29 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/time.h"

# 29 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/time.h" 3


namespace kj {
namespace _ {

class NanosecondLabel;
class TimeLabel;
class DateLabel;

}

using Duration = Quantity<int64_t, _::NanosecondLabel>;


constexpr Duration NANOSECONDS = unit<Duration>();
constexpr Duration MICROSECONDS = 1000 * NANOSECONDS;
constexpr Duration MILLISECONDS = 1000 * MICROSECONDS;
constexpr Duration SECONDS = 1000 * MILLISECONDS;
constexpr Duration MINUTES = 60 * SECONDS;
constexpr Duration HOURS = 60 * MINUTES;
constexpr Duration DAYS = 24 * HOURS;

using TimePoint = Absolute<Duration, _::TimeLabel>;




using Date = Absolute<Duration, _::DateLabel>;


kj::String operator*(::kj::_::Stringifier, TimePoint);
kj::String operator*(::kj::_::Stringifier, Date);
kj::String operator*(::kj::_::Stringifier, Duration);

constexpr Date UNIX_EPOCH = origin<Date>();


class Clock {

public:
  virtual Date now() const = 0;
};

class MonotonicClock {






public:
  virtual TimePoint now() const = 0;
};

const Clock& nullClock();



const Clock& systemCoarseCalendarClock();
const Clock& systemPreciseCalendarClock();
# 102 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/time.h" 3
const MonotonicClock& systemCoarseMonotonicClock();
const MonotonicClock& systemPreciseMonotonicClock();
# 114 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/time.h" 3
}


# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/timer.h" 2



# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/timer.h"

# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/timer.h" 3


namespace kj {

class Timer: public MonotonicClock {
# 45 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/timer.h" 3
public:
  virtual TimePoint now() const = 0;




  virtual Promise<void> atTime(TimePoint time) = 0;


  virtual Promise<void> afterDelay(Duration delay) = 0;


  template <typename T>
  Promise<T> timeoutAt(TimePoint time, Promise<T>&& promise) __attribute__((warn_unused_result));




  template <typename T>
  Promise<T> timeoutAfter(Duration delay, Promise<T>&& promise) __attribute__((warn_unused_result));




private:
  static kj::Exception makeTimeoutException();
};

class TimerImpl final: public Timer {



public:
  TimerImpl(TimePoint startTime);
  ~TimerImpl() noexcept(false);

  Maybe<TimePoint> nextEvent();



  Maybe<uint64_t> timeoutToNextEvent(TimePoint start, Duration unit, uint64_t max);
# 101 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/timer.h" 3
  void advanceTo(TimePoint newTime);



  TimePoint now() const override;
  Promise<void> atTime(TimePoint time) override;
  Promise<void> afterDelay(Duration delay) override;

private:
  struct Impl;
  class TimerPromiseAdapter;
  TimePoint time;
  Own<Impl> impl;
};




template <typename T>
Promise<T> Timer::timeoutAt(TimePoint time, Promise<T>&& promise) {
  return promise.exclusiveJoin(atTime(time).then([]() -> kj::Promise<T> {
    return makeTimeoutException();
  }));
}

template <typename T>
Promise<T> Timer::timeoutAfter(Duration delay, Promise<T>&& promise) {
  return promise.exclusiveJoin(afterDelay(delay).then([]() -> kj::Promise<T> {
    return makeTimeoutException();
  }));
}

inline TimePoint TimerImpl::now() const { return time; }

}


# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 2


# 29 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h"

# 29 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3


struct sockaddr;

namespace kj {





class UnixEventPort;


class AutoCloseFd;
class NetworkAddress;
class AsyncOutputStream;
class AsyncIoStream;




class AsyncInputStream {


public:
  virtual Promise<size_t> read(void* buffer, size_t minBytes, size_t maxBytes);
  virtual Promise<size_t> tryRead(void* buffer, size_t minBytes, size_t maxBytes) = 0;

  Promise<void> read(void* buffer, size_t bytes);

  virtual Maybe<uint64_t> tryGetLength();







  virtual Promise<uint64_t> pumpTo(
      AsyncOutputStream& output, uint64_t amount = kj::maxValue);
# 80 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  Promise<Array<byte>> readAllBytes(uint64_t limit = kj::maxValue);
  Promise<String> readAllText(uint64_t limit = kj::maxValue);





};

class AsyncOutputStream {


public:
  virtual Promise<void> write(const void* buffer, size_t size) __attribute__((warn_unused_result)) = 0;
  virtual Promise<void> write(ArrayPtr<const ArrayPtr<const byte>> pieces)
      __attribute__((warn_unused_result)) = 0;

  virtual Maybe<Promise<uint64_t>> tryPumpFrom(
      AsyncInputStream& input, uint64_t amount = kj::maxValue);
# 108 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  virtual Promise<void> whenWriteDisconnected() = 0;
# 121 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
};

class AsyncIoStream: public AsyncInputStream, public AsyncOutputStream {


public:
  virtual void shutdownWrite() = 0;


  virtual void abortRead() {}



  virtual void getsockopt(int level, int option, void* value, uint* length);
  virtual void setsockopt(int level, int option, const void* value, uint length);




  virtual void getsockname(struct sockaddr* addr, uint* length);
  virtual void getpeername(struct sockaddr* addr, uint* length);
# 150 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  virtual kj::Maybe<int> getFd() const { return nullptr; }


};

class AsyncCapabilityStream: public AsyncIoStream {
# 182 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
public:
  virtual Promise<void> writeWithFds(ArrayPtr<const byte> data,
                                     ArrayPtr<const ArrayPtr<const byte>> moreData,
                                     ArrayPtr<const int> fds) = 0;
  Promise<void> writeWithFds(ArrayPtr<const byte> data,
                             ArrayPtr<const ArrayPtr<const byte>> moreData,
                             ArrayPtr<const AutoCloseFd> fds);






  struct ReadResult {
    size_t byteCount;
    size_t capCount;
  };

  virtual Promise<ReadResult> tryReadWithFds(void* buffer, size_t minBytes, size_t maxBytes,
                                             AutoCloseFd* fdBuffer, size_t maxFds) = 0;
# 210 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  virtual Promise<void> writeWithStreams(ArrayPtr<const byte> data,
                                         ArrayPtr<const ArrayPtr<const byte>> moreData,
                                         Array<Own<AsyncCapabilityStream>> streams) = 0;
  virtual Promise<ReadResult> tryReadWithStreams(
      void* buffer, size_t minBytes, size_t maxBytes,
      Own<AsyncCapabilityStream>* streamBuffer, size_t maxStreams) = 0;
# 225 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  Promise<Own<AsyncCapabilityStream>> receiveStream();
  Promise<Maybe<Own<AsyncCapabilityStream>>> tryReceiveStream();
  Promise<void> sendStream(Own<AsyncCapabilityStream> stream);


  Promise<AutoCloseFd> receiveFd();
  Promise<Maybe<AutoCloseFd>> tryReceiveFd();
  Promise<void> sendFd(int fd);

};

struct OneWayPipe {


  Own<AsyncInputStream> in;
  Own<AsyncOutputStream> out;
};

OneWayPipe newOneWayPipe(kj::Maybe<uint64_t> expectedLength = nullptr);






struct TwoWayPipe {



  Own<AsyncIoStream> ends[2];
};

TwoWayPipe newTwoWayPipe();



struct CapabilityPipe {


  Own<AsyncCapabilityStream> ends[2];
};

CapabilityPipe newCapabilityPipe();
# 277 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
struct Tee {


  Own<AsyncInputStream> branches[2];
};

Tee newTee(Own<AsyncInputStream> input, uint64_t limit = kj::maxValue);
# 301 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
Own<AsyncOutputStream> newPromisedStream(Promise<Own<AsyncOutputStream>> promise);
Own<AsyncIoStream> newPromisedStream(Promise<Own<AsyncIoStream>> promise);






class PeerIdentity {


public:
  virtual kj::String toString() = 0;



};

struct AuthenticatedStream {



  Own<AsyncIoStream> stream;


  Own<PeerIdentity> peerIdentity;
# 341 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
};

class NetworkPeerIdentity: public PeerIdentity {





public:
  virtual NetworkAddress& getAddress() = 0;




  static kj::Own<NetworkPeerIdentity> newInstance(kj::Own<NetworkAddress> addr);

};

class LocalPeerIdentity: public PeerIdentity {




public:
  struct Credentials {
    kj::Maybe<int> pid;
    kj::Maybe<uint> uid;



  };

  virtual Credentials getCredentials() = 0;
# 387 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  static kj::Own<LocalPeerIdentity> newInstance(Credentials creds);

};

class UnknownPeerIdentity: public PeerIdentity {
public:
  static kj::Own<UnknownPeerIdentity> newInstance();


};




class ConnectionReceiver {


public:
  virtual Promise<Own<AsyncIoStream>> accept() = 0;


  virtual Promise<AuthenticatedStream> acceptAuthenticated();






  virtual uint getPort() = 0;




  virtual void getsockopt(int level, int option, void* value, uint* length);
  virtual void setsockopt(int level, int option, const void* value, uint length);
  virtual void getsockname(struct sockaddr* addr, uint* length);

};




class AncillaryMessage {



public:
  inline AncillaryMessage(int level, int type, ArrayPtr<const byte> data);
  AncillaryMessage() = default;

  inline int getLevel() const;


  inline int getType() const;


  template <typename T>
  inline Maybe<const T&> as();





  template <typename T>
  inline ArrayPtr<const T> asArray();




private:
  int level;
  int type;
  ArrayPtr<const byte> data;

};

class DatagramReceiver {



public:
  virtual Promise<void> receive() = 0;




  template <typename T>
  struct MaybeTruncated {
    T value;

    bool isTruncated;


  };

  virtual MaybeTruncated<ArrayPtr<const byte>> getContent() = 0;


  virtual MaybeTruncated<ArrayPtr<const AncillaryMessage>> getAncillary() = 0;
# 494 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  virtual NetworkAddress& getSource() = 0;


  struct Capacity {
    size_t content = 8192;



    size_t ancillary = 0;


  };
};

class DatagramPort {
public:
  virtual Promise<size_t> send(const void* buffer, size_t size, NetworkAddress& destination) = 0;
  virtual Promise<size_t> send(ArrayPtr<const ArrayPtr<const byte>> pieces,
                               NetworkAddress& destination) = 0;

  virtual Own<DatagramReceiver> makeReceiver(
      DatagramReceiver::Capacity capacity = DatagramReceiver::Capacity()) = 0;



  virtual uint getPort() = 0;




  virtual void getsockopt(int level, int option, void* value, uint* length);
  virtual void setsockopt(int level, int option, const void* value, uint length);

};




class NetworkAddress {


public:
  virtual Promise<Own<AsyncIoStream>> connect() = 0;




  virtual Promise<AuthenticatedStream> connectAuthenticated();







  virtual Own<ConnectionReceiver> listen() = 0;




  virtual Own<DatagramPort> bindDatagramPort();




  virtual Own<NetworkAddress> clone() = 0;


  virtual String toString() = 0;




};

class Network {







public:
  virtual Promise<Own<NetworkAddress>> parseAddress(StringPtr addr, uint portHint = 0) = 0;
# 590 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  virtual Own<NetworkAddress> getSockaddr(const void* sockaddr, uint len) = 0;


  virtual Own<Network> restrictPeers(
      kj::ArrayPtr<const kj::StringPtr> allow,
      kj::ArrayPtr<const kj::StringPtr> deny = nullptr) __attribute__((warn_unused_result)) = 0;
# 653 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
};




class AsyncIoProvider {






public:
  virtual OneWayPipe newOneWayPipe() = 0;



  virtual TwoWayPipe newTwoWayPipe() = 0;



  virtual CapabilityPipe newCapabilityPipe();






  virtual Network& getNetwork() = 0;
# 702 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  struct PipeThread {






    Own<Thread> thread;
    Own<AsyncIoStream> pipe;
  };

  virtual PipeThread newPipeThread(
      Function<void(AsyncIoProvider&, AsyncIoStream&, WaitScope&)> startFunc) = 0;
# 723 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  virtual Timer& getTimer() = 0;






};

class LowLevelAsyncIoProvider {
# 745 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
public:
  enum Flags {


    TAKE_OWNERSHIP = 1 << 0,







    ALREADY_CLOEXEC = 1 << 1,






    ALREADY_NONBLOCK = 1 << 2







  };
# 781 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
  typedef int Fd;
  typedef AutoCloseFd OwnFd;



  virtual Own<AsyncInputStream> wrapInputFd(Fd fd, uint flags = 0) = 0;




  virtual Own<AsyncOutputStream> wrapOutputFd(Fd fd, uint flags = 0) = 0;




  virtual Own<AsyncIoStream> wrapSocketFd(Fd fd, uint flags = 0) = 0;





  virtual Own<AsyncCapabilityStream> wrapUnixSocketFd(Fd fd, uint flags = 0);







  virtual Promise<Own<AsyncIoStream>> wrapConnectingSocketFd(
      Fd fd, const struct sockaddr* addr, uint addrlen, uint flags = 0) = 0;





  class NetworkFilter {
  public:
    virtual bool shouldAllow(const struct sockaddr* addr, uint addrlen) = 0;



    static NetworkFilter& getAllAllowed();
  };

  virtual Own<ConnectionReceiver> wrapListenSocketFd(
      Fd fd, NetworkFilter& filter, uint flags = 0) = 0;
  inline Own<ConnectionReceiver> wrapListenSocketFd(Fd fd, uint flags = 0) {
    return wrapListenSocketFd(fd, NetworkFilter::getAllAllowed(), flags);
  }





  virtual Own<DatagramPort> wrapDatagramSocketFd(Fd fd, NetworkFilter& filter, uint flags = 0);
  inline Own<DatagramPort> wrapDatagramSocketFd(Fd fd, uint flags = 0) {
    return wrapDatagramSocketFd(fd, NetworkFilter::getAllAllowed(), flags);
  }

  virtual Timer& getTimer() = 0;







  Own<AsyncInputStream> wrapInputFd(OwnFd&& fd, uint flags = 0);
  Own<AsyncOutputStream> wrapOutputFd(OwnFd&& fd, uint flags = 0);
  Own<AsyncIoStream> wrapSocketFd(OwnFd&& fd, uint flags = 0);

  Own<AsyncCapabilityStream> wrapUnixSocketFd(OwnFd&& fd, uint flags = 0);

  Promise<Own<AsyncIoStream>> wrapConnectingSocketFd(
      OwnFd&& fd, const struct sockaddr* addr, uint addrlen, uint flags = 0);
  Own<ConnectionReceiver> wrapListenSocketFd(
      OwnFd&& fd, NetworkFilter& filter, uint flags = 0);
  Own<ConnectionReceiver> wrapListenSocketFd(OwnFd&& fd, uint flags = 0);
  Own<DatagramPort> wrapDatagramSocketFd(OwnFd&& fd, NetworkFilter& filter, uint flags = 0);
  Own<DatagramPort> wrapDatagramSocketFd(OwnFd&& fd, uint flags = 0);


};

Own<AsyncIoProvider> newAsyncIoProvider(LowLevelAsyncIoProvider& lowLevel);


struct AsyncIoContext {
  Own<LowLevelAsyncIoProvider> lowLevelProvider;
  Own<AsyncIoProvider> provider;
  WaitScope& waitScope;




  UnixEventPort& unixEventPort;



};

AsyncIoContext setupAsyncIo();
# 914 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
class CapabilityStreamConnectionReceiver final: public ConnectionReceiver {



public:
  CapabilityStreamConnectionReceiver(AsyncCapabilityStream& inner)
      : inner(inner) {}

  Promise<Own<AsyncIoStream>> accept() override;
  uint getPort() override;

  Promise<AuthenticatedStream> acceptAuthenticated() override;




private:
  AsyncCapabilityStream& inner;
};

class CapabilityStreamNetworkAddress final: public NetworkAddress {
# 948 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/async-io.h" 3
public:
  CapabilityStreamNetworkAddress(kj::Maybe<AsyncIoProvider&> provider, AsyncCapabilityStream& inner)
      : provider(provider), inner(inner) {}

  Promise<Own<AsyncIoStream>> connect() override;
  Own<ConnectionReceiver> listen() override;

  Own<NetworkAddress> clone() override;
  String toString() override;

  Promise<AuthenticatedStream> connectAuthenticated() override;




private:
  kj::Maybe<AsyncIoProvider&> provider;
  AsyncCapabilityStream& inner;
};




inline AncillaryMessage::AncillaryMessage(
    int level, int type, ArrayPtr<const byte> data)
    : level(level), type(type), data(data) {}

inline int AncillaryMessage::getLevel() const { return level; }
inline int AncillaryMessage::getType() const { return type; }

template <typename T>
inline Maybe<const T&> AncillaryMessage::as() {
  if (data.size() >= sizeof(T)) {
    return *reinterpret_cast<const T*>(data.begin());
  } else {
    return nullptr;
  }
}

template <typename T>
inline ArrayPtr<const T> AncillaryMessage::asArray() {
  return arrayPtr(reinterpret_cast<const T*>(data.begin()), data.size() / sizeof(T));
}

}


# 42 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 2


# 43 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
namespace kj {
# 82 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
enum class HttpMethod {






GET, HEAD, POST, PUT, DELETE, PATCH, PURGE, OPTIONS, TRACE, COPY, LOCK, MKCOL, MOVE, PROPFIND, PROPPATCH, SEARCH, UNLOCK, ACL, REPORT, MKACTIVITY, CHECKOUT, MERGE, MSEARCH, NOTIFY, SUBSCRIBE, UNSUBSCRIBE,

};

kj::StringPtr 
# 93 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
             operator*(::kj::_::Stringifier, 
# 93 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
             HttpMethod method
# 93 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
             )
# 93 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
                                            ;
kj::Maybe<HttpMethod> tryParseHttpMethod(kj::StringPtr name);

class HttpHeaderTable;

class HttpHeaderId {
# 109 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
public:
  HttpHeaderId() = default;

  inline bool operator==(const HttpHeaderId& other) const { return id == other.id; }
  inline bool operator!=(const HttpHeaderId& other) const { return id != other.id; }
  inline bool operator< (const HttpHeaderId& other) const { return id < other.id; }
  inline bool operator> (const HttpHeaderId& other) const { return id > other.id; }
  inline bool operator<=(const HttpHeaderId& other) const { return id <= other.id; }
  inline bool operator>=(const HttpHeaderId& other) const { return id >= other.id; }

  inline size_t hashCode() const { return id; }



  kj::StringPtr toString() const;

  void requireFrom(const HttpHeaderTable& table) const;
# 163 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  static const HttpHeaderId CONNECTION; static const HttpHeaderId KEEP_ALIVE; static const HttpHeaderId TE; static const HttpHeaderId TRAILER; static const HttpHeaderId UPGRADE; static const HttpHeaderId CONTENT_LENGTH; static const HttpHeaderId TRANSFER_ENCODING; static const HttpHeaderId SEC_WEBSOCKET_KEY; static const HttpHeaderId SEC_WEBSOCKET_VERSION; static const HttpHeaderId SEC_WEBSOCKET_ACCEPT; static const HttpHeaderId SEC_WEBSOCKET_EXTENSIONS; static const HttpHeaderId HOST; static const HttpHeaderId DATE; static const HttpHeaderId LOCATION; static const HttpHeaderId CONTENT_TYPE;;


private:
  const HttpHeaderTable* table;
  uint id;

  inline explicit constexpr HttpHeaderId(const HttpHeaderTable* table, uint id)
      : table(table), id(id) {}
  friend class HttpHeaderTable;
  friend class HttpHeaders;
};

class HttpHeaderTable {
# 198 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  struct IdsByNameMap;

public:
  HttpHeaderTable();


  class Builder {
  public:
    Builder();
    HttpHeaderId add(kj::StringPtr name);
    Own<HttpHeaderTable> build();

    HttpHeaderTable& getFutureTable();







  private:
    kj::Own<HttpHeaderTable> table;
  };

  HttpHeaderTable
# 222 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 (const 
# 222 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
 HttpHeaderTable
# 222 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 &) = delete; 
# 222 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
 HttpHeaderTable
# 222 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 & operator=(const 
# 222 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
 HttpHeaderTable
# 222 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 &) = delete
# 222 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
                                  ;
  ~HttpHeaderTable() noexcept(false);

  uint idCount() const;


  kj::Maybe<HttpHeaderId> stringToId(kj::StringPtr name) const;





  kj::StringPtr idToString(HttpHeaderId id) const;


private:
  kj::Vector<kj::StringPtr> namesById;
  kj::Own<IdsByNameMap> idsByName;
};

class HttpHeaders {






public:
  explicit HttpHeaders(const HttpHeaderTable& table);

  static bool isValidHeaderValue(kj::StringPtr value);
# 263 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  HttpHeaders
# 263 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 (const 
# 263 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
 HttpHeaders
# 263 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 &) = delete; 
# 263 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
 HttpHeaders
# 263 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 & operator=(const 
# 263 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
 HttpHeaders
# 263 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 &) = delete
# 263 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
                              ;
  HttpHeaders(HttpHeaders&&) = default;
  HttpHeaders& operator=(HttpHeaders&&) = default;

  size_t size() const;


  void clear();



  HttpHeaders clone() const;


  HttpHeaders cloneShallow() const;



  bool isWebSocket() const;






  kj::Maybe<kj::StringPtr> get(HttpHeaderId id) const;


  template <typename Func>
  void forEach(Func&& func) const;



  template <typename Func1, typename Func2>
  void forEach(Func1&& func1, Func2&& func2) const;




  void set(HttpHeaderId id, kj::StringPtr value);
  void set(HttpHeaderId id, kj::String&& value);
# 312 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  void add(kj::StringPtr name, kj::StringPtr value);
  void add(kj::StringPtr name, kj::String&& value);
  void add(kj::String&& name, kj::String&& value);
# 325 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  void unset(HttpHeaderId id);





  void takeOwnership(kj::String&& string);
  void takeOwnership(kj::Array<char>&& chars);
  void takeOwnership(HttpHeaders&& otherHeaders);



  struct Request {
    HttpMethod method;
    kj::StringPtr url;
  };
  struct Response {
    uint statusCode;
    kj::StringPtr statusText;
  };

  struct ProtocolError {
# 356 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
    uint statusCode;




    kj::StringPtr statusMessage;


    kj::StringPtr description;


    kj::ArrayPtr<char> rawContent;







  };

  using RequestOrProtocolError = kj::OneOf<Request, ProtocolError>;
  using ResponseOrProtocolError = kj::OneOf<Response, ProtocolError>;

  RequestOrProtocolError tryParseRequest(kj::ArrayPtr<char> content);
  ResponseOrProtocolError tryParseResponse(kj::ArrayPtr<char> content);
# 391 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  bool tryParse(kj::ArrayPtr<char> content);


  kj::String serializeRequest(HttpMethod method, kj::StringPtr url,
                              kj::ArrayPtr<const kj::StringPtr> connectionHeaders = nullptr) const;
  kj::String serializeResponse(uint statusCode, kj::StringPtr statusText,
                               kj::ArrayPtr<const kj::StringPtr> connectionHeaders = nullptr) const;
# 409 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  enum class BuiltinIndicesEnum {

    CONNECTION, KEEP_ALIVE, TE, TRAILER, UPGRADE, CONTENT_LENGTH, TRANSFER_ENCODING, SEC_WEBSOCKET_KEY, SEC_WEBSOCKET_VERSION, SEC_WEBSOCKET_ACCEPT, SEC_WEBSOCKET_EXTENSIONS, HOST, DATE, LOCATION, CONTENT_TYPE,

  };

  struct BuiltinIndices {

    static constexpr uint CONNECTION = static_cast<uint>(BuiltinIndicesEnum::CONNECTION); static constexpr uint KEEP_ALIVE = static_cast<uint>(BuiltinIndicesEnum::KEEP_ALIVE); static constexpr uint TE = static_cast<uint>(BuiltinIndicesEnum::TE); static constexpr uint TRAILER = static_cast<uint>(BuiltinIndicesEnum::TRAILER); static constexpr uint UPGRADE = static_cast<uint>(BuiltinIndicesEnum::UPGRADE); static constexpr uint CONTENT_LENGTH = static_cast<uint>(BuiltinIndicesEnum::CONTENT_LENGTH); static constexpr uint TRANSFER_ENCODING = static_cast<uint>(BuiltinIndicesEnum::TRANSFER_ENCODING); static constexpr uint SEC_WEBSOCKET_KEY = static_cast<uint>(BuiltinIndicesEnum::SEC_WEBSOCKET_KEY); static constexpr uint SEC_WEBSOCKET_VERSION = static_cast<uint>(BuiltinIndicesEnum::SEC_WEBSOCKET_VERSION); static constexpr uint SEC_WEBSOCKET_ACCEPT = static_cast<uint>(BuiltinIndicesEnum::SEC_WEBSOCKET_ACCEPT); static constexpr uint SEC_WEBSOCKET_EXTENSIONS = static_cast<uint>(BuiltinIndicesEnum::SEC_WEBSOCKET_EXTENSIONS); static constexpr uint HOST = static_cast<uint>(BuiltinIndicesEnum::HOST); static constexpr uint DATE = static_cast<uint>(BuiltinIndicesEnum::DATE); static constexpr uint LOCATION = static_cast<uint>(BuiltinIndicesEnum::LOCATION); static constexpr uint CONTENT_TYPE = static_cast<uint>(BuiltinIndicesEnum::CONTENT_TYPE);

  };

  static constexpr uint HEAD_RESPONSE_CONNECTION_HEADERS_COUNT = BuiltinIndices::CONTENT_LENGTH;
  static constexpr uint CONNECTION_HEADERS_COUNT = BuiltinIndices::SEC_WEBSOCKET_KEY;
  static constexpr uint WEBSOCKET_CONNECTION_HEADERS_COUNT = BuiltinIndices::HOST;


  kj::String toString() const;

private:
  const HttpHeaderTable* table;

  kj::Array<kj::StringPtr> indexedHeaders;


  struct Header {
    kj::StringPtr name;
    kj::StringPtr value;
  };
  kj::Vector<Header> unindexedHeaders;

  kj::Vector<kj::Array<char>> ownedStrings;

  void addNoCheck(kj::StringPtr name, kj::StringPtr value);

  kj::StringPtr cloneToOwn(kj::StringPtr str);

  kj::String serialize(kj::ArrayPtr<const char> word1,
                       kj::ArrayPtr<const char> word2,
                       kj::ArrayPtr<const char> word3,
                       kj::ArrayPtr<const kj::StringPtr> connectionHeaders) const;

  bool parseHeaders(char* ptr, char* end);




};

class HttpInputStream {







public:
  struct Request {
    HttpMethod method;
    kj::StringPtr url;
    const HttpHeaders& headers;
    kj::Own<kj::AsyncInputStream> body;
  };
  virtual kj::Promise<Request> readRequest() = 0;





  struct Response {
    uint statusCode;
    kj::StringPtr statusText;
    const HttpHeaders& headers;
    kj::Own<kj::AsyncInputStream> body;
  };
  virtual kj::Promise<Response> readResponse(HttpMethod requestMethod) = 0;
# 494 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  struct Message {
    const HttpHeaders& headers;
    kj::Own<kj::AsyncInputStream> body;
  };
  virtual kj::Promise<Message> readMessage() = 0;







  virtual kj::Promise<bool> awaitNextMessage() = 0;

};

class EntropySource {





public:
  virtual void generate(kj::ArrayPtr<byte> buffer) = 0;
};

class WebSocket {
# 530 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
public:
  virtual kj::Promise<void> send(kj::ArrayPtr<const byte> message) = 0;
  virtual kj::Promise<void> send(kj::ArrayPtr<const char> message) = 0;



  virtual kj::Promise<void> close(uint16_t code, kj::StringPtr reason) = 0;






  virtual kj::Promise<void> disconnect() = 0;




  virtual void abort() = 0;




  virtual kj::Promise<void> whenAborted() = 0;





  struct Close {
    uint16_t code;
    kj::String reason;
  };

  typedef kj::OneOf<kj::String, kj::Array<byte>, Close> Message;

  static constexpr size_t SUGGESTED_MAX_MESSAGE_SIZE = 1u << 20;

  virtual kj::Promise<Message> receive(size_t maxSize = SUGGESTED_MAX_MESSAGE_SIZE) = 0;



  virtual kj::Promise<void> pumpTo(WebSocket& other);
# 581 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  virtual kj::Maybe<kj::Promise<void>> tryPumpFrom(WebSocket& other);




};

class HttpClient {
# 597 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
public:
  struct Response {
    uint statusCode;
    kj::StringPtr statusText;
    const HttpHeaders* headers;
    kj::Own<kj::AsyncInputStream> body;

  };

  struct Request {
    kj::Own<kj::AsyncOutputStream> body;





    kj::Promise<Response> response;

  };

  virtual Request request(HttpMethod method, kj::StringPtr url, const HttpHeaders& headers,
                          kj::Maybe<uint64_t> expectedBodySize = nullptr) = 0;
# 631 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  struct WebSocketResponse {
    uint statusCode;
    kj::StringPtr statusText;
    const HttpHeaders* headers;
    kj::OneOf<kj::Own<kj::AsyncInputStream>, kj::Own<WebSocket>> webSocketOrBody;

  };
  virtual kj::Promise<WebSocketResponse> openWebSocket(
      kj::StringPtr url, const HttpHeaders& headers);





  virtual kj::Promise<kj::Own<kj::AsyncIoStream>> connect(kj::StringPtr host);


};

class HttpService {
# 665 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
public:
  class Response {
  public:
    virtual kj::Own<kj::AsyncOutputStream> send(
        uint statusCode, kj::StringPtr statusText, const HttpHeaders& headers,
        kj::Maybe<uint64_t> expectedBodySize = nullptr) = 0;





    virtual kj::Own<WebSocket> acceptWebSocket(const HttpHeaders& headers) = 0;


    kj::Promise<void> sendError(uint statusCode, kj::StringPtr statusText,
                                const HttpHeaders& headers);
    kj::Promise<void> sendError(uint statusCode, kj::StringPtr statusText,
                                const HttpHeaderTable& headerTable);






  };

  virtual kj::Promise<void> request(
      HttpMethod method, kj::StringPtr url, const HttpHeaders& headers,
      kj::AsyncInputStream& requestBody, Response& response) = 0;
# 705 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  virtual kj::Promise<kj::Own<kj::AsyncIoStream>> connect(kj::StringPtr host);


};

class HttpClientErrorHandler {
public:
  virtual HttpClient::Response handleProtocolError(HttpHeaders::ProtocolError protocolError);
# 725 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  virtual HttpClient::WebSocketResponse handleWebSocketProtocolError(
      HttpHeaders::ProtocolError protocolError);



};

struct HttpClientSettings {
  kj::Duration idleTimeout = 5 * kj::SECONDS;



  kj::Maybe<EntropySource&> entropySource = nullptr;
# 746 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
  kj::Maybe<HttpClientErrorHandler&> errorHandler = nullptr;


};

kj::Own<HttpClient> newHttpClient(kj::Timer& timer, const HttpHeaderTable& responseHeaderTable,
                                  kj::Network& network, kj::Maybe<kj::Network&> tlsNetwork,
                                  HttpClientSettings settings = HttpClientSettings());
# 767 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
kj::Own<HttpClient> newHttpClient(kj::Timer& timer, const HttpHeaderTable& responseHeaderTable,
                                  kj::NetworkAddress& addr,
                                  HttpClientSettings settings = HttpClientSettings());
# 781 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
kj::Own<HttpClient> newHttpClient(const HttpHeaderTable& responseHeaderTable,
                                  kj::AsyncIoStream& stream,
                                  HttpClientSettings settings = HttpClientSettings());
# 794 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
kj::Own<HttpClient> newConcurrencyLimitingHttpClient(
    HttpClient& inner, uint maxConcurrentRequests,
    kj::Function<void(uint runningCount, uint pendingCount)> countChangedCallback);





kj::Own<HttpClient> newHttpClient(HttpService& service);
kj::Own<HttpService> newHttpService(HttpClient& client);


kj::Own<HttpInputStream> newHttpInputStream(
    kj::AsyncInputStream& input, const HttpHeaderTable& headerTable);
# 816 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
kj::Own<WebSocket> newWebSocket(kj::Own<kj::AsyncIoStream> stream,
                                kj::Maybe<EntropySource&> maskEntropySource);
# 830 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
struct WebSocketPipe {
  kj::Own<WebSocket> ends[2];
};

WebSocketPipe newWebSocketPipe();




class HttpServerErrorHandler;
class HttpServerCallbacks;

struct HttpServerSettings {
  kj::Duration headerTimeout = 15 * kj::SECONDS;



  kj::Duration pipelineTimeout = 5 * kj::SECONDS;



  kj::Duration canceledUploadGracePeriod = 1 * kj::SECONDS;
  size_t canceledUploadGraceBytes = 65536;






  kj::Maybe<HttpServerErrorHandler&> errorHandler = nullptr;



  kj::Maybe<HttpServerCallbacks&> callbacks = nullptr;

};

class HttpServerErrorHandler {
public:
  virtual kj::Promise<void> handleClientProtocolError(
      HttpHeaders::ProtocolError protocolError, kj::HttpService::Response& response);
  virtual kj::Promise<void> handleApplicationError(
      kj::Exception exception, kj::Maybe<kj::HttpService::Response&> response);
  virtual kj::Promise<void> handleNoResponse(kj::HttpService::Response& response);
# 899 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
};

class HttpServerCallbacks {
public:
  virtual bool shouldClose() { return false; }






};

class HttpServer final: private kj::TaskSet::ErrorHandler {


public:
  typedef HttpServerSettings Settings;
  typedef kj::Function<kj::Own<HttpService>(kj::AsyncIoStream&)> HttpServiceFactory;

  HttpServer(kj::Timer& timer, const HttpHeaderTable& requestHeaderTable, HttpService& service,
             Settings settings = Settings());




  HttpServer(kj::Timer& timer, const HttpHeaderTable& requestHeaderTable,
             HttpServiceFactory serviceFactory, Settings settings = Settings());




  kj::Promise<void> drain();




  kj::Promise<void> listenHttp(kj::ConnectionReceiver& port);






  kj::Promise<void> listenHttp(kj::Own<kj::AsyncIoStream> connection);






  kj::Promise<bool> listenHttpCleanDrain(kj::AsyncIoStream& connection);







private:
  class Connection;

  kj::Timer& timer;
  const HttpHeaderTable& requestHeaderTable;
  kj::OneOf<HttpService*, HttpServiceFactory> service;
  Settings settings;

  bool draining = false;
  kj::ForkedPromise<void> onDrain;
  kj::Own<kj::PromiseFulfiller<void>> drainFulfiller;

  uint connectionCount = 0;
  kj::Maybe<kj::Own<kj::PromiseFulfiller<void>>> zeroConnectionsFulfiller;

  kj::TaskSet tasks;

  HttpServer(kj::Timer& timer, const HttpHeaderTable& requestHeaderTable,
             kj::OneOf<HttpService*, HttpServiceFactory> service,
             Settings settings, kj::PromiseFulfillerPair<void> paf);

  kj::Promise<void> listenLoop(kj::ConnectionReceiver& port);

  void taskFailed(kj::Exception&& exception) override;
};




inline void HttpHeaderId::requireFrom(const HttpHeaderTable& table) const {
  
# 988 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 if (__builtin_expect(
# 988 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
 this->table == nullptr || this->table == &table
# 988 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 , true)); else ::kj::_::inlineRequireFailure( "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h", 988, 
# 988 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
 "this->table == nullptr || this->table == &table"
# 988 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 , 
# 988 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
 "\"the provided HttpHeaderId is from the wrong HttpHeaderTable\""
# 988 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 , 
# 988 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
 "the provided HttpHeaderId is from the wrong HttpHeaderTable"
# 988 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h" 3
 )
                                                                    
# 989 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/compat/http.h"
                                                                   ;
}

inline kj::Own<HttpHeaderTable> HttpHeaderTable::Builder::build() { return kj::mv(table); }
inline HttpHeaderTable& HttpHeaderTable::Builder::getFutureTable() { return *table; }

inline uint HttpHeaderTable::idCount() const { return namesById.size(); }

inline kj::StringPtr HttpHeaderTable::idToString(HttpHeaderId id) const {
  id.requireFrom(*this);
  return namesById[id.id];
}

inline kj::Maybe<kj::StringPtr> HttpHeaders::get(HttpHeaderId id) const {
  id.requireFrom(*table);
  auto result = indexedHeaders[id.id];
  return result == nullptr ? kj::Maybe<kj::StringPtr>(nullptr) : result;
}

inline void HttpHeaders::unset(HttpHeaderId id) {
  id.requireFrom(*table);
  indexedHeaders[id.id] = nullptr;
}

template <typename Func>
inline void HttpHeaders::forEach(Func&& func) const {
  for (auto i: kj::indices(indexedHeaders)) {
    if (indexedHeaders[i] != nullptr) {
      func(table->idToString(HttpHeaderId(table, i)), indexedHeaders[i]);
    }
  }

  for (auto& header: unindexedHeaders) {
    func(header.name, header.value);
  }
}

template <typename Func1, typename Func2>
inline void HttpHeaders::forEach(Func1&& func1, Func2&& func2) const {
  for (auto i: kj::indices(indexedHeaders)) {
    if (indexedHeaders[i] != nullptr) {
      func1(HttpHeaderId(table, i), indexedHeaders[i]);
    }
  }

  for (auto& header: unindexedHeaders) {
    func2(header.name, header.value);
  }
}

}
# 25 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/async-coroutine-test.c++" 2
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h" 1
# 107 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h"
       






# 113 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h"

# 113 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h" 3



# 115 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h" 3
namespace kj {
# 351 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h" 3
namespace _ {

class Debug {
public:
  Debug() = delete;

  typedef LogSeverity Severity;
# 367 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h" 3
  static inline bool shouldLog(LogSeverity severity) { return severity >= minSeverity; }


  static inline void setLogLevel(LogSeverity severity) { minSeverity = severity; }




  template <typename... Params>
  static void log(const char* file, int line, LogSeverity severity, const char* macroArgs,
                  Params&&... params);

  class Fault {
  public:
    template <typename Code, typename... Params>
    Fault(const char* file, int line, Code code,
          const char* condition, const char* macroArgs, Params&&... params);
    Fault(const char* file, int line, Exception::Type type,
          const char* condition, const char* macroArgs);
    Fault(const char* file, int line, int osErrorNumber,
          const char* condition, const char* macroArgs);




    ~Fault() noexcept(false);

    __attribute__((noinline)) void fatal() __attribute__((noreturn));


  private:
    void init(const char* file, int line, Exception::Type type,
              const char* condition, const char* macroArgs, ArrayPtr<String> argValues);
    void init(const char* file, int line, int osErrorNumber,
              const char* condition, const char* macroArgs, ArrayPtr<String> argValues);





    Exception* exception;
  };

  class SyscallResult {
  public:
    inline SyscallResult(int errorNumber): errorNumber(errorNumber) {}
    inline operator void*() { return errorNumber == 0 ? this : nullptr; }
    inline int getErrorNumber() { return errorNumber; }

  private:
    int errorNumber;
  };

  template <typename Call>
  static SyscallResult syscall(Call&& call, bool nonblocking);
  template <typename Call>
  static int syscallError(Call&& call, bool nonblocking);
# 432 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h" 3
  class Context: public ExceptionCallback {
  public:
    Context();
    Context(const Context&) = delete; Context& operator=(const Context&) = delete;
    virtual ~Context() noexcept(false);

    struct Value {
      const char* file;
      int line;
      String description;

      inline Value(const char* file, int line, String&& description)
          : file(file), line(line), description(mv(description)) {}
    };

    virtual Value evaluate() = 0;

    virtual void onRecoverableException(Exception&& exception) override;
    virtual void onFatalException(Exception&& exception) override;
    virtual void logMessage(LogSeverity severity, const char* file, int line, int contextDepth,
                            String&& text) override;

  private:
    bool logged;
    Maybe<Value> value;

    Value ensureInitialized();
  };

  template <typename Func>
  class ContextImpl: public Context {
  public:
    inline ContextImpl(Func& func): func(func) {}
    ContextImpl(const ContextImpl&) = delete; ContextImpl& operator=(const ContextImpl&) = delete;

    Value evaluate() override {
      return func();
    }
  private:
    Func& func;
  };

  template <typename... Params>
  static String makeDescription(const char* macroArgs, Params&&... params);

private:
  static LogSeverity minSeverity;

  static void logInternal(const char* file, int line, LogSeverity severity, const char* macroArgs,
                          ArrayPtr<String> argValues);
  static String makeDescriptionInternal(const char* macroArgs, ArrayPtr<String> argValues);

  static int getOsErrorNumber(bool nonblocking);

};

template <typename... Params>
void Debug::log(const char* file, int line, LogSeverity severity, const char* macroArgs,
                Params&&... params) {
  String argValues[sizeof...(Params)] = {str(params)...};
  logInternal(file, line, severity, macroArgs, arrayPtr(argValues, sizeof...(Params)));
}

template <>
inline void Debug::log<>(const char* file, int line, LogSeverity severity, const char* macroArgs) {
  logInternal(file, line, severity, macroArgs, nullptr);
}

template <typename Code, typename... Params>
Debug::Fault::Fault(const char* file, int line, Code code,
                    const char* condition, const char* macroArgs, Params&&... params)
    : exception(nullptr) {
  String argValues[sizeof...(Params)] = {str(params)...};
  init(file, line, code, condition, macroArgs,
       arrayPtr(argValues, sizeof...(Params)));
}

inline Debug::Fault::Fault(const char* file, int line, int osErrorNumber,
                           const char* condition, const char* macroArgs)
    : exception(nullptr) {
  init(file, line, osErrorNumber, condition, macroArgs, nullptr);
}

inline Debug::Fault::Fault(const char* file, int line, kj::Exception::Type type,
                           const char* condition, const char* macroArgs)
    : exception(nullptr) {
  init(file, line, type, condition, macroArgs, nullptr);
}
# 541 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h" 3
template <typename Call>
Debug::SyscallResult Debug::syscall(Call&& call, bool nonblocking) {
  while (call() < 0) {
    int errorNum = getOsErrorNumber(nonblocking);



    if (errorNum != -1) {
      return SyscallResult(errorNum);
    }
  }
  return SyscallResult(0);
}

template <typename Call>
int Debug::syscallError(Call&& call, bool nonblocking) {
  while (call() < 0) {
    int errorNum = getOsErrorNumber(nonblocking);



    if (errorNum != -1) {
      return errorNum;
    }
  }
  return 0;
}

template <typename... Params>
String Debug::makeDescription(const char* macroArgs, Params&&... params) {
  String argValues[sizeof...(Params)] = {str(params)...};
  return makeDescriptionInternal(macroArgs, arrayPtr(argValues, sizeof...(Params)));
}

template <>
inline String Debug::makeDescription<>(const char* macroArgs) {
  return makeDescriptionInternal(macroArgs, nullptr);
}
# 615 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h" 3
template <typename T>
struct DebugExpression;

template <typename T, typename = decltype(toCharSequence(instance<T&>()))>
inline auto tryToCharSequence(T* value) { return kj::toCharSequence(*value); }
inline StringPtr tryToCharSequence(...) { return "(can't stringify)"_kj; }


template <typename Left, typename Right>
struct DebugComparison {
  Left left;
  Right right;
  StringPtr op;
  bool result;

  inline operator bool() const { return __builtin_expect(result, true); }

  template <typename T> inline void operator&(T&& other) = delete;
  template <typename T> inline void operator^(T&& other) = delete;
  template <typename T> inline void operator|(T&& other) = delete;
};

template <typename Left, typename Right>
String operator*(::kj::_::Stringifier, DebugComparison<Left, Right>& cmp) {
  return _::concat(tryToCharSequence(&cmp.left), cmp.op, tryToCharSequence(&cmp.right));
}

template <typename T>
struct DebugExpression {
  DebugExpression(T&& value): value(kj::fwd<T>(value)) {}
  T value;
# 654 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h" 3
  template <typename U> DebugComparison<T, U> operator ==(U&& other) { bool result = value == other; return { kj::fwd<T>(value), kj::fwd<U>(other), " " "==" " "_kj, result }; };
  template <typename U> DebugComparison<T, U> operator !=(U&& other) { bool result = value != other; return { kj::fwd<T>(value), kj::fwd<U>(other), " " "!=" " "_kj, result }; };
  template <typename U> DebugComparison<T, U> operator <=(U&& other) { bool result = value <= other; return { kj::fwd<T>(value), kj::fwd<U>(other), " " "<=" " "_kj, result }; };
  template <typename U> DebugComparison<T, U> operator >=(U&& other) { bool result = value >= other; return { kj::fwd<T>(value), kj::fwd<U>(other), " " ">=" " "_kj, result }; };
  template <typename U> DebugComparison<T, U> operator <(U&& other) { bool result = value < other; return { kj::fwd<T>(value), kj::fwd<U>(other), " " "<" " "_kj, result }; };
  template <typename U> DebugComparison<T, U> operator >(U&& other) { bool result = value > other; return { kj::fwd<T>(value), kj::fwd<U>(other), " " ">" " "_kj, result }; };
# 669 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/debug.h" 3
  template <typename U> inline auto operator <<(U&& other) { return DebugExpression<decltype(kj::fwd<T>(value) << kj::fwd<U>(other))>( kj::fwd<T>(value) << kj::fwd<U>(other)); };
  template <typename U> inline auto operator >>(U&& other) { return DebugExpression<decltype(kj::fwd<T>(value) >> kj::fwd<U>(other))>( kj::fwd<T>(value) >> kj::fwd<U>(other)); };
  template <typename U> inline auto operator &(U&& other) { return DebugExpression<decltype(kj::fwd<T>(value) & kj::fwd<U>(other))>( kj::fwd<T>(value) & kj::fwd<U>(other)); };
  template <typename U> inline auto operator ^(U&& other) { return DebugExpression<decltype(kj::fwd<T>(value) ^ kj::fwd<U>(other))>( kj::fwd<T>(value) ^ kj::fwd<U>(other)); };
  template <typename U> inline auto operator |(U&& other) { return DebugExpression<decltype(kj::fwd<T>(value) | kj::fwd<U>(other))>( kj::fwd<T>(value) | kj::fwd<U>(other)); };


  inline operator bool() {



    return value;
  }
};

template <typename T>
StringPtr operator*(::kj::_::Stringifier, const DebugExpression<T>& exp) {


  return "false"_kj;
}

struct DebugExpressionStart {
  template <typename T>
  DebugExpression<T> operator<<(T&& value) const {
    return DebugExpression<T>(kj::fwd<T>(value));
  }
};
static constexpr DebugExpressionStart MAGIC_ASSERT;

}
}


# 26 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/async-coroutine-test.c++" 2
# 1 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/test.h" 1
# 22 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/test.h"
       







# 29 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/test.h"

# 29 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/test.h" 3


namespace kj {

class TestRunner;

class TestCase {
public:
  TestCase(const char* file, uint line, const char* description);
  ~TestCase();

  virtual void run() = 0;

private:
  const char* file;
  uint line;
  const char* description;
  TestCase* next;
  TestCase** prev;
  bool matchedFilter;

  friend class TestRunner;
};
# 119 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/../kj/test.h" 3
namespace _ {

bool hasSubstring(kj::StringPtr haystack, kj::StringPtr needle);


bool expectFatalThrow(Maybe<Exception::Type> type, Maybe<StringPtr> message,
                      Function<void()> code);





class LogExpectation: public ExceptionCallback {
public:
  LogExpectation(LogSeverity severity, StringPtr substring);
  ~LogExpectation();

  void logMessage(LogSeverity severity, const char* file, int line, int contextDepth,
                  String&& text) override;

private:
  LogSeverity severity;
  StringPtr substring;
  bool seen;
  UnwindDetector unwindDetector;
};

class GlobFilter {




public:
  explicit GlobFilter(const char* pattern);
  explicit GlobFilter(ArrayPtr<const char> pattern);

  bool matches(StringPtr name);

private:
  String pattern;
  Vector<uint> states;

  void applyState(char c, int state);
};

}
}


# 27 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/async-coroutine-test.c++" 2


# 28 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/async-coroutine-test.c++"
namespace kj {
namespace {
# 568 "/home/vlovich/cloudflare/capnproto-join-promises/c++/src/kj/async-coroutine-test.c++"
}
}
